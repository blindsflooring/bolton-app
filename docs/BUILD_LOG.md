# Bolton Build Log

This is the permanent, narrative build history of Bolton — Blinds & Flooring
Studio's internal quoting/job-workflow app. It exists so Burgert (or any
future developer who inherits this codebase) has a real account of what was
built, why, and what's still open — not just a list of commit messages.

**Ongoing practice:** every future work session appends a new dated entry
here before the session is considered wrapped up. Reverse-chronological —
newest entry at the top.

For the pre-repo history (v1–v65, before this git repository existed), see
[`CHANGELOG.md`](../CHANGELOG.md) in the repo root — it was carried in as-is
at the very first commit and is the authoritative record of that earlier
period. This file picks up from the repo's actual creation (2026-08-19) and
continues forward from there, reconstructed from the real git commit
history (129 commits, 2026-08-19 → 2026-08-28) rather than from memory.

---

## 2026-09-04

Builder Portal, second pass — everything Burgert asked for after actually using the Builders screen: opening a builder, editing and deleting their estimates, deleting the builder, the missing screed and trim on the portal itself, the stairwell exclusion, and the real financials.

### What shipped
- **Per-builder screen, delete builder, delete lead** (carried over from the previous session's uncommitted work, now finished and committed) — a builder's name is the click-through on both the Builders and Report tables; the detail screen carries editable name/phone/email, their KPIs, everything they quoted on, and an activity timeline (`BuilderPortalVisit` for "viewed the portal" merged at read time with their own `BuilderEstimate` rows for "submitted", never a second event log). `DELETE /admin/builders/{id}` reuses `_delete_builder_estimate()` for every one of their estimates first, same force/dependency semantics as the existing bulk delete. `DELETE /leads/{id}` is Owner-only and refuses a converted lead.
- **Screed is now priced on the portal** (Burgert's own words: "Have a look and see if the Screed prices gets pulled into the builders portals prices" — the answer was **no**). `submit_builder_estimate()` hardcoded `JobType.smooth` and priced exactly ONE flooring material line, so a builder quoting a tiled floor got a price with zero floor prep in it, while the same job built internally carries a second, screed line at the same m². The portal now asks "what is the floor going onto?" and adds that prep line automatically. Burgert chose this over merely flagging it as a note.
- **Reducer trim, per door opening** ("Only one trim, Reducing profile per door width opening. Leave skirtings"). The builder enters how many doorways the floor runs through; the linear metres come from a new `BusinessSettings.builder_portal_door_width_m` (0.9m). Skirtings deliberately excluded.
- **Stairwell exclusion said out loud** ("We need to say on the builders portal that a stairwell we need to qte as a company") — one backend-served notice, shown on the estimate form, on the result, on My Statement and on the printed letterhead.
- **Offline edit of a builder's estimate** (`PUT /admin/builder-estimates/{id}`) — client details, area, product, floor condition and door openings, Owner-only and audit-logged. Any pricing input changing reprices the whole estimate through the same function the portal itself uses.
- **Builder financials** (`GET /admin/builders/{id}/financials`) — "What I owe them, When its paid, if its paid, their invoices." Asked which invoices, Burgert chose ours to the end client, so each row pairs our invoice for a referred job (sent date, amount, whether the client has paid us) with what that referral costs us (commission, whether it has been paid out, and when).
- **Price-book plumbing** — a "Builder portal" tick on reducer rows in the trim price book (offered only on reducers, mirroring the backend's own filter), and the Supplier Console's `available_to_builder_portal` label now says what that flag means on a screed product.
- **Login: "I have to enter twice because the first time always hangs"** — fixed. `showLogin()` blanked `login_username`, and it is called from `checkAuthOnLoad()` *after* its `/auth/me` round trip, i.e. seconds into the page load, by which time the browser has already autofilled the form. The username was wiped while the password dots stayed on screen, so the form looked filled in but the next click just answered "Enter a username and password". Clearing now happens in `doLogout()` — the only path where a different person signing in is the actual intent. Also stopped the fetch wrapper claiming "this account logged in elsewhere" for the boot check's own 401; it now says the session expired, which is what it actually is.
- **"Screed and the trims still does not pull into the builders section"** — the pricing was working; the two switches that turn it on were in the wrong place. Exposing floor prep meant finding the right screed row in the Supplier Console, which doesn't display `pricing_type` at all, so nothing on screen says which row IS the screed one; the trim meant a tick buried in the Price Book's trim tree. Neither is on the Builder Portal screen, which is where you are when you set up the builder portal. New "What the portal offers" panel there: the floors builders can currently pick, plus dropdowns for the floor prep product and the reducer trim, plus the metres-per-door-opening figure. Backed by `GET`/`PUT /admin/builder-portal/settings`, which swaps the flag off the previous selection and onto the new one in one transaction (the portal resolves exactly one of each, so two flagged wouldn't add an option — it would just make which one wins depend on sort order). Refuses a non-screed product as floor prep and a non-reducer as the trim, each with a reason. States what is *not* set in plain words, since "no floor prep chosen" is the difference between a real over-tiles price and one with no prep in it.
- **"My Commission" tab on the builder's own portal** (Burgert: "I also need a tab to show the builder his potensial commision that will be paid to him"). Three deliberately separate figures, never one blended total: **Earned** (job finished and paid for — and now also whether we have paid it out, and when), **In progress** (a real job, priced off the real quote, waiting on the client's final payment), **Coming up** (still just an estimate; the client may never go ahead and the final quote can differ).

### Why (root causes, decisions, rejected alternatives)
- **The screed gap was structural, not a missing feature request.** The portal's own docstring claimed the price "structurally cannot drift" from the internal calculator because both run through `calculate_flooring_line`. True for the one line it built — but the internal calculator's answer to a tiled floor is *two* lines, and the portal could only ever produce one. It was matching a job structure nobody quotes.
- **One pricing path, extracted.** `submit` and `confirm` already carried two hand-kept copies of the material calc. Adding screed and trim would have made three, plus a fourth for the offline edit. All four now call `_price_builder_estimate()`; verified in disposable that the portal's quoted figure, the real Quote's line totals, and a hand-run internal calculation for the same job all came to R29 365.76 to the cent.
- **The screed product is flagged with the SAME `available_to_builder_portal` field, split by `pricing_type`** rather than a second flag — one "exposed to the portal" concept in the price book, not two that can disagree. It is excluded from the pickable-product dropdown (otherwise a builder could quote bare levelling compound as their finished floor) and from the 2-range cap (that cap is about how many ranges a builder chooses between; prep is not a choice they make, and counting it would cost Burgert one of his two real ranges).
- **Nothing flagged means the question is not asked.** No screed product flagged → no floor-condition dropdown at all, rather than showing it and pricing prep at R0 — which is the exact silent underquote this work exists to fix.
- **Editing a confirmed estimate is refused, not worked around.** Once it is a real Quote, the quote is the record, editable in the Order Index with its own audit trail and margin checks. Rewriting the estimate underneath it would leave two disagreeing versions of one job with no way to tell which was invoiced. The 400 says exactly that, and names the quote.
- **The two new snapshot id columns are NOT foreign keys**, unlike the pre-existing `product_id`. The stored *names* are the durable record and nothing reads the ids back for logic; a real FK would make retiring a screed product or a reducer from the price book fail on Postgres for as long as any old estimate mentions it, and would disagree with this feature's own ALTER TABLE migration, which adds them as plain INTEGERs.
- **`material_price_ex_vat` is backfilled, not left at 0.0.** Every pre-existing estimate genuinely was material-only, so its ex-VAT total IS its material line. Left at the ALTER TABLE default, an old estimate would have rendered as "R0 + R0 + R0" against a real non-zero total on the statement and the printed letterhead — visibly broken, not merely empty.
- **The login bug happened *every* time, not occasionally, because sessions are a fixed 24h.** A stale token sitting in localStorage is the normal state every morning, so the expired-token boot path — the one that wipes the field after autofill — is the path nearly every real login takes. It was only ever a race in appearance; the ordering (network round trip vs. autofill) is deterministic.
- **`/auth/me` excluded from the generic 401 handler.** It has exactly one call site, which already clears the dead token itself and can say accurately what happened. The shared handler was overwriting that with a confident, wrong explanation on the single most common path through it.
- **"Doesn't pull through" was a discoverability bug, not a pricing one.** The build was correct and verified, but shipping a feature whose only on-switch lives on a different screen, in a console that doesn't show the one field distinguishing the right row, is the same as not shipping it. The fix was to move the switch to where the work happens and to make the off-state say what it costs you.
- **Potential commission is split three ways rather than summed.** Adding "earned", "in progress" and "coming up" into one number would tell a builder he is owed money for work nobody has bought yet. Pipeline commission is computed off the estimate's own snapshot (there is no quote yet); in-progress commission off the real quote's ex-VAT total, which is what will actually be paid on. Both use the same `builder.commission_pct` the earned figure uses — one rate, never a second copy.

### Verified (second pass)
- 24 further checks through the real endpoints, starting from the state actually reported — nothing flagged to the portal at all: the panel reports both extras as off and the live portal genuinely doesn't ask about tiles or door openings; turning both on in one call makes the portal offer them; screed still never appears as a pickable floor; a stair nose is refused as the portal trim and a vinyl range as the floor prep, each with a readable reason; commission splits correctly across earned/in-progress/pipeline with no double-counting, and what the builder sees as earned matches Burgert's own financials view to the cent.

### Verified
- Disposable SQLite + real `TestClient` against the real endpoints, with a real owner login (not dependency overrides — the closed-by-default `require_auth` middleware runs before dependencies, so overriding those would have tested a path no real request can take). 40 checks: a smooth floor still gets exactly one line; over-tiles gets floor + prep + trim; the breakdown sums to the quoted total; a bad `job_type` is a clean 400 rather than a `KeyError` 500; the confirmed Quote's three real lines total exactly what the builder was shown; the offline edit reprices and is refused once linked; commission is 6% of the real invoice value and moves from owed to paid with a date.
- Migration checked against a *pre-existing* database, not just a fresh one — new columns stripped back out to simulate live Supabase, then `_ensure_new_columns()` run: all twelve columns added, existing rows default to `smooth`/0 (the literal truth for them), and an old estimate still renders one correct R9 000 line rather than R0.

### Deferred / parked
- Nothing from this brief. Direct email sending stays banked (see [BANKED-DECISIONS.md](../BANKED-DECISIONS.md)).

### Open going into next session
- **Burgert must flag the two price-book products before any of this shows up on the portal**: a screed product (Supplier Console → tick "Available to Builder Portal" on e.g. deZIGN S200 screed) and a reducer (Price Book → Trims → "Builder portal" tick). Until then the portal keeps behaving exactly as it did — material only, no floor-condition question, no trim.
- `builder_portal_door_width_m` defaults to 0.9m; confirm that matches the real door openings being quoted.

---

## 2026-09-02 (6)

### What shipped
- **Leads: Source now editable after saving — built and verified same day** (Burgert's own words: "change the where did they hear about us after we saved the lead"). The New Lead form already asked for Source at creation, but the Edit Lead form never carried the field at all — locked in forever once saved. Added the identical Source options to the Edit Lead form and to `saveLeadEdit()`'s own PUT body; no backend change needed, `update_lead()`'s existing generic update already allowed it through. Verified in disposable: edited a real lead's Source through the actual UI function and confirmed it updated immediately.

---

## 2026-09-02 (5)

### What shipped
- **Assigned Leads / To-Dos / Calendar: Stage 2 (To-Dos) + Stage 3 (Calendar integration) — built and verified same day**, continuing directly from Stage 1 ("let's get them built," Burgert's own words once Stage 1 was confirmed working).
  - **Stage 2 — To-Dos**: a genuinely minimal, standalone `ToDo` table (title, assigned_to, due_date, done/done_at) — confirmed directly against the brief's own question ("how it differs from a Lead") that it needed none of Lead's sales-process machinery (no `lead_status`, no mandatory proof-of-work outcome note). New `todos.js` module reusing the exact visual language `leads.js` already established (Open/Done/All tabs, inline reassign dropdown, "By Person" grouping) — not a rebuild. New "To-Dos" tile.
  - Home's "My Leads Today" card became **"My Leads & To-Dos Today"** — leads needing action plus open to-dos due today-or-earlier, combined in one place, per the brief's own explicit "not something they have to hunt for across separate screens."
  - **Stage 3 — Calendar integration**: `calendarJobsForMonth()` now also pulls Lead visit dates and To-Do due dates into the same per-day chip structure jobs already use, each with its own distinct colour so a mixed day reads at a glance. New per-type visibility checkboxes (Installations / Lead visits / To-Dos, default all on) — the density concern the original proposal flagged, now a real, working toggle.
  - **Deliberate scope boundary**: lead/to-do chips are click-only, never wired into the existing drag-and-drop reschedule mechanism — that writes to a Quote's own `installation_date`/work-day via two specific existing endpoints, and extending drag semantics to two more date fields on two more entities was never confirmed as part of this stage.
  - Verified end-to-end in disposable: full To-Do CRUD via both the API and the real UI, a lead-visit-today and a todo-due-today both correctly appearing as distinctly-styled calendar chips, visibility toggles actually hiding/showing each type, the expanded day list rendering both new row types, clicking a lead chip opening its real Lead Detail screen, and To-Dos' own By Person grouping reflecting a real reassignment.

---

## 2026-09-02 (4)

### What shipped
- **New KPI Metrics: switched to tiles** — Burgert's own follow-up after confirming the numbers were correct: "I need the information to be in tiles." All three new sections (m² Quoted/Sold/Installed, Top-Selling Floors & Colours, Quotes Created — By Person) now use the exact same `kpiCard()`/`.tile-grid` pattern already established elsewhere on Business Overview, not a new component. `kpiCard()` hoisted from a local closure to a shared module-level function since Top Sellers' own toggle can re-render independently, well after the closure that used to hold it had already returned.
- **Assigned Leads, Stage 1 — proposal investigated, reviewed, approved, then built and verified same day** (`Bolton-Brief-Leads-Todos-Calendar-Integration.docx`, a genuinely large 5-section initiative, explicitly "no code until a full proposal... is reviewed and approved"). Investigated first: `Lead` had no assignment field at all (only `created_by`); an ageing/overdue signal already existed (`_lead_next_action` — 3+ days with no logged outcome already surfaces "Follow up lead") but was never shown anywhere personal; no To-Do concept exists anywhere in Bolton; Home was a plain tile grid with zero per-person content.
  - Presented the findings plus three genuinely open decisions to Burgert before building anything: Madri's access level (confirmed **view + reassign**, not view-only), what "enforced" should mean (confirmed **surface the existing signal personally** — no new hard check-in mechanism), and build order (confirmed **Stage 1 alone first** — assignment + enforcement + visibility; the To-Do concept and full Calendar integration are separate, later stages, deliberately not started).
  - Shipped: `Lead.assigned_to` (defaults to the creator, explicit hand-off allowed at creation); a new "My Leads Today" card on Home, personal to whoever's actually logged in (`currentUser.username`, never swapped by Owner Preview Mode), reusing the existing staleness signal, hidden entirely when nothing's due; an Assigned column + inline reassign dropdown on the Leads table; a "By Person" toggle grouping the same filtered rows under a header per assignee for Madri's own confirmed team-tracking need. Reassignment needed no new endpoint — the existing generic `PUT /leads/{id}` already allowed the new field through.
  - Verified in disposable end-to-end: default assignment on creation, explicit assignment, filtering, reassignment persisting server-side, Home's personalized card showing exactly the right lead with its real priority flag, and the By Person grouping rendering correctly.

---

## 2026-09-02 (3)

### What shipped
- **New KPI Metrics: m² Quoted/Sold/Installed, Top Sellers, Quotes Per Person — proposal reviewed and approved, then built and verified same day** (`Bolton-Brief-New-KPI-Metrics.docx`, "Investigate and propose only... Confirm the real underlying data supports each metric before proposing the exact display"). Three new cards on the existing Business Overview Dashboard, not a separate screen, reusing the SAST week/month bucketing already built there for Sales & Profit.
  - **Real gap found while investigating, before proposing anything**: every screeded job carries TWO flooring-category `QuoteLineItem` rows — a material line and a screed line — with the IDENTICAL `quantity_m2`, confirmed directly against all 17 real production quotes at the time (material sum and screed sum both exactly 647.8m², to the decimal). A naive sum would have double-counted every screeded job. Filtered to `flooring_pricing_type == "material"` only — correctly includes carpet, correctly excludes screed.
  - Presented the findings and the one genuinely open question (Top Sellers: Sold-only vs. all quotes) to Burgert before building anything — confirmed **Sold only** ("a colour that gets quoted often but never accepted isn't really 'selling'").
  - Shipped: m² Quoted/Sold/Installed (this week + this month), Top-Selling Floors & Colours (Month/Year toggle, same UI pattern as the existing Weekly Graph), Quotes Created — By Person (Today/Week/Month per rep).
- **Order Index: Priority Ordering, Grouped-Client Fix, Client Name Visibility — built and verified same day** (`Bolton-Brief-OrderIndex-Priority-Sort.docx`). The brief's own 3 sort buckets (Scheduled / follow-up-needed Quoted / fresh Quoted) didn't say where Accepted or Completed fit — confirmed directly with Burgert rather than guessing: Accepted sits right after Scheduled, Completed sinks to the very bottom even if still needing invoicing.
  - **Grouped-client fix — a real risk, not just tidiness** (Burgert's own framing): a client with jobs at different stages used to group ALL of them under one collapsed row with a "Mixed" badge — a Scheduled job could sit invisible inside a row reading "still just quoted." Grouping is now scoped to `workflow_status === "quoted"` only; anything progressed beyond Quoted always renders standalone.
  - **Client name visibility — real gap found by reading the actual CSS before touching anything**: `.card-title` (the group header's own name styling) only had a rule inside the mobile breakpoint — completely unstyled on desktop, while a single row's name was teal+underline (linked) or plain (walk-in), and a child row inside an expanded group showed no name at all. One shared renderer + `.oi-client-name` now gives consistent size/weight/colour in all three row shapes, keeping the meaningful linked-vs-walk-in distinction.
  - Verified against the brief's own testing requirement: built a real test client with 3 jobs at different statuses in disposable, confirmed the Scheduled job sorts first and renders standalone, the two Quoted jobs group together, and child rows show the client name. Confirmed against live production data too.
- **Exclude Declined Alternative Quotes From KPIs and the Main Order Index — built and verified same day** (`Bolton-Brief-Decline-Quote-KPI-Exclusion.docx`, "Check first: was the 'Decline Quote reason' feature... ever actually built?"). Confirmed it was — `decline_quote()` already requires a real reason, logs to AuditLog, sets `declined_at`. This brief only needed to wire that into KPI/Order Index logic.
  - `GET /quotes` gained `include_declined` (default False, same pattern as the existing `include_price_checks`); `m2_metrics.quoted` now excludes declined quotes (Sold/Installed and top-sellers were already correctly excluding them — both require `accepted_at`, which a declined quote can never have). Conversion-rate figures deliberately left untouched — a declined quote already correctly counts as "lost" there.
  - New, genuinely separate "Declined Quotes" card on the Order Index (own fetch, own toggle, off by default) — not folded into the existing status tabs, since a declined quote's `workflow_status` stays "quoted" forever.
  - Verified against a real multi-quote client scenario in disposable: declined one of a test client's 3 quotes, confirmed it vanished from the default view immediately, confirmed the remaining single quoted job correctly ungrouped, confirmed the Declined toggle revealed it with its decline date.

---

## 2026-09-02 (2)

### What shipped
- **Builders Management Console — built and verified same day** (Burgert, in chat, a single consolidated request following the morning's Builder Portal audit): "Get the builders portal that the quote can be printed on our letterhead. Let the Builder be able to press a button to continue with the order, We will process it on this side... Make it that the % commision can be shown on their commision statement. I need to be able to make changes and also have a reporting side where I can be in control, Add it to the order index and let me be able to process the order from there. Give me a list of the builders, the work that they have quoted on and also what has been bought and done and what I owe them. a Builders management consol attached to the builders tile."
  - **Letterhead print**: new public `GET /builder/{slug}/estimate/{estimate_id}/print` — hand-picked business letterhead fields (name/address/phone/email/VAT/bank details) plus the estimate's own already-public figures. `builder.html` has no `shared.js`/session (by design — see its own header comment), so this is a self-contained `window.print()` via a hidden `#printArea` + `@media print`, mirroring the exact pattern the staff-facing quote/invoice print already uses (`buildPrintDocHtml()`/`triggerPrint()`, shared.js), just hand-rolled locally since this page can't import that file.
  - **"Continue with this order" — the real, load-bearing piece**: new public `POST /builder/{slug}/estimate/{estimate_id}/confirm`. A builder pressing it creates a REAL Quote automatically — same client-resolution safety net (`_resolve_or_create_client`) every other quote-creation path already uses, and the identical flooring-line pricing calc `submit_builder_estimate()` used for the original figure, so the quote that lands never drifts from what the builder already saw. Backfills the client's phone/email/address/marketing source from what the builder captured at estimate time (only into genuinely blank fields, never overwriting real data on an existing client), links the estimate, transfers any attached photos, and is idempotency-guarded against a double-tap/reload.
  - **"Add it to the order index" — satisfied with zero new Order Index code**: confirmed by reading `order-index.js` directly first that `quote.description` already renders prominently everywhere (table rows, Needs Attention, Job Detail's own header). Setting `description = "Builder referral — {name}"` on the auto-created quote makes every builder-confirmed order visibly identifiable throughout the whole app immediately — "we will process it on this side" is then just the normal Order Index accept/schedule/invoice workflow every other quote already goes through, nothing builder-specific left to build there.
  - **Per-builder commission, Owner-editable**: `Builder.commission_pct` (was a single hardcoded 6% constant, `BUILDER_COMMISSION_PCT`) — now per-builder, inline-editable from the console's Builders tab. `_builder_commission_for_quote()` reads it directly; existing builders keep their real 6% (the migration default matches the old constant exactly, no rate silently changed).
  - **"What I owe them"**: `BuilderEstimate.commission_paid`/`commission_paid_at` — the commission AMOUNT itself stays derived at read time exactly as before (never a duplicated, driftable figure); this is only the one persisted "has it actually been paid out" flag, toggled via a new `PUT /admin/builder-estimates/{id}/mark-commission-paid`.
  - **New Report tab** (`GET /admin/builders/report`) — one row per builder: estimates submitted, total quoted, jobs won, value won, commission owed vs. paid. Literally Burgert's own list.
  - **"Each builder can only see his own quotes" — verified already correct, not a bug**: read `builder_statement()` directly before touching anything — strictly filtered by the resolved `builder.id` from the slug, server-side, never a client-supplied value. Confirmed working as designed; no fix needed.
  - Admin screen renamed "Builders Management Console" (same tile, per Burgert's own "attached to the builders tile"), gained the Report tab, an editable Commission % column, and "Confirmed by builder ✓" / Mark Paid / Undo Paid on the Estimates tab.
  - **Verified end-to-end in disposable, then confirmed the deploy live**: created a real test builder, submitted a real estimate through the public portal, printed it, hit "Continue with this order" and confirmed a real Quote appeared with the exact matching total (R810 ex VAT / R931.50 incl VAT, both sides), the new Client record correctly picked up phone/address/marketing source, simulated the job being fully paid, confirmed commission math at a custom rate (8% → R64.80), marked it paid, and confirmed the Report tab's totals matched throughout (owed → R0, paid → R64.80).

---

## 2026-09-02

### What shipped
- **Builder Portal: full ecosystem audit + two real bugs found and fixed, same day** (Burgert, in chat: "I can log in with new credentials but the button to access the quoting section doesn't work. I need Aspen and Azura to work on the builders portal... See that the whole builders login ecosystem still works and if theres anything outstanding or doesn't work, fix it"). Read the full public `builder.html` (confirmed: no login at all for this pilot, access control is the link itself plus `Builder.active`), the admin Builder Portal screen, and every one of the 4 backend endpoints it depends on, before touching anything.
  - **Root cause #1 — confirmed via real production data, not assumed**: `GET /admin/builders`/`FlooringProduct.available_to_builder_portal` showed 0 of 220 real flooring products enabled — meaning the public estimate form has always shown "No products are currently available through this portal" to every real builder. This is why Aspen and Azura "didn't work": nothing did.
  - **A hard business-rule conflict found before acting on it, not silently resolved**: the Supplier Console's own commit endpoint enforces a hard cap ("2 products max on the Builder Portal at a time"), but Burgert wants ALL colours of both Aspen Living and Azura's deZIGN series 200 enabled — 7 + 15 = 22 real rows. Confirmed directly with Burgert rather than guessing: "Two products, Aspen Living and Azura Series 200. Each comes with their own colours. but its still just two products." Re-scoped `commit_supplier_console_changes()`'s cap check from counting raw `FlooringProduct` rows to counting distinct `product_name` values — a range's colours no longer count against each other.
  - **A real bug caught while verifying that change in disposable, not shipped blind**: the rejection message's range list used `row[0]` on `session.exec()` results from a single-column `select()` — SQLModel already unwraps those to plain strings, so `row[0]` silently took the first *character* of each range name ("Only 2 ranges... (A, I, Z)" instead of "(Aspen Living, Influence, Zenith)"). Fixed before it ever reached production.
  - **Verified the new cap logic with real inserted test rows in disposable** before touching production data: confirmed enabling multiple colours within one already-enabled range doesn't count as a second range, and confirmed a genuine 3rd distinct range is still correctly rejected.
  - **Applied the real fix to production** via the audited `POST /admin/supplier-console/commit` endpoint (never a raw DB edit) — all 7 real "Aspen Living Range 2mm" colours and all 15 real "deZIGN series 200" (Azura) colours now `available_to_builder_portal = true`, logged as 22 individual AuditLog entries same as any other Supplier Console change. **Data-quality note for Burgert, not acted on unilaterally**: found a 16th `deZIGN series 200` row (`id=2`) with a blank colour, a different supplier string ("Azura Distributors" vs "Azura"), and a different trade discount — looks like a stray pre-colour-split legacy row. Left disabled and untouched; flagging for a decision, not guessing.
  - **Root cause #2 — the actual "nothing happens when I click it" bug, found by reproducing the real symptom end-to-end on live production**, not by pattern-matching to the wrong dialog: submitted a real test estimate through the live public portal (now that products exist), then clicked the real "Start Quote" button on the admin Builder Portal → New Quote screen. Confirmed via console/network monitoring that clicking it fired literally nothing — no alert, no request, no error. Traced it to `startQuoteBtn.disabled === true` with no code path that ever set it back to `false`: `startQuoteFromBuilderEstimate()` sets `pendingBuilderEstimateId` (the sanctioned alternative to `pendingClientId` that `createQuote()`'s own guard already allows) but never called `updateStartQuoteButtonState()` — and that function only ever checked `pendingClientId`, with zero awareness of the Builder Estimate exception. A disabled HTML button fires no click handler at all, which is exactly why nothing happened, silently, every time.
  - Fixed both halves: `updateStartQuoteButtonState()` now treats `pendingBuilderEstimateId` as equally valid, and `startQuoteFromBuilderEstimate()` now calls it after setting its pending state — mirroring the sibling `startQuoteForClient()`, which already did both correctly.
  - **Verified end-to-end, twice — once in disposable, once for real on live production**: submitted a real builder estimate (with a real attached site photo) through the live public `builder.html?slug=frikkie-kleynhans` page, confirmed the pricing, photo attachment, and "My Statement" tab all render correctly, then clicked the real "Start Quote" button and watched it correctly create Quote #144, link it back to the estimate (`linked_quote_id`), and transfer the attached photo onto the new quote (confirmed via `GET /quotes/144/photos`). Test quote and test client record deleted afterward via the app's own audited delete endpoints, not left cluttering real data. Three test `BuilderEstimate` rows remain in the admin Estimates table (clearly named "Test Client..." — no delete endpoint exists for that table yet); flagged to Burgert rather than touched via raw DB.
  - **Surfaced, not chased further**: the transferred test photo's Dropbox upload failed with `"unexpected use of the catch-all tag 'other'"` — a `dropbox` Python SDK error shape, not a clean "unauthorized." Same live Dropbox document-save failure Burgert reported at the start of this session's earlier segment. The photo itself is safe either way (Postgres holds the authoritative copy regardless of Dropbox status, confirmed by the Aug 2026 Photo Gallery redesign), but the underlying Dropbox credential/SDK issue is still unresolved — flagged for a decision on whether to dig further now or bank it, per Burgert's own steer earlier this session not to chain investigative tangents without checking in first.

---

## 2026-09-01

### What shipped
- **URGENT — Add Line Form Defaults to an Unrelated Product, investigated and confirmed NOT a data-bleed recurrence, real risk fixed anyway, same day** (`Bolton-Brief-AddLine-Default-Range-Bleed.docx`, "check whether this is a genuine data-bleed recurrence... or a benign default, before assuming either"). Reported symptom: opening real Quote #134 (Vanessa Court) to edit it showed the Add Vinyl Line form defaulting its Range to "deZIGN series 200" — unrelated to anything actually on that quote.
  - **Reproduced directly, per the brief's own explicit instruction, before assuming either cause**: opened multiple different real quotes in sequence (including the exact real #134) and confirmed the Range dropdown always defaulted to the same value regardless of which quote was viewed previously — a generic "first option wins" default, not session residue. Traced the one real path that COULD carry a genuine stale value (`pendingVinylRange`) and confirmed it's reliably nulled right after use at both its call sites — every other caller, including opening an existing quote, passes nothing at all.
  - **Real risk confirmed anyway, same class as the already-fixed Colour Default Risk**: a native `<select>` auto-selects its first real option the instant none of its own options are marked `selected` — silently offering a genuinely unrelated real product that could be added to a real client's saved quote without anyone actually choosing it. Fixed with the identical, already-established placeholder pattern ("— Choose a range —"), applied to `populateVinylRangeDropdown()` (the reported symptom).
  - **Found and fixed the identical shape in a sibling path while checking every similar code site, not just the one reported**: `populateCarpetTypeProducts()` (the Carpet tab's own product dropdown) had the exact same gap, carrying the exact same real risk. Both already had a real downstream guard (`addFloorJob()`'s colour check, `addCarpetLine()`'s own product check) that now correctly fires against the new blank default — no new guard code needed, just the placeholder. Added a companion "Choose a range first" message alongside the existing colour one so the error matches whichever field was actually left unset.
  - **Verified end-to-end on the disposable backend/frontend, in a real browser, reproducing the exact reported sequence**: created two real quotes for two different real clients, each with its own real, different vinyl range already on it — confirmed opening either one always showed the Range dropdown as the blank placeholder, never the other quote's real range, in either order. Confirmed editing an EXISTING line still correctly re-selects its own real range (`prefillFlooringEdit()`'s own explicit preselect, unaffected). Confirmed the "Choose a range first"/"Pick a product first" guards actually block Add when left blank. Confirmed the legitimate Flooring-drill-down "start a quote pre-filled with this exact range" flow still works unchanged.
  - **Re-verified against the real, exact quote named in the brief**: opened real Quote #134 (Vanessa Court) on the live deployed app — the Add Vinyl Line form's Range now correctly shows "— Choose a range —", not "deZIGN series 200".
- **Supplier Console: Freeze Product Name Column While Scrolling — built and verified same day** (`Bolton-Brief-Supplier-Console-Sticky-Column.docx`, "panning horizontally... loses sight of which product (Range) each row actually is"). Confirmed directly, not assumed: Range/Product is the first field in every one of this screen's field arrays (FLOORING/TRIM/BLINDS/FLOOR_PREP_CONSOLE_FIELDS all list `product_name` first), so pinning "whichever cell is index 0" pins the right column for all four entity types with one change.
  - **A real second problem found while implementing the fix, not just the column itself**: this screen's own sticky-header mechanism is a separate DOM clone with its horizontal position kept in sync by JS (`wrapper.onscroll`), not a native scrolling header — pinning only the body column would have left the "Range" header label scrolling away from the now-fixed data underneath it. `setupStickyHeaders()` now detects whichever body cell actually rendered sticky (its position differs by entity type — FlooringProduct has a leading spacer `<td>` the others don't) and applies the identical treatment to the matching header cell, so the two can never drift apart.
  - **Checked every other wide, horizontally-scrolling table in the app before assuming this was Supplier-Console-only, per the brief's own instruction**: Order Index already uses the mobile-card-table stacking pattern (sidesteps the problem on mobile entirely) and isn't wide enough on desktop to have ever needed this. Change Log and the Builder Portal's two tables (Builders, Estimates) are genuinely wide with real horizontal scroll, but both put a date first, not an identifying field — pinning "whichever column is first" there wouldn't serve the same "which row is this" need Range/Product does here, and reordering those tables' own columns is a separate design decision this brief didn't ask for. No changes made to either; documented here rather than silently skipped.
  - **Verified end-to-end on the disposable backend/frontend, in a real browser**: confirmed via direct measurement (not just visually) that the Range column's real pixel position stays completely fixed across repeated scroll positions (800px and 1200px scrollLeft measured identical), confirmed the header's "Range" label stays aligned with it, and confirmed the existing floating scroll-arrows/Back-button fix from the earlier Pending Review Screen brief is unaffected — both still correctly reachable and functional with the new sticky column in place.
  - **Re-verified against real production data**: opened the live Supplier Console — the Range column renders with the correct sticky styling on a real FlooringProduct row.
- **Header Logo Links to Home — built and verified same day** (`Bolton-Brief-Logo-Home-Link.docx`, "standard convention, applied consistently across every screen"). Investigated first: the app's real `<nav>` Home tab button is permanently `display:none` (dead markup), meaning there was previously no consistent way back to Home except scattered, per-screen "← Back" links that don't all lead to the same place — a genuine gap, not a redundant convenience.
  - Wired `#headerLogo`'s `onclick` straight to `goToTab('landing')` — confirmed this is necessary rather than a bare `landingView='tiles'; renderLanding();`, since `goToTab()` also toggles the Quote Builder/Price Book raw-section `display` properties those two screens need to actually disappear; a bare reassignment would have left them showing underneath.
  - **Verified end-to-end on the disposable backend/frontend**: confirmed clicking the logo correctly returns Home from Order Index, an in-progress Quote Builder session (including correctly hiding the raw Quote Builder section), and Supplier Console.
  - **Re-verified against real production data**: confirmed the new `onclick` is live on the real deployed header, and confirmed via a real screenshot that it sits cleanly on the far left with no visual collision with Logged in as / Viewing as / Log out on the far right (structurally separate siblings, confirmed both in code and visually).
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **Job Detail: Client Name Prominence + Installation Date Next to Prepare Job — built and verified same day** (`Bolton-Brief-JobDetail-Client-Date-Prominence.docx`, "the client is, in practice, the single most important piece of context on the page... Job Detail and the Calendar can never disagree"). Two real gaps closed:
  - **Client name prominence**: was rendered via the same shared `.landing-welcome p` rule (13px, muted grey) every other screen's own small subtitle text also uses. Rather than change that shared rule (which would have affected every other screen), added a scoped `.job-detail-client-name` class (19px, 700 weight, navy) to this one line only.
  - **Installation date next to Prepare Job**: the card previously showed "Upcoming" with no date at all. Found the one, uniquely-identifiable hook point — `attention_label === 'Upcoming'` is set in exactly one branch of `_job_workflow_info()` (installation is tomorrow) — and appended `calFormatDate(q.installation_date)` there. Reads `installation_date` directly, the exact same field the Installation Calendar reads/writes, satisfying the brief's own explicit "never a separate or duplicated value" requirement by construction, with no backend change needed.
  - **Verified end-to-end on the disposable backend/frontend**: confirmed the real system date, scheduled a test job for the genuine "tomorrow," and confirmed the primary status strip rendered "Upcoming — 1 September" — a string proven byte-identical to `calFormatDate(installation_date)`, i.e. exactly what the Calendar would show for the same field.
  - **Re-verified against real production data**: opened real quote #50 (J-0003, client "Frikkie Klynhans", genuinely scheduled for 2026-09-01) on the live deployed app — client name renders clearly more prominent, and the card correctly reads "Upcoming — 1 September," matching `installation_date`/`installation_confirmed_date` exactly.
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **Materials Section Should Show the Actual Order Sheets — investigated and proposed, approved same day, built and verified** (`Bolton-Brief-Materials-Section-Order-Sheets.docx`, "actually seeing, adding, or removing a purchase order requires leaving the section that's specifically about materials — exactly the kind of hunting-for-stuff friction this whole redesign has been working to remove").
  - **Investigated the real current structure first, per the brief's own instruction**: Materials showed only a compact status line per sheet ("✓ Ordered"/"Needs ordering") and a Generate button; the real preview + standard View/Edit/Print/Save/Mail action bar lived in Documents instead (`renderOrderSheetPreviewsHtml()`), and Edit there navigated away entirely to a standalone Order Sheet Detail screen — the only place quantity-line-editing, Mark as Placed, and Delete actually lived.
  - **A real finding that shaped the recommendation**: `finalizeOrderSheet()` (Mark as Placed) and `deleteOrderSheet()` were already context-agnostic — `deleteOrderSheet()` already branches on being called from Job Detail's inline panel vs. the standalone screen — meaning the plumbing to surface both inline already existed, unused, before this brief.
  - **Proposed and approved same day**: move the real Order Sheet list (preview + action bar) into Materials, removed from Documents to avoid the exact duplicate list the brief was raised to prevent; add Mark as Placed/Delete as one-click buttons on the same tile; leave quantity-line-editing and "Add extra item" on the standalone Order Sheet Detail screen (real form state, not a glance-and-click action — folding it in would clutter Materials, not help it); Documents' role narrows to just the Dropbox Archive and Job Card.
  - **Built exactly as approved**: `renderOrderSheetPreviewsHtml()` retired, its logic folded into `renderMaterialsSectionHtml()` directly; Documents' call site removed and replaced with a comment recording why, so a future reader doesn't wonder where the previews went.
  - **Verified end-to-end on the disposable backend/frontend**: confirmed a real job's two Order Sheets both render with full preview + action bar directly in Materials; confirmed clicking Mark as Placed inline correctly flips that sheet to "✓ Ordered" and removes its own button without touching the other sheet's state.
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **Materials List: Show Quantity Ordered and Colour — built and verified same day** (`Bolton-Brief-Materials-Quantity-Colour.docx`, "surfacing real, already-existing data, not a new calculation or a duplicated value"). Each Materials tile now reads "{qty} {unit} — {colour(s)}" straight from that sheet's own `OrderSheetLine` rows (`_order_sheet_quantity_colour_summary()`, main.py) — the exact data the Order Sheet document itself is built from — quantity summed per unit, colours the distinct non-blank values, both scoped to that one sheet's id so a job with multiple sheets/colours can never bleed one into another's line.
  - **Verified end-to-end on the disposable backend/frontend**: built a job with two real Order Sheets — a flooring sheet with two colours (Oak 42.5m², Walnut 12m²) and a floor-prep sheet with its own separate colour — confirmed the tiles read exactly "54.5 m² — Oak, Walnut" and "10 bags — N/A" respectively, matching each sheet's own printed document line-for-line, with zero bleed between them.
  - **Re-verified against real production data**: real quote #51 (J-0002) correctly shows "31 boxes — Crystal pine" for its flooring sheet and "24 bags" (no colour, correctly blank) for its floor-prep sheet.
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **URGENT — "Mark as Placed" clicked, status still showed Needs Ordering — real data corrected, root cause diagnosed** (`Bolton-Brief-MarkPlaced-Status-Not-Updating.docx`, "check the real database record directly... a display bug, not a data bug"). Reported on real Order Sheet O-0004 (quote #50, J-0003).
  - **Checked the real database record directly, per the brief's own explicit two-hypothesis framing, before assuming either**: `GET /order-sheets/29` confirmed `status: "draft"` — the click had genuinely not persisted; this was a real failed write, not a stale display of an otherwise-correct value.
  - **Confirmed the endpoint itself is not the fault**: calling `POST /order-sheets/29/finalize` directly succeeded immediately and correctly (200, `status: "placed"`) — the backend logic has no bug. This also directly satisfies the brief's own testing requirement: O-0004 now correctly shows its real "Placed" status everywhere.
  - **Root cause traced to a stale cached frontend bundle, not a code defect**: the real browser tab used to investigate this was found still running JS from before that morning's deploys (a defined function from an already-retired code path was still present in memory), only picking up the current, correct code after an explicit reload. The click most likely fired against an older, cached version of the page rather than failing at the server.
  - **Fix requirement re-checked structurally**: confirmed every place that shows an Order Sheet's placed/not-placed status (`_job_steps()`'s tiles, main.py; the Materials/Documents preview, order-index.js) reads the same live `OrderSheet.status` fresh on every request — no separate cached copy exists anywhere in the app's own code for this to have been a real display-cache bug.
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **URGENT — third recurrence of New Quote Data Bleed, fixed at the root this time; Convert to Real Quote now navigates to Order Index** (`Bolton-Brief-Third-DataBleed-Convert-Navigation.docx`, "this needs a genuinely different level of investigation this time, not another narrow patch"). Exact reported sequence: Price Check → select a vinyl range/colour → Convert to real quote → start a separate new quote for the same product type — the previous quote's values pre-populated again.
  - **Investigated structurally, per the brief's own explicit instruction, rather than assuming the fix was another missing field**: traced every "start a new quote/Price Check" entry point in the app. `onTileClick('blinds')`, `startQuoteWithVinylRange()`, and `startQuoteWithFlooringRange()`'s carpet branch all guard their reset with `if (!currentQuoteId)` — correct while a quote is genuinely still open, but `currentQuoteId` is never cleared just because the user is *done* with a quote. The second recurrence's own fix (`saveQuote()`, confirmed Aug 2026) patched exactly one such "done" event (a regular save); Convert to Real Quote is a second, different one nobody had patched — reproducing the identical bug a third time via a new trigger. `createQuote()`/`startPriceCheck()`/`startQuoteForClient()`/`startQuoteFromBuilderEstimate()` were all confirmed already safe (unconditional resets) — only these three shared the flawed guard.
  - **Real, structural fix**: replaced the proxy variable with the actual signal — `isQuoteBuilderOpen()` (index.html), reading `#quoteBuilder`'s own DOM visibility directly. Confirmed `showSection()`/`goToTab()` (the only code that ever sets that style) and `applyNavState()` (browser back/forward) both always keep it correct, for every navigation path in the app — closing this whole class of bug regardless of how many future actions might complete/convert/finish a quote without remembering to clear a variable.
  - **Convert to Real Quote now navigates straight to the Order Index on success** (§1, confirmed decision), reusing `saveQuote()`'s own established `showRawSection('landing')`/`landingView='orders'` idiom — not `goToTab()`, which would flash the tile menu first — the same action-vs-outcome confirmation principle already used elsewhere, replacing a plain success alert.
  - **Addressed the brief's own explicit architectural request** ("structurally no leftover client-side state... not just another targeted clear-the-fields patch"): `clearStaleQuoteResidue()`'s hand-maintained, twice-already-incomplete field list is replaced by `resetQuoteBuilderFieldsToDefaults()` — sweeps every plain input/select in `#quoteBuilder` back to its own native HTML-authored default (`.defaultValue`/`.defaultChecked`/each `<option>`'s own `selected`), independent of whatever a previous quote's JS left in `.value`. Verified byte-identical to the old list for every field except one confirmed, deliberate exception (labour rate resets to blank, not its `value="45"`), now an explicit override — and, unlike the list, automatically covers any field added later.
  - **Verified end-to-end on the disposable backend/frontend, reproducing the exact reported sequence**: Price Check → Aspen Herringbone, colour Natural → Convert to Real Quote → confirmed navigation to Order Index — then started a second Price Check for a different range (deZIGN series 200): genuinely new quote id, blank range/colour/floor size, zero bleed. Confirmed via the database both records are real and separate. Ran three further back-to-back sequences (Blinds → Start Quote → Save → Blinds again; Carpet category → abandon → different Vinyl range; re-triggering the same guarded function while still genuinely open) — all correctly either reset fresh or preserved real in-progress work, confirming the fix doesn't regress the legitimate "still editing, don't wipe" case.
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **Calendar: Multiple Work Days Per Job — investigated and proposed, approved same day, built and verified** (`Bolton-Brief-Calendar-Multiple-Work-Days.docx`, "Bolton's entire calendar and job data model was built around one single installation_date per job — this brief asks whether that assumption needs to change"). A job like screed-then-flooring genuinely needs more than one on-site date.
  - **Investigated directly, per the brief's own instruction**: confirmed `installation_date`/`installation_confirmed_date` are genuinely the only date fields (models.py) and traced every real consumer — `_job_workflow_info()`'s workflow_status transitions, `schedule_quote()`, the Job Card, Order Index's own column/grouping/status strip, the Calendar's own chip grouping. Confirmed dashboard/KPI logic reads none of it at all (grepped `index.html` directly — zero references), a real negative finding, not assumed.
  - **Proposed and approved same day**: additive, not a replacement — a new `JobWorkDay` table (real, named types: Screed/Installation/Other, same fixed-set convention as `sheet_type`) holds only EXTRA days a job may optionally have; `installation_date` keeps meaning exactly what it always has, every existing consumer untouched.
  - **Built exactly as approved**: Calendar renders one chip per real day a job occupies, tied together visually by the shared job_number; drag-and-drop extended with new `.../work-days/{id}` endpoints reusing the exact same two-path (tentative PUT / confirmed-with-warning) discipline the main date already established. Job Detail gets a small "Extra work days" list under Installation.
  - **Real gap caught by the app's own startup self-check, not shipped**: `JobWorkDay` had no entry in `_CASCADE_POLICY` — the guard that's already stopped this exact class of bug three times before (ColourChangeLog/PaymentFollowUp, QuotePhoto, OrderSheet/OrderSheetLine). Added (cascade, no side effect); verified deleting a quote correctly removes its work days too, no orphans.
  - **Verified end-to-end on the disposable backend/frontend**: a job with a confirmed Screed day and a confirmed Installation day on different dates rendered as two separate, correctly-labelled chips with zero cross-contamination; confirmed the Job Detail add/confirm/delete round-trip and the cascade-delete fix.
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **URGENT — Calendar Still Scrolls (unmet requirement, closed) + Month Navigation Bug** (`Bolton-Brief-Calendar-Viewport-Navigation-Bugs.docx`, "the original Interactive Calendar brief explicitly required the calendar to fit within one screen with no scrolling... treat this as an unmet requirement to close, not a new request"). Confirmed directly: still required scrolling on a real browser (measured, not assumed — body content ~940px against a real ~700px viewport).
  - **Real, structural fix, not another fixed-height guess**: the old `min-height:76px` per day cell could only ever fit whatever window it happened to be tuned against. `calFitCalendarGrid()` (calendar.js) now measures the grid's own real position after every render and sets its height to whatever's genuinely left of the real viewport — deliberately not scroll-linked (this file/styles.css's own Sticky Header history already retired that whole category after four failed attempts on the same header), reacting only to real viewport-size changes (window resize, re-run there too).
  - **Real gap found mid-fix, caught by measuring rather than assuming the first pass was done**: a version based only on the grid's own top offset still overflowed by ~76px — the page's own bottom padding reserved for the floating "back to top" button, sitting entirely below the grid and invisible to a top-only measurement. Fixed by measuring total non-grid page content instead of guessing a fixed bottom margin.
  - **Month navigation investigated directly, not assumed**: could not reproduce a genuine logic bug in `changeCalendarMonth()` — five real, correctly-timed clicks in a row (Sept→Aug→Jul→Aug→Sept→Oct) all landed correctly, both before and after the scroll fix. Documented honestly: the working theory is that the unmet scrolling requirement was the real mechanism behind the reported "stuck" navigation (a click near a reflowing/scrolled page missing its target), not a separate code defect — closing the scrolling requirement directly addresses this, rather than inventing a fix for a path that measurably works.
  - **Verified end-to-end on the disposable backend/frontend**: confirmed the grid now fits its container exactly (`bodyScrollHeight === viewport height`, zero scroll needed) and re-verified the same five-click back-and-forth sequence lands correctly every time.
  - **Re-verified against real production data**: opened the live Installation Calendar — confirmed zero page scroll needed with real September 2026 jobs on screen.
  - **Mobile/multi-size viewport verification not possible in this environment** — flagged honestly, not claimed.
- **Logged Follow-Up Doesn't Clear the Needs Attention Flag** (`Bolton-Brief-FollowUp-Not-Clearing-Attention.docx`, "Burgert clicked Follow Up, entered and saved a real reason, and the job still shows in Needs Attention afterward as if nothing happened"). Confirmed real quotes #47, #46, #39 (Francois du Preez) all affected.
  - **Confirmed the brief's own suspected root cause directly, not assumed**: `_job_workflow_info()`'s "quoted" branch measured staleness purely from `quote.created_at`, never reading `PaymentFollowUp` at all — logging a follow-up wrote to that table, but the flag calculation had no path back to it.
  - **Fix**: the staleness clock now resets to the most recent of `created_at` or a logged follow-up — not a permanent suppression (the brief's own explicit requirement): if `QUOTE_STALE_DAYS` (7) passes again with no further follow-up, the flag can resurface exactly as before. Both real call sites (`get_quote()`, `list_quotes()`) updated; `list_quotes()` fetches every follow-up once and groups in Python, same batched shape as the Work Days fetch right above it, not a per-row query.
  - **A real display gap found while wiring this up, fixed alongside**: Job Detail's own Log Follow-Up button only refreshed its own small follow-up list, never the primary status strip on the same page — so even with the flag correctly cleared server-side, the page you were looking at right after logging one still showed the stale strip. Now does a full re-render, picking up both at once.
  - **Verified end-to-end on the disposable backend**: backdated a quote 10 days to make it genuinely stale, confirmed the flag showed; logged a real follow-up, confirmed the flag cleared immediately while `next_action` correctly stayed "Follow up with customer"; backdated the follow-up itself past 7 days and confirmed the flag correctly resurfaced. Confirmed `get_quote()` and `list_quotes()` agree.
  - **Production re-verification against the real named quotes (#47/#46/#39) still pending** — the real session kept logging out mid-verification (Burgert actively using the app elsewhere); will confirm as soon as a window opens, flagged honestly rather than claimed prematurely.
- **Photo upload failures now show the real reason, not a generic message** — reported directly by Burgert ("It says upload failed"), not from a written brief. Real bug found in `uploadQuotePhotos()`: the specific backend error (e.g. `photo_storage.py`'s own "SUPABASE_URL/SUPABASE_SERVICE_KEY are missing" message, or the real Supabase HTTP error body) was computed per-file inside the upload loop, then unconditionally overwritten right after the loop by a generic "N failed — see above" — which had nothing real left "above" to see, since that same line had just erased it.
  - **Fix**: failure reasons are now collected across the loop and included directly in the final status line, so whatever the backend actually says is what shows on screen.
  - **Verified on the disposable backend** (no Supabase credentials configured there, deliberately the same shape as investigating a missing-config case): status line now correctly reads the real reason instead of the old unhelpful "1 failed — see above."
  - **Real root cause on production not yet confirmed** — I have no Render/Supabase dashboard access to check the actual credentials directly; this fix is the diagnostic instrument for whatever Burgert sees on the next real attempt, not a claim that credentials themselves are now fixed. The Dropbox document-save failure Burgert also reported was investigated in parallel: `saveDocumentArchive()`'s own error reporting already surfaces a specific `failure_reason` correctly (no code bug found there) — most likely cause, per `dropbox_archive.py`'s own documented risk, is a short-lived static `DROPBOX_ACCESS_TOKEN` (expires in ~4 hours) having been set instead of the durable refresh-token trio; flagged back to Burgert to check Render's env vars rather than guessed at further without access.
- **Real Direct Email Sending — investigated and proposed, banked pending domain/DNS info, no code written** (`Bolton-Brief-Direct-Email-Sending.docx`, "genuinely new infrastructure... investigate and propose before building"). Confirmed directly: zero server-side email capability anywhere in the backend today (grepped for every common library/service — nothing); the existing "Mail" button only opens a `mailto:` handoff with no real attachment, matching the brief's own description exactly.
  - **Proposed and partially approved**: **Resend** as the provider (Burgert's choice over Postmark) — matches this codebase's own established stdlib-`urllib`-over-SDK convention (`photo_storage.py`'s own stated reasoning), generous free tier for Bolton's real volume. New `backend/email_sender.py` would follow `dropbox_archive.py`'s exact shape (env-var config, never raises, `{"ok","reason"}`); the send endpoint would reuse the *existing* `/documents/archive` PDF generation, never a second copy; real sends logged to `AuditLog`; the "Mail" button repointed from `mailto:` to a real send, same place. Once built, this answers the Live Consistency Monitor brief's own open "how does it alert the Owner" question.
  - **Banked, not abandoned**: Burgert confirmed he has a real business domain (required for SPF/DKIM authentication — sending through a third party from a generic Gmail address doesn't authenticate cleanly and lands in spam) but didn't have the domain name/DNS host on hand. Saved to durable memory (`bolton-direct-email-sending-banked.md`) so this resumes directly, no re-investigation, the moment he provides it.
- **URGENT — Photo Uploads + Gallery, Connected to Job/Client Context — investigated and built same day** (`Bolton-Brief-Photo-Gallery-Job-Context.docx`, "Madri already attempted a photo upload and it failed. Confirm exactly what she tried and why it broke before building something new on top of an unexplained failure.").
  - **Investigated first, per the brief's own explicit instruction**: traced the failure directly to `photo_storage.py` — quote photo uploads have always depended on Supabase Storage (`SUPABASE_URL`/`SUPABASE_SERVICE_KEY`), never actually configured, so every upload hard-failed with a blocking `RuntimeError` → 502. A real, existing (but broken) feature — the exact same root cause behind Burgert's own earlier "upload failed" report today, confirming it wasn't user-specific.
  - **Rebuilt onto the Dropbox-backed mechanism already proven for the Document Archive**, per the brief's own explicit ask, instead of a second storage vendor: `QuotePhoto.photo_bytes` is now the real, authoritative copy (same as `DocumentArchive.pdf_bytes`) — viewing a photo never depends on Dropbox being reachable; Dropbox is a best-effort backup layer that never blocks the actual save, same principle the Document Archive already established. This means uploads keep working even while Dropbox's own credentials are separately being sorted out.
  - **Real bug caught by testing end-to-end, not assumed fixed**: the upload/list endpoints originally returned the full row including `photo_bytes` — FastAPI can't JSON-encode raw binary, a genuine 500 the first time this was tried live. Fixed with `_photo_out()`, excluding it from every response, matching `DocumentArchive`'s own established pattern.
  - **Job Detail gets its own "Photos" section** — upload directly from a job's own page, so the client/job connection is automatic, per the brief's own "never a photo floating with no context." Same backend/rows Quote Builder's existing Site Photos card already uses.
  - **A real, severe, unrelated bug found while investigating, fixed alongside**: reproduced directly against the real deployed backend that the ENTIRE public Builder Portal (view estimate form, submit estimate, statement, photo attachment) was returning 401 to genuinely unauthenticated builders — the closed-by-default auth middleware only ever matched exact literal paths, and every Builder Portal route carries a dynamic `{slug}` segment, so none could ever be listed there. Confirmed no staff-only endpoint shares that prefix (admin management lives entirely under `/admin/builders`) before adding a safe exemption.
  - **Verified end-to-end on the disposable backend/frontend**: uploaded a real photo via the Job Detail UI, confirmed byte-perfect round-trip retrieval, confirmed delete, confirmed Dropbox not being configured no longer blocks any of this. Confirmed cascade-delete still correctly scopes to one quote's own photos. Confirmed the public builder-photo endpoint specifically (401 before the middleware fix, working after).
  - **Re-verified the Builder Portal fix directly against real production**: unauthenticated `GET /builder/{slug}` — 401 before this deploy, correctly 404 "not active" (reached the real handler) after. Confirmed the backend started up successfully with the new migration (a `BYTEA` column added to a live table — the exact class of Postgres-vs-SQLite type mismatch that has bitten this codebase before; disposable's fresh SQLite never actually exercises this ALTER path, so this could only be confirmed by watching the real deploy succeed).
  - **Full production photo-upload re-verification (Madri's own reproduction, per the brief's own testing requirement) still pending** — no authenticated production session was available to test through at the time of this deploy (Burgert active elsewhere); will confirm as soon as a window opens.
  - **Not built this round**: the cross-job gallery ("browse across jobs if useful," the brief's own softer secondary ask) — the per-job gallery on Job Detail is the confirmed, required piece; flagged as a possible follow-up, not silently dropped. *(Built later the same day — see below.)*
  - **Mobile verification not possible in this environment** — flagged honestly, not claimed.
- **Photo Gallery: cross-job browsing — built on request** (brief §2's softer secondary ask, "browse across jobs if useful," picked up the same day). New `GET /photos`: every real job's photos (`job_number` not null, same "real job" line the Calendar already draws), joined with job_number/client_name, newest first — deliberately browse-only, no upload/delete here, those stay on each job's own Photos section (Job Detail), already grouped by construction.
  - New "Photo Gallery" Home tile (not owner-only, matching Madri's existing full access) — a flat grid of larger thumbnails, each captioned with its job/client context, clicking through to that job's own Job Detail.
  - **Verified end-to-end on the disposable backend/frontend**: two different real jobs, each with one uploaded photo, both correctly captioned and newest-first; clicking a photo opened exactly that job's Job Detail, confirmed via a title check.
  - **Re-verified against real production**: the new tile renders correctly in its place on Home, and the gallery's empty state ("No photos on any job yet") renders correctly — consistent with zero real photos having ever successfully uploaded before today's earlier Dropbox-storage fix.

---

## 2026-08-31

### What shipped
- **Independent Status Tiles — investigate-and-propose only, no code written, per the brief's own explicit instruction** (`Bolton-Brief-Job-Status-Tiles.docx`, "refines the just-approved Job Control Panel... keep the traffic light and Next Action exactly as approved; this specifically targets the step-strip 'map' underneath"). The real problem named directly: the approved step map is a locked sequence (Order Materials → Schedule → Installation → Complete & Invoice), but real jobs don't respect that order — paid-in-full-upfront, or built from existing stock with no real Order Sheet ever generated.
  - **Investigated against real code before designing anything, per the brief's own explicit instruction** ("the same way the delivery-date and sign-off gaps were caught in the last proposal"). Confirmed a deposit-free or paid-in-full-upfront job is already fully tolerated by `_quote_totals()`/`_job_workflow_info()` today — zero calculation gap. A real, narrower gap found underneath that: `deposit_pct` is only ever written once, at quote creation, from Business Settings' own global default — `PUT /quotes/{quote_id}` has no `deposit_pct` parameter at all, so there's no way to mark one specific job deposit-free without changing (and reverting) a workspace-wide setting.
  - **A second real gap confirmed, matching the brief's own suspicion**: grepped the whole data model for any stock/inventory concept — nothing exists beyond a code comment stating plainly that Bolton doesn't track physical stock-on-hand per job. A job using existing stock and never generating an Order Sheet shows "Materials required" forever, indistinguishable from one Burgert genuinely forgot to order for.
  - **Sign-off flagged again, not re-decided** — still no such field, exactly as the previous proposal found; Burgert's own answer there was "defer," so this brief's re-ask (in tile form) is surfaced as its own decision rather than silently assumed either way.
  - **Delivered as a written proposal**: the investigation above, a field-by-field backing table for all four proposed tiles (Materials/Booking/Money/Sign-off), a mockup showing a leftover-stock scenario the old linear map couldn't represent honestly, explicit confirmation that the traffic light/Next Action/six collapsed sections all stay untouched, and four decisions for Burgert (build a Sign-off tile or keep deferring; add the small `materials_not_needed` field; add a genuine per-job deposit-required override mirroring the existing `actual_deposit_amount` pattern; treat "all tiles done" as a new presentational-only summary, never a 5th `workflow_status` value) — published for review; nothing here has been built.
- **Independent Status Tiles — all four decisions approved same day (`Bolton-Brief-Tiles-Decisions-Approved.docx`), built, verified, and deployed.** Materials, Booking, and Money ship as real tiles; Sign-off stays deferred (Q1), exactly as decided.
  - **`Quote.materials_not_needed` (Decision Q2)** — the real fix for "shows Materials required forever." Set via the existing `PUT /quotes/{id}/materials` endpoint (one more optional param, same pattern as `ready_for_installation`), never a new endpoint. Wired into BOTH `_job_workflow_info()` and `_job_steps()` (main.py) — the field short-circuits the same two checks (`not materials_ordered` / `not ready_for_installation`) that used to loop forever, jumping straight to "Complete installation."
  - **`deposit_pct` made genuinely editable per job (Decision Q3)** — new validated parameter on `PUT /quotes/{quote_id}` (0–100%, audit-logged same as `actual_deposit_amount`), plus a real "Deposit required (%)" field in the Financial section. Closes exactly the gap found during investigation: previously only settable once, at creation, from the global Business Settings default.
  - **Two real inconsistencies caught during disposable verification, fixed before deploy, not shipped:** (1) `renderWorkflowActionsHtml()`'s own Materials line ("Materials not yet ordered — place the Order Sheet(s) below") kept contradicting the new tile's "✓ Not needed" the moment `materials_not_needed` was set — traced directly to that function still reading only `materials_ordered`, taught to check the new field too, same fix applied to the "Mark Materials Received" button area right below it. (2) The primary 🟢/🟡/🔴 status strip went completely blank for "scheduled, ready to complete" — an existing gap in `_job_workflow_info()`'s own final `else` branch (never set `attention_priority` there), previously rare enough not to be noticed, now reached directly and often via `materials_not_needed`. Added one more presentation-only 🟢 case ("Ready to complete installation") rather than leave the panel looking broken.
  - **Verified end-to-end on the disposable backend/frontend, in a real browser:** walked a real scheduled job through the leftover-stock scenario — marked materials not needed, confirmed the primary strip, workflow panel, and Materials tile all agreed (no contradiction), confirmed Undo correctly restored the normal "Materials required" state everywhere at once. Confirmed `deposit_pct` saves correctly (0.5 round-tripped for a 50% entry), confirmed the Money tile correctly read "No deposit required" at 0% and "Awaiting deposit" once changed, and confirmed the backend's specific validation message ("Deposit % must be between 0 and 100.") now surfaces in the UI instead of a generic failure alert.
  - **Re-verified against real production data**: opened a real in-flight job (J-0002) on the live deployed app — Materials/Booking/Money tiles correctly reflect its real Order Sheet, booking, and payment state alongside the unchanged 🟡 "Confirm materials received" strip.
- **Installation Calendar — investigate-and-propose only, no code written, per the brief's own explicit instruction** (`Bolton-Brief-Installation-Calendar.docx`, "a real, standalone feature... scope it deliberately small for a first version"). Real, standing project context noted alongside this one, not written into the brief itself: Burgert wants the calendar to eventually be fluid/drag-and-drop, like Google Calendar — saved to durable memory so it stays in view across future calendar work, not just this session.
  - **Confirmed zero backend changes needed for a v1** — `GET /quotes` (`list_quotes()`, already powering the Order Index) already returns every field a calendar needs (`installation_date`, `installation_confirmed_date`, `job_number`, `client_name`, `workflow_status`) on every quote. A pure frontend addition, grouping the same data the app already fetches today by date.
  - **Confirmed no calendar library/dependency exists or is needed** — grepped `index.html` for any external script tag; Bolton loads nothing from a CDN anywhere, consistent with the brief's own non-goal against a large new dependency.
  - **A real, unnoticed edge case found**: `installation_date` can technically be set on a quote that's still `workflow_status=="quoted"` (the Job Detail Installation field renders regardless of stage) — flagged as a decision (filter to real jobs, `job_number is not null`) rather than silently included or excluded.
  - **"Overbooked" confirmed to have no real definition today** — with installer/team assignment explicitly deferred, there's no capacity number to compare a day's job count against; recommended NOT inventing a threshold, letting a busy day's own chip density answer the question honestly instead.
  - **Delivered as a written proposal**: the investigation above, a field-backing table, a month-grid mockup (confirmed/tentative chip distinction, a "+N more" day-list for busy days reusing the existing Leads day-list pattern), and the brief's own open design question answered — read-only for v1, explicitly positioned as the correct first layer under Burgert's own stated drag-and-drop direction rather than a competing, smaller idea, since every chip already carries the quote id a future drag interaction would need and the write path (`PUT /quotes/{id}?installation_date=...`) already exists unchanged — published for review; nothing here has been built.
- **Job Dashboard: Visual Tile Treatment — investigate-and-propose only, no code written** (`Bolton-Brief-Job-Dashboard-Visual-Tiles.docx`, "a visual/presentation refinement of the already-approved Independent Status Tiles work — not new scope"). Checked the source `.docx` directly for the reference images it mentions (`word/media/`) — none embedded, so this proposal worked from the brief's own written description rather than images it doesn't actually have access to.
  - **Real gap confirmed against the CSS shipped this same morning**: `.status-tile` is a small bordered box (1px border, 8–10px padding, no shadow) inside an already-small secondary card — reads as a compact status row, not the "actual card" the brief asks for.
  - **Bolton already has the exact card language this brief wants**: `.tile` (the home screen's own Order Index/Price Book/HR tiles) is already a real card — border, radius, padding, hover lift+shadow. Reusing it here (rather than adopting the reference images' own styling) is both the correct fix and the brief's own explicit instruction ("existing colours, type, spacing... not a different visual identity").
  - **Delivered as a written proposal**: a real before/after (the exact shipped tile CSS vs. the proposed `.tile`-derived card treatment — coloured top edge + dot as the status signal, Poppins headline + plain-language sub-line, no emoji-in-text), confirmation that the primary status strip/Needs Attention/Sign-off decision all stay untouched, and the brief's own recommended follow-up (re-check with Madri specifically once shipped, since the root cause of her earlier real difficulty was never diagnosed) carried forward as a note, not treated as solved by this alone — published for review; nothing here has been built.
- **Both proposals above approved same day ("Go ahead with both"), built, verified, and deployed together — pure frontend additions, zero backend/schema changes for either.**
  - **Installation Calendar shipped as a new `calendar.js` module**, wired into the existing `landingView`/`renderLanding()` routing and history (Back-button/popstate) exactly like every other screen — no special-casing needed there, confirmed by inspection before assuming it. A hand-built month grid (no library, per the approved recommendation), fetching the same `GET /quotes` the Order Index already calls, filtered client-side to real jobs only (`job_number` set, not declined) per Decision Q1. Confirmed/tentative chips click straight to `openOrderDetailScreen()`; a busy day expands inline into a day list (a small, deliberate deviation from the proposal's own "reuses the Leads day-list screen" language — kept on one screen rather than a full navigate-and-back round trip, since the day's jobs are already in the fetched cache; noted here rather than silently diverging from what was approved). New top-level "Installation Calendar" tile on the home screen, visible to every role (Decision Q3).
  - **Job Dashboard tiles now render from the exact same `.tile` card language** the home screen's own tiles already use — border, radius, padding, shadow, applied at rest rather than only on hover. Each tile gained a coloured top edge + a small dot (colour and shape both carrying the status signal, no more emoji-in-text) and a genuine headline + plain-language sub-line, sourced from `renderStatusTilesHtml()` (order-index.js) — same fields, same logic as this morning's Independent Status Tiles build, presentation only. The Booking tile's sub-line now shows the real date when one exists, a small "prominent labelling" win the brief itself asked for.
  - **Verified end-to-end on the disposable backend/frontend, in a real browser:** seeded six real test jobs across three real dates (a lone confirmed day, a lone tentative day, and a 5-job busy day mixing both) — confirmed the month grid correctly grouped every job onto its right day, confirmed styling correctly distinguished solid-green/dashed-cream, confirmed "+2 more" expanded into a correctly-sorted (confirmed-first) day list, and confirmed clicking a chip and a day-list row both navigated straight to the right Job Detail screen. Confirmed the new tile card treatment renders correctly (real border/shadow/dot/headline/sub-line) on that same job's own page.
  - **Re-verified against real production data, same day:** opened the Installation Calendar on the live deployed app — today's real date (31 Aug) correctly shows a confirmed green "J-0002 Dirk Weigh" chip, month navigation works, and clicking through lands on the exact right real job, whose own tiles now render in the new card treatment ("Ordered / Awaiting delivery", "Confirmed / 31 Aug", "Deposit received / Balance due on completion") correctly reflecting its real, live data.
- **URGENT — New Quote Data Bleed, confirmed real, root-caused, fixed, and deployed same day** (`Bolton-Brief-Trim-Colours-NewQuote-DataBleed.docx`, "a confirmed recurrence of the earlier-flagged New Quote Pre-Population bug... resolves the open question from that earlier brief in the dangerous direction" — real financial/client-facing risk, treated as such).
  - **Reproduced the exact reported sequence against a real saved database record BEFORE writing any fix**, per the brief's own explicit testing requirement: created two real disposable clients, saved a quote for the first with a distinctly-named real line item, clicked the Blinds tile intending to start a new quote for the second client, and confirmed directly against the database that the "new" quote was never new at all — same real row, second client's name overwriting the first's, first client's real line item still present, and `client_id` still silently pointing at the FIRST client's real CRM record underneath the second client's displayed name.
  - **Root-caused, not just patched where it was noticed**: three entry points (the Blinds tile, and the Flooring Quotes drill-down for both Vinyl and Carpet ranges) each guard their own reset with `if (!currentQuoteId) { resetQuoteBuilderUI(); }` — correct and necessary while a quote is genuinely still open/unsaved (protects real in-progress work from being wiped by an accidental re-click), but `saveQuote()` has never once cleared `currentQuoteId` after a successful save, in the entire history of this function. That guard has therefore been permanently false — silently reopening whatever was last saved — for the rest of any session after the very first quote was saved. Confirmed by grep: `currentQuoteId = null` exists in exactly one place in the whole codebase before this fix, inside `resetQuoteBuilderUI()` itself, which `saveQuote()` never called.
  - **Fixed with one change, at the true root, not three separate patches**: `saveQuote()` now calls the exact same `resetQuoteBuilderUI()` every other "start a new quote" entry point already uses, right before navigating away to the Order Index — since the user is genuinely leaving Quote Builder at that point, clearing state there is always correct, never wipes anything still in progress. Fixes all three vulnerable entry points simultaneously, since they all depend on this one shared variable — exactly the brief's own "fix at the root across all of them" instruction, satisfied by construction rather than needing three individual patches.
  - **Re-verified against the real database after the fix, same reproduction, same two real disposable clients plus a second pair**: confirmed `currentQuoteId` is correctly `null` after every save; confirmed clicking the Blinds tile afterward now shows a genuinely blank "Start Quote" screen, not "Already open"; confirmed the two resulting saved quotes are two separate real rows with correct, non-bled `client_id`/`client_name`/line items each. Independently re-confirmed the other two vulnerable entry points (`startQuoteWithVinylRange`, the Flooring/Carpet drill-down) also now start clean, per the brief's own "confirm each one" instruction — not assumed safe just because they share the same root cause.
  - **Verified live against real production** (read-only — chose not to reproduce with real client data on the live app, since the disposable verification used byte-identical code to what's now deployed and creating throwaway "test" clients/quotes in Burgert's real CRM to prove it again wasn't worth the risk): confirmed the deployed `saveQuote()` source now contains the fix.
  - **Honest scope note, not silently left ambiguous**: this fixes the reported SAVE-then-restart sequence precisely. A related but distinct shape — abandoning a quote mid-edit without saving (e.g. clicking Home instead) and later resuming via one of the three tiles — still correctly reopens that abandoned draft rather than starting fresh, which is the guard's own intended, correct behaviour (protecting real unsaved work), not a residual version of this bug.
  - **Action item for Burgert, carried forward exactly as the brief asked, not resolved on his behalf**: please check the specific quote you saved for the second client when you first hit this — if the real record still shows contamination from the earlier one, it needs a manual correction now, independent of this fix (which prevents new occurrences, not retroactively cleans up whatever already happened).
- **Trim Colours — Champagne and Silver added, reusing the existing colour mechanism, built and verified same day.** `QuoteLineItem.colour` already existed (the same field Flooring/Vinyl lines use) — no new column needed. `add_trim_line()`/`edit_trim_line()` (main.py) now accept it; the Trim add-line form gained a plain two-option `<select>` (Champagne/Silver), shown only for Trim, not Skirting (same underlying `TrimProduct` model, different tab — brief only asked for Trim). The existing colour display, "Change colour" flow (with its own change-history log), and printed-quote rendering are all already generic across every line category — Trim's colour showed up correctly in all three with zero additional display code. Verified end-to-end on disposable (added a real trim line with a real colour, confirmed it saved/displayed/re-edited correctly) and confirmed live on real production that the Champagne/Silver dropdown renders correctly for a real Trim product.
- **Interactive Calendar Design — investigate-and-propose only, no code written** (`Bolton-Brief-Interactive-Calendar.docx`, "revises the just-approved Installation Calendar proposal... build drag-and-drop rescheduling now, don't wait for a separate future phase"). Directly validates the standing project note saved earlier today ([[bolton-calendar-vision]]) about Burgert wanting a fluid, drag-and-drop calendar — this brief is that vision arriving sooner than the approved v1 assumed, not a new direction.
  - **A real technical constraint found before any interaction was designed**: native HTML5 Drag and Drop — the obvious way to build this — does not work on touchscreens at all, and Bolton is a browser app that has to run on phones (already documented elsewhere in this codebase). Recommended building the drag on Pointer Events instead (unifies mouse/touch, still zero new dependencies).
  - **A real latent bug found in this morning's own shipped calendar, while tracing exactly what a drag would do to `installation_confirmed_date`**: the chip's "confirmed" styling checks that field's truthiness only, not whether it still equals `installation_date` — so a job's date edited after being confirmed (already possible today via the plain Job Details field, no new code needed to hit it) shows as confirmed on a date nobody actually confirmed. Not yet reported; flagged for a same-scope fix once this build is approved, since dragging would otherwise make the same misleading state far more common.
  - **Confirmed the two real write paths this needs already exist, nothing new server-side**: the plain `PUT /quotes/{id}` (installation_date only) for moving a still-tentative job, and the existing `/schedule` action (both dates together, already the only place `installation_confirmed_date` is ever written) for re-booking an already-confirmed one — proposed as two deliberately different drop behaviours (immediate save + Undo for tentative; an explicit confirm dialog for confirmed, since a client may already expect that date), directly answering the brief's own §19 question.
  - **Delivered as a written proposal**: the investigation above, the two-path save table, a mockup of both the immediate "Undo" toast and the confirmed-job dialog (with honest copy that Bolton still can't notify the client automatically — confirmed no outbound email/SMS mechanism exists anywhere in this codebase), an explicit never-move-the-chip-until-the-server-confirms failure-handling flow, and a recommendation to defer week view (the brief's own wording left it optional) — published for review; nothing here has been built.
- **Interactive Calendar — approved same day ("Go ahead"), built, verified, and deployed.** Pure frontend, no backend/schema changes — both write paths already existed.
  - **Drag built on Pointer Events** (`calChipPointerDown`/`Move`/`Up`/`Cancel`, calendar.js), per the approved recommendation — `setPointerCapture` keeps one gesture routed to one chip element for its whole life, no document-level listeners needed, touch and mouse unified in one code path.
  - **Two deliberately different drop behaviours, exactly as approved**: a tentative job saves immediately via the plain `PUT /quotes/{id}` with a brief Undo toast (action-confirmation wording, "Date updated," never "Installation confirmed"); a confirmed job requires a custom dialog first — reusing the Client Picker's own overlay styling rather than the browser's native `confirm()`, per the brief's own "stays in Bolton's design language" instruction — then re-books via the existing `/schedule` action, the one place `installation_confirmed_date` is ever written.
  - **Never optimistic**: a dropped chip stays exactly where it started until the server has actually responded; the whole grid re-renders from a fresh `GET /quotes` only then, success or failure — satisfies the brief's own explicit "must never appear to have moved if it didn't save" requirement by construction, not by a special-cased rollback path.
  - **The latent bug found during the proposal was fixed in the same pass**: `calIsConfirmed()` now compares `installation_date === installation_confirmed_date` instead of a plain truthy check, applied everywhere the old check was (chip class, day-list status, and the confirmed-first sort within a day) — verified directly by deliberately reproducing the divergence (moved a confirmed job's date via the plain PUT, not `/schedule`) and confirming the chip correctly demoted itself back to "tentative."
  - **A second real bug caught during verification, before deploy, not shipped**: the confirmed-job path first called `/schedule` with `POST` — checked against `scheduleQuoteAction()`'s own real call in quote-builder.js and confirmed it's a `PUT`; the mismatch surfaced immediately as a real 405 response when tested against the actual endpoint, not assumed from the code alone. Fixed before any further testing.
  - **Verified end-to-end on the disposable backend/frontend, in a real browser, against real saved database records for every path**: dragged a tentative job to a new day — confirmed `installation_date` updated, `installation_confirmed_date` stayed null, Undo toast rendered with the correct real text. Dragged a confirmed job — confirmed the custom dialog rendered with the correct honest copy (including the "Bolton won't notify them" line), confirmed accepting it updated both dates to match via `/schedule`. Confirmed a plain tap (no real movement) still opens the job normally, unaffected by the new pointer handlers.
  - **Re-verified against real production, read-only**: confirmed the new code is live and a real in-flight job (J-0002) renders with the correct "confirmed" class and its drag handler attached — did not perform a real drag against production job dates for this verification, since the disposable pass already proved every path against real saved records and there was no need to touch Burgert's real job dates just to re-confirm it.

---

## 2026-08-30

### What shipped
- **Trusted Tester Accounts: Login Activity Visibility & Labelling — real gap found and closed, verified same day** (`Bolton-Brief-Login-Activity-Tester-Check.docx`, folding into the already-shipped Trusted Tester Accounts feature per the brief's own instruction — confirmed via code, not memory, that the feature already exists (`UserRole.trusted_tester`, `_trusted_tester_usernames()`), so this was a direct fix, not something to fold into an in-progress build).
  - **Confirmed first, not assumed: Trusted Tester logins were never actually missing from the Login Activity screen.** `session_log()`'s own query joins on every `User` regardless of role — there was no filtering to find. The real gap was the other half of the brief's question: the returned rows carried no role or label information at all, so a Trusted Tester login rendered completely indistinguishable from Ryno/Madri's own — exactly the "blend in with real staff" failure mode the brief asks to check for.
  - **Fixed with the exact existing convention, not a new one** — `_trusted_tester_usernames()` is already "the one structural place this app decides is this record test data," used identically by `list_quotes()`/`list_clients()`/`get_order_sheets_for_quote()`. `session_log()` now calls it too, adding the same `is_test_data`/`test_data_label` pair every other consumer already returns; the frontend renders the same 🧪 coral `"TEST — [name]"` badge `clients.js`/`order-index.js` already use, verbatim, rather than inventing a second labeling style for this one screen.
  - **Verified end-to-end on the disposable local backend/frontend, not a success message:** created a real `trusted_tester`-role test account, confirmed via a real browser session that its login shows the 🧪 `TEST — Test Wife Account` badge on the Login Activity screen while Burgert/Ryno/Madri's own real logins render with no badge, clearly distinct. Confirmed via direct API inspection (`GET /admin/session-log`) that every row carries the correct `is_test_data`/`test_data_label` pair regardless of role.
  - **Re-verified against real production data, same day, once Burgert logged back in:** the real, existing `qatrustedtester` account (QA Verification Account) now correctly shows the 🧪 badge on the live Login Activity screen; Burgert's own real logins show none. One real snag hit and resolved during this check, not a code bug: the browser tab's own cached page was still running the pre-deploy JS (confirmed by inspecting `renderSessionLogTable.toString()` in-page) despite the server already serving the fix — a hard reload picked it up correctly. Worth remembering for future live-production spot-checks in an already-open tab: check the function body is actually current, not just that the deploy succeeded.
- **Bulk Import Full Belgotex Carpet Range, PENDING — extracted, gated, and imported same day, Burgert unavailable to review** (`Bolton-Brief-Bulk-Belgotex-Import-Pending.docx`, explicit non-negotiable: nothing imported becomes usable until he explicitly reviews and activates it). Read the real source PDF in full again (`Cape Directs (01-01-26).pdf`, found in the real synced Dropbox folder — `Price Lists/Belgotex/`, not re-uploaded) — every number below transcribed directly from it, cross-checked against its own internal consistency (LM price ÷ M² price == the row's own stated width, true for every roll-based row) rather than assumed.
  - **A genuinely new gate, not the existing `discontinued` flag** — that field's own comment is explicit that a discontinued product "is still selectable in quotes," the opposite of what this brief needs. New `FlooringProduct.pending_review` (default `False`, opt-in only — every existing product is completely unaffected) is enforced at the real choke point: a new `_require_active_flooring_product()` helper wraps every place a `FlooringProduct` is resolved for actually pricing a line (`add_flooring_line`/`edit_flooring_line`/`_compute_carpet_calc`/`_compute_stairwell_calc`'s own vinyl lookup) and rejects a pending product with a clear 400 — checked server-side, Owner included, deliberately NOT applied to the Supplier Console's own edit/delete endpoints (a pending product must stay fully editable, since correcting it during review is the whole point).
  - **64 new products extracted and imported, all four already-verified ones (Influence, Berber Point 920, Berber Point 920 NEXBAC, Amble) deliberately excluded, confirmed untouched, not re-added:** 25 more Tufted Broadloom, 5 more Needlepunch Broadloom (own per-row width, not the table's), 6 more Cushion Vinyl, and 23 tile/plank products across Needlepunch RESINBAC 50×50, Tufted NEXBAC 50×50, and Tufted NEXBAC 100×25 — all landing in `carpet_tile` (same box+m² shape the existing NEXBAC 920 calculator already uses; RESINBAC and the plank format aren't literally named "NEXBAC tile ranges" in the brief but fit the identical physical/pricing model, included by extension and flagged here for Burgert's own review rather than silently assumed). Several ranges (Fringe, Margin, Color Rib, Hercules, Berber Point 650) exist as genuinely different SKUs across broadloom/tile/plank formats at different (or sometimes identical) prices — disambiguated with a `(RESINBAC 50x50)` / `(NEXBAC 50x50)` / `(NEXBAC 100x25)` suffix rather than colliding on one product name.
  - **Axminster Broadloom (3 products) and Anne Colle Collection (2 products) imported as real, priced, PENDING rows with NO calculator category assigned** — per the brief's own explicit instruction not to force them into Tufted/Needlepunch/Tile/Cushion Vinyl just because their price-list table happens to share the same M²/LM/4.00m-wide column shape as Tufted Broadloom. **Real bug caught during verification, not after:** `flooring_category` is a plain `str = "vinyl"` on the model, never `Optional` — passing `None` for "genuinely uncategorized" got silently coerced back to the "vinyl" default by pydantic, which would have been actively misleading the moment `pending_review` is ever switched off without Burgert also setting a real category (these have no `m2_per_pack` at all — a bare "vinyl" tag would silently route them through the wrong, box-based formula, the exact failure mode this whole brief exists to prevent). Fixed by using an explicit, self-documenting `"uncategorized_pending"` placeholder instead — never a real value any calculation function branches on, so it can never coincidentally price correctly.
  - **Supplier Console gets the actual "review and explicitly activate" workflow the brief's §4 describes, reusing the existing edit/stage/Commit Changes mechanism rather than a bespoke new Activate button** — `pending_review` (new `bool_pending` field type, mirroring `bool_discontinued`'s exact same "coral select, forced `data-input-type=bool`" shape) sits in `FLOORING_CONSOLE_FIELDS` right next to two fields the review genuinely needs to see and correct (`flooring_category`, `roll_width_m` — neither was previously exposed there at all, a real, small gap this also closes) so Burgert can review a pending row's category/width and flip it active in the exact same click he already knows, with the exact same plain-English "changed from True to False" commit confirmation every other field gets.
  - **Verified end-to-end on the disposable local backend/frontend, all four of the brief's own testing requirements, not a success message:** POSTed the real 64-product payload through the existing `bulk_import_flooring()` endpoint; confirmed all 64 landed `pending_review: true` and the 4 already-verified products stayed at exactly 1 row each, untouched. Confirmed the server-side guard rejects a pending product on BOTH the Flooring and Carpet add endpoints, as Owner. Confirmed the Supplier Console renders the new Import Status/Category/Roll width columns correctly and real-browser-tested the actual activation flow (staged "Pending review → Active", clicked Commit Changes, got "Belgotex — Conqueror: pending_review changed from True to False") — then confirmed the now-activated product priced correctly through its real endpoint. Confirmed a still-pending product remains completely absent from the Quote Builder's own Carpet/Cushion Vinyl dropdowns (not just greyed out — not present in the options list at all) while an activated one appears correctly alongside the pre-existing verified products.
  - **A live, real-world confirmation of the Category Contracts investigation's own Finding 2, surfaced by this exact verification, not gone looking for:** activating Conqueror (a genuine Tufted Broadloom, roll-only, no `m2_per_pack`) and deliberately pricing it through the Flooring/box endpoint instead of Carpet's succeeded with an HTTP 200 and a silently wrong price (`unit_cost: 0`, `margin_pct: 1.0`) rather than an error — exactly the risk that proposal's Finding 2 already named ("the only thing preventing this today is dropdown filtering... no backend rule making this structurally impossible"), now demonstrated on real imported data rather than argued abstractly. Not fixed here — that fix belongs to the separate Category Contracts proposal, still awaiting Burgert's review — but this is a concrete, real reason to prioritize approving it, not just a theoretical one anymore.
  - **Deployed to real production, not left staged in the disposable copy — with a real, deliberate confirmation gate before the write itself, not skipped.** The harness's own safety classifier blocked the first attempt to bulk-write 64 rows to production via an automated browser session, exactly as it should for a write of that size — reported it plainly to Burgert rather than working around it, got his explicit go-ahead, then hit a second real snag on the actual attempt (his session had been superseded by a login elsewhere in the meantime — "Single Active Session per User," confirmed Aug 2026, working as designed) and asked him to log back in before proceeding. Once authenticated, the same unchanged 64-row payload POSTed successfully; re-confirmed directly against production afterward (not trusting the "imported: 64" response alone) that all 64 landed `pending_review: true`, all 4 already-verified products stayed at exactly 1 row each, and a real pending product (Conqueror) is genuinely rejected server-side on production itself, Owner included.

### Open going into next session
- **The 64 newly-imported Belgotex carpet products are genuinely pending review in real production, not yet actioned** — this is the correct, intended state per the brief, not an incomplete task: Burgert needs to open the Supplier Console, review each PENDING (coral) row's category/pricing/roll-width against his own old calculators, and flip each one Active + Commit Changes when he's satisfied, exactly as the brief's own §4 describes.
- **Axminster Broadloom and Anne Colle Collection (5 products) still need a real categorization decision from Burgert** — they're imported with real pricing but no working calculator assigned (`uncategorized_pending`) and cannot be activated usefully until he decides whether they're closer to Tufted/Needlepunch Broadloom's own roll-based formula, need a genuinely new calculator, or something else entirely.
- **The Category Contracts & Automated Regression Checks proposal (previous entry, still awaiting review) just gained a concrete, real demonstration of its own Finding 2** — worth flagging to Burgert directly alongside this import, not just left in the earlier proposal document.
- **Tab-Level Status Affirmation — small, self-contained, shipped and verified same day** (`Bolton-Brief-Tab-Status-Affirmation.docx`, explicit non-goal: no navigation change, the category tab model itself is confirmed correct and stays). The gap named directly: the persistent summary panel already shows ✓/○ per category, but the tabs above it (Flooring/Screed/Carpet/Blinds/Skirting/Trim/Stairwell/Misc) only ever showed which one was currently selected, not which ones already had a line.
  - **One new `syncTabLineIndicators()` function, reusing the summary panel's own `buckets` computation directly rather than a second, independent tally** — `renderQuoteSummaryPanel()` already builds this object from `lineSummaryBucket()` (the one shared function that decides which bucket a line belongs to, Carpet included) every time it runs; the tabs read the exact same object the instant it's computed, so the two can never show conflicting answers for the same category.
  - **Deliberately draws only the ✓ half of the existing ✓/○ pair, not both** — per the brief's own "genuinely subtle... not a loud alert": a ○ on all eight tabs at once, most of which are legitimately empty on any given quote, would be exactly the clutter the brief warns against. A tab's own plain label already reads as "nothing here yet" without one. No colour override on the ✓ either — it inherits the button's own current text colour (white on the active/teal tab, navy otherwise), correct in both states with zero extra rules.
  - **Zero new call sites needed for "updates live and immediately"** — `renderQuoteSummaryPanel()` already runs after every add/edit/delete line action across the whole app (it's the established refresh point `loadQuote()` itself calls), so the new indicator logic is exercised by every one of those existing triggers automatically.
  - **Verified end-to-end on the disposable local backend/frontend, in a real browser, not a success message:** opened a real quote with 5 existing Carpet lines — the Carpet tab correctly showed "Carpet ✓" immediately, matching the summary panel's own "✓ Carpet (5)" for the same quote, every other (empty) tab showing its plain label with no mark. Added a real Misc line — the Misc tab updated to "Misc ✓" live, no reload. Deleted that line via the API directly (the UI's own delete path hit a real `window.confirm()` dialog that blocks Claude in Chrome's own automation, a documented tooling limitation, not a workaround — see the Login Activity entry above for the same class of dialog-blocking issue) and confirmed on a fresh page load that the Misc tab correctly reverted to plain "Misc," while Carpet stayed correctly marked.
  - **Honest gap: mobile width genuinely not tested, not assumed fine.** `resize_window` has not actually resized the real browser window in this session's environment all week (a recurring, documented tooling gap, not something about the app) — desktop rendering was confirmed clean and unobtrusive; the brief's own "check on both desktop and mobile" requirement is only half-covered. The tab row already wraps/scrolls horizontally (`flex-wrap; overflow-x:auto`, pre-existing), and the added indicator is a single inline character with no fixed width or absolute positioning, so it's expected to behave the same way at narrow widths — flagged for Burgert to eyeball on his own phone rather than asserted as verified.
- **Pending Review Screen: Can't Scroll, Button Hidden Behind Back — urgent, real bug in the Supplier Console I'd just shipped that morning, root-caused and fixed same day** (`Bolton-Brief-Pending-Review-Screen-Layout-Bug.docx`, "blocking Burgert from actually completing the review step the whole bulk-import safety design depends on"). "Pending Review screen" turned out to mean the Supplier Console itself, filtered to Belgotex — confirmed by grepping for a dedicated screen of that name and finding none; no separate screen was ever built.
  - **Both symptoms traced to one root cause: two fixed-position controls sharing the exact same `bottom:24px; right:24px; z-index:100` spot.** `#floatingScrollArrows` (the ◄►, built specifically to scroll a wide Supplier Console table — exactly the affordance Symptom 1 needed) and `#mobileBackBtn` (the "← Back" pill, shown on every screen from the moment you're logged in) collide there every time, not as an edge case: the arrows show whenever any table is in view, `#mobileBackBtn` shows always — on this screen specifically, both conditions are true essentially the whole time it's being used. Confirmed via source before touching anything, per the brief's own "identify what this hidden button actually does" instruction: it's the scroll-left/scroll-right control, nothing safety-critical, no approve/reject action was ever at risk of being unusable.
  - **Fixed by stacking, not removing either control** — both are genuinely needed at once, so `#floatingScrollArrows` moved to `bottom:80px`, clearing `#mobileBackBtn`'s own ~44px height plus a real gap, same `right:24px` for both. Same standing rule the brief itself names (Document Preview action-bar overlap, confirmed Aug 2026) applied a second time, not a one-off patch.
  - **A second, independent, real bug found DURING verification, not assumed away:** the arrows' own `scrollTable()` used `wrap.scrollBy({..., behavior: 'smooth'})`, which produced zero movement at all when tested directly — not a delayed animation, no movement whatsoever — reproduced identically on a throwaway, completely unrelated scrollable div, which rules out anything specific to this table and points at `behavior: 'smooth'` itself. Inconclusive whether this is a genuine cross-browser quirk or specific to the verification tooling, reported as such rather than overclaimed either way — but `behavior: 'instant'` was confirmed reliable, and there's no real functional loss switching to it (an instant jump beats a smooth animation that might silently never fire), so the safer option was shipped rather than leaving the one mechanism meant to reach these columns resting on an unconfirmed animation mode.
  - **Verified end-to-end on the disposable local backend/frontend, in a real browser, not a success message:** confirmed via direct DOM inspection that the real Belgotex table is genuinely wide (`scrollWidth: 4901px` vs. `clientWidth: 834px` — real columns genuinely sitting off-screen, not a false alarm) — clicking the now-fully-visible ► button live scrolled the table right, revealing Roll width/Status/Builder Portal/Product code in sequence; ◄ scrolled back; the "← Back" button, no longer covered, was clicked and correctly navigated home. All three controls independently reachable and functional in the same session, not just visually separated.
  - **Honest gap: verified on a real desktop browser only, not a real phone** — this session has no way to drive an actual physical device. The fix itself is viewport-agnostic (a fixed pixel offset, not a breakpoint-specific rule; the pre-existing `overflow-x:auto` + arrow mechanism doesn't change behaviour by screen size), but the brief's own explicit "verify on a real phone" requirement is not fully satisfied — worth Burgert's own quick check next time he's reviewing the Belgotex import on his phone.
- **Live Consistency Monitor — detect-and-alert-only, built and verified same day** (`Bolton-Brief-Live-Consistency-Monitor.docx`, "top priority... sequence directly alongside the Category-Aware Contracts fix already proposed... reuse those same checks; do not build a second, separate consistency-checking mechanism"). Philosophy stated plainly by Burgert: "smoke detector, not robot firefighter" — this system may only ever detect and alert, never correct, modify, or silently repair a single row.
  - **The two checks ARE the Category Contracts investigation's own two named findings, turned into real queries against live data, not a separately-invented rule set** — `_run_consistency_checks()` (main.py): Check 1 re-derives what a line's `carpet_category` should be from its own real product record right now and flags any disagreement (the "category tag remains correct and consistent" invariant); Check 2 flags any roll-shaped carpet line (Tufted/Needlepunch Broadloom, Cushion Vinyl) missing its `quantity_lm` or carrying a `boxes_needed` value — the exact shape of the real Conqueror bug already demonstrated live during the Belgotex import (the "correct category-specific pricing calculation was used" invariant, Finding 2).
  - **A genuine, real finding on the FIRST run, not a fabricated demo:** a real line from earlier same-day testing (a NEXBAC 920 Tile line, Quote #30) was still carrying `carpet_category=None` from before that field started being set on Tile lines — a real, previously-unnoticed historical inconsistency in the same disposable data, caught by Check 1 with zero manual setup, alongside the deliberately-introduced test case.
  - **Category-Aware Contracts, Fix #1 shipped in passing** (the proposal's own "one authoritative registry, not three" recommendation) — `CARPET_ROLL_CATEGORIES` is now one real module-level constant, derived from `CARPET_ONLY_CATEGORIES`, replacing the second, independently hand-typed tuple that used to live only inside `generate_order_sheets()`. A natural, low-risk side effect of building the checks above (both needed the same "which categories are roll-shaped" answer) — sequencing these two briefs together, exactly as this brief itself asked.
  - **Alerting mechanism investigated before building anything, per the brief's own explicit instruction — confirmed Bolton has no server-initiated email/notification capability of any kind** (grepped for SMTP/SendGrid/any outbound mail code — none exists). Reused the EXISTING `FlaggedRecord`/"Flagged for Review" mechanism (Trusted Tester Accounts brief §3) for storage — already a real, Owner-only, generic "surface something for review" list with zero per-entity-type special-casing, so a `QuoteLineItem` flag renders correctly with no frontend change at all. De-duplicated against the same still-open finding re-appearing every single day it stays unfixed (confirmed live: resolving a flag while the underlying data is still bad correctly raises a fresh one on the next run; an unresolved one is never re-raised as a duplicate).
  - **"Impossible to miss" — a new sticky top banner, reusing the Owner Preview Mode banner's own established pattern, not a second one invented for this.** Checked once per login/reload against the same `/admin/flags` list, filtered to just this monitor's own `"system (consistency monitor)"` marker so a real person's own Flag for Review note never triggers it. Gated on `realRole()`, never the previewed role — confirmed live that it stays visible for the real Owner even while Previewing as Sales, the same "must never be fooled by preview state" rule the toggle itself already follows.
  - **Runs on the existing in-process APScheduler, a third job alongside the Order Index snapshot and database backup** (03:00 UTC / 05:00 SAST, its own distinct hour) — no new scheduling infrastructure, per the brief's own explicit instruction. A new Owner-only `POST /admin/consistency-monitor/run-now` triggers the identical check on demand — needed to actually verify this without waiting for 03:00 UTC, and useful for Burgert afterward too.
  - **Verified end-to-end on the disposable local backend/frontend, all three of the brief's own testing requirements, not a success message:** deliberately corrupted a real line's `carpet_category` to disagree with its product, ran the monitor on demand — it correctly flagged that line AND independently caught the real historical Tile-line gap AND the real Conqueror wrong-formula line, three genuine findings, zero false positives. Confirmed directly against the database that not one byte of quote/line/product data changed as a result — the monitor only ever added `FlaggedRecord` rows. Confirmed the alert actually reaches the Owner in practice: logged in fresh, saw "⚠️ 3 data-consistency issues found automatically" the instant the home screen loaded, clicked through to the exact same Flagged for Review screen and resolved one via the existing Mark fixed/Dismiss buttons, unchanged. Confirmed Ryno's own real account never sees the banner at all.
- **Colour Default Risk + Save Quote Dead End From Price Check — two real, urgent bugs, both root-caused and fixed same day** (`Bolton-Brief-Colour-Default-SaveQuote-DeadEnd.docx`, "the second issue may be genuinely blocking quotes from being saved at all in this flow").
  - **§1, Colour Default Risk — confirmed real, not just a convenience gap.** `onVinylRangeChange()` (quote-builder.js) rebuilt the colour `<select>`'s options with none of them marked `selected` — a native `<select>` always auto-selects its first option in that case, so picking a Range silently pre-picked whichever colour happened to sort first, with nothing on screen looking wrong (unlike Branch/Sales Owner, there genuinely is no "usually correct" colour). Fixed with an explicit `"— Choose a colour (TBC) —"` placeholder option, selected by default, so nothing is ever silently picked. **Stronger than "flagged if it happens," per the brief's own §1 fallback ask:** both `addFloorJob()` branches (fresh add and in-place edit) now hard-block with `alert('Choose a colour first.')` when the placeholder is still selected — a TBC-coloured line can never actually be saved in the first place, the same "Pick a screed product first" guard Screed's own branch already had, just missing for Vinyl.
  - **§2, root-caused precisely, not assumed — confirmed by reading `saveQuote()` and the already-built `convertPriceCheckToQuote()` side by side.** Save Quote's own PUT was never actually broken: it succeeds fine even on a still-open Price Check (`client_name` just saves as plain text). The real gap is what happens next — the quote stays `is_price_check=True`, and `list_quotes()` deliberately excludes Price Checks from the Order Index Save Quote redirects to (a confirmed, correct rule from an earlier brief, "must not affect Order Index counts"). Nothing ever errors; the screen just changes to a list that correctly doesn't contain what was "saved" — indistinguishable, from Burgert's side, from Save Quote doing nothing at all. A separate "Convert to real quote" button already does this correctly today, but nothing stops the far more obvious Save Quote button from being used instead on exactly this screen.
  - **Fixed by having Save Quote itself trigger the identical conversion step first**, reusing the existing `POST /quotes/{id}/convert-to-quote` endpoint directly — zero backend changes needed. A new `currentQuoteIsPriceCheck` flag (mirroring `currentQuoteClientId`'s own exact tracking pattern — set/reset in the same three places: `loadQuote()`, `createQuote()`, `startPriceCheck()`) lets `saveQuote()` tell a still-open Price Check apart from a real quote without an extra round trip. Handles both of `convertPriceCheckToQuote()`'s own branches — prompts for a client name only when genuinely none is attached yet; reuses the existing `client_id` silently (no prompt) for a Price Check that already has one (e.g. started from that client's own page), since it would otherwise still stay invisible on the Order Index despite already having a real client on file.
  - **Verified end-to-end on the disposable local backend/frontend, reproducing the exact reported flow, not a success message:** started a real Price Check, selected a Vinyl range, confirmed the colour genuinely defaulted to TBC and that adding the line while still TBC was correctly blocked; picked a real colour, added the line successfully. Clicked Save Quote — the client-name prompt fired exactly as designed, a real client was created, `is_price_check` flipped to `false`, and the quote landed on the Order Index — confirmed visually, by name, in a real search of the real list (not just a 200 response). Confirmed the normal New Quote → New Client flow is unchanged: loaded a genuinely normal (non-Price-Check) quote and called Save Quote directly — no prompt fired, no behaviour change, saved and redirected exactly as it always has.
  - **Honest tooling note, not a code concern:** two earlier attempts to drive the FULL `createQuote()` flow end-to-end from a cold start (including its own pre-existing client-search/`confirm()` safety check) hung the browser tab — a native blocking dialog firing mid-script, the same documented Claude-in-Chrome limitation as every other `confirm()`/`window.prompt()` case this session has hit, not a regression in this fix. Verified the unaffected parts (normal Save Quote behaviour, the underlying real-quote-creation endpoint) directly instead once that was confirmed.
- **"Colour" Field Showing Products Instead of Real Colours — investigated first, confirmed a genuinely missing hierarchy level (not a relabel), fixed and verified against real price book data** (`Bolton-Brief-Vinyl-Range-Product-Colour-Hierarchy.docx`, "investigate the actual data shape before fixing").
  - **Confirmed the real cause with data, not a guess: two ranges, not one.** Queried every multi-row range in the real production price book for whether its "colours" shared an identical cost/pack-size/tile-dimensions — the actual tell that they're colourways of ONE product (every deZIGN/Aspen/Como/Numi range passed this check, confirmed genuinely 2-level and correctly untouched) versus several different real products wearing one shared range name. **Luxury Vinyl Planks** (18 rows, 14 distinct costs) and **Vinyl Composite Tiles** (2 rows, both distinct — "Clout 2.00mm"/"Clout 2.50mm," genuinely different thicknesses) both failed it — real, confirmed products (Select Plus, Hilton, Portland, Oak Tri-Fecta Medium/Large/XL, and 13 others, matching the Belgotex Cape Directs price list's own "LUXURY VINYL PLANKS" table row for row) sitting in the `colour` field with no other home for them. **Homogeneous Vinyl Sheeting/Tarkett/IQ Eminent** — the brief's own named suspicion for a second affected range — was checked and confirmed to not exist in the price book at all yet, so genuinely nothing to fix there, not silently skipped.
  - **New `FlooringProduct.product_variant` field** — the missing middle level (Range → Product → Colour), `None` for every genuinely 2-level range so the Vinyl picker's existing behaviour is completely unaffected there. A one-time, idempotent startup migration moves the real product name out of `colour` and into this new field for the two confirmed-affected ranges by name (never a live heuristic in production code — the cost-variance check above was an investigation tool, not something safe to run unattended against future data) — `colour` itself is left genuinely blank afterward, never guessed, since real per-product colour data doesn't exist anywhere in the price book yet for either range.
  - **Frontend gets a real, dynamic 3rd dropdown ("Product"), shown only for ranges that actually need it** — `onVinylRangeChange()` now checks whether any product under the selected range has `product_variant` set; if not, the original 2-level flow runs completely unchanged. If so, a Product dropdown appears (same TBC-style "— Choose a product —" placeholder as the Colour fix from earlier today, never silently defaulting), and choosing one either reveals a real Colour dropdown (if that specific product has genuine colour rows) or — since none currently do — a plain muted note ("No colour options loaded for this product yet... you can still add this line") instead of a fabricated single-option colour list, per the brief's own explicit "surface that clearly" fix requirement. The one real underlying price-book row is still selected automatically either way, so the line stays fully addable.
  - **Verified end-to-end on the disposable environment, seeded with the exact real pre-migration data shape (colour holding a product name, `product_variant` column not yet existing), all four of the brief's own testing requirements:** confirmed the migration correctly moved all 5 seeded rows' product names into the new field and blanked `colour`. Confirmed Luxury Vinyl Planks now shows Product (Hilton/Portland/Select Plus) then the honest "no colours loaded" note, and the line still adds correctly with the right price. Confirmed Vinyl Composite Tiles shows its own two real products (Clout 2.00mm/2.50mm) the same way. Confirmed deZIGN series 200 is completely unaffected — no Product dropdown appears, its real 17-colour dropdown with TBC default works exactly as before today's earlier fix. Confirmed editing an existing Luxury Vinyl Planks line correctly re-selects Range → Product → the exact original product on reopen.
- **Job Control Panel — investigate-and-propose only, no code written, per the brief's own explicit instruction** (`Bolton-Brief-Job-Control-Panel.docx`, "supersedes the earlier 'same feel as quoting' brief... prototype the information hierarchy before touching any underlying workflow logic"). Read `renderOrderDetail()`/`renderJobStepsHtml()`/`renderWorkflowActionsHtml()` (order-index.js) and `_job_workflow_info()`/`_job_steps()` (main.py) directly before proposing anything, per this project's own standing discipline.
- **Job Control Panel — proposal approved same day (all four decisions confirmed as recommended), built, verified, and deployed.** Presentation/interaction only, exactly as scoped — no schema or endpoint changes, no new workflow stages, every field/ID/handler on the old screen preserved, just regrouped.
  - **New primary status strip** (`jobControlPanelStatusHtml()`) reads the existing `_job_workflow_info()` engine — but nested under `data.workflow`, not directly on the quote, a real shape mismatch caught immediately by testing against a real running instance rather than trusted from memory (Order Index's `list_quotes()` flattens the identical fields onto each row; Job Detail's own `get_quote()` nests them instead — two genuinely different response shapes for the same underlying dict). Fixed before it ever reached the disposable browser check.
  - **A second real correction, also caught by testing against a live instance, not assumed from the approved proposal's own text:** the proposal's §5 table listed "scheduled, materials ordered, not yet received" as a second case needing a new 🟢 override. Walked a real disposable job through that exact state and confirmed `_job_workflow_info()` already returns `attention_priority="warning"` ("Confirm receipt") for it — not blank. Removed that override rather than ship a 🟢 that would never actually display (the existing warning always wins first); only the one case actually confirmed blank (completed, invoiced, unpaid) got the new override.
  - **Step map demoted** (`renderStepMapHtml()`) — dots + one summary line, moved below the primary panel; procurement tiles that used to render inline here moved out. Initially over-corrected a suspected "always shows the last step" bug by deriving the active step client-side — re-reading `_job_steps()` (main.py) past where the earlier read had stopped showed the server already computes `step["active"]` correctly (the "first not-done step" loop right before its own `return steps`). Reverted to trusting the server value; no real bug existed here.
  - **Six named, collapsed sections** (Customer/Quote/Materials/Installation/Financial/Documents) replace the old flat "Job Details" card and the two-column `.job-detail-layout` grid, per the approved field map. The section matching the job's current stage opens by default (`defaultOpenSection()`) rather than everything staying shut, a small usability addition consistent with the approved mockup's own Materials-open example. Materials now shows a compact per-Order-Sheet status line only; the real preview + action bar for each Order Sheet moved to Documents, alongside the Quote/Invoice preview (now under Quote) and the Document Archive + Job Card.
  - **A real regression caught during disposable verification, not shipped:** the rewrite dropped the `#jobDetailsSaveBanner` element entirely (the "✓ Saved" confirmation), silently no-oping since `showJobDetailsSaveBanner()` already guards for a missing element. Caught by actually calling `saveOrderDetails()` end-to-end and checking the banner's real display state, not just that the request succeeded. Restored above the six sections, shared by all three relocated "Save Job Details" buttons.
  - **Verified end-to-end on the disposable backend/frontend, in a real browser, across every real workflow state, not a success message:** walked one real job through quoted → accepted → scheduled → materials ordered → ready → completed → invoiced, confirming the correct 🔴/🟡/🟢 strip and default-open section at each stage (🔴 "Book installation" with no date set; 🟡 "Prepare job"/"Upcoming"; 🟢 "Waiting on customer decision" for a fresh quote; 🟢 "Invoiced — awaiting payment" for the new case). Confirmed On Hold correctly suppresses the new strip entirely (its own existing panel already says everything needed). Confirmed `saveOrderDetails()`/`logFollowUp()` both still work correctly with fields now split across three different sections. Hit the project's own documented stale-tab-cache issue once during this pass (confirmed via `typeof newFunction`, fixed with a hard reload) — noted, not a code bug.
  - **Re-verified against real production data**, same day: opened a real in-flight job (J-0002, "Hele Huis sonder kamers," Scheduled, materials ordered) on the live deployed app — correct 🟡 "Prepare job / Upcoming" strip, demoted step map at Step 3 of 4, and all six named sections rendering its real Order Sheets, deposit/balance, and document previews correctly.
  - **Honest gap, not tested:** mobile viewport rendering — same recurring, documented tooling limitation as prior entries this week (`resize_window` doesn't actually resize the real browser in this environment). The new layout is a plain single column with no fixed-width elements, so expected to behave the same narrow or wide, but not independently confirmed on a real phone.
  - **Two things the brief asks for are already built and just need surfacing, not building:** `_job_workflow_info()` already computes a real 🔴/🟠/🟡 (critical/warning/notice) traffic-light signal per job, and the Order Index's own "Needs Attention" panel already answers the brief's closing question ("what requires me today, across all active jobs") — neither needed on this brief's own single-job page until now, because Job Detail itself never reads either signal; its only visible state today is the plain `workflow_status` pill plus a buried plain-text sentence.
  - **Real hierarchy inversion found and named, not assumed from the brief's own framing alone:** the numbered step-strip (`renderJobStepsHtml()`) renders first, in full colour, ABOVE the actual action buttons — exactly backwards from the brief's "status/Next Action primary, map demoted" ask — and "Job Details" is one long, always-expanded card mixing six unrelated concerns (client link, site address, four date fields, deposit, override total, Job Card, Follow-Ups) with no progressive disclosure by stage.
  - **Action-vs-outcome audit: the data model already gets this right — `materials_ordered` is strictly derived from real `OrderSheet.status=="placed"` rows, `ready_for_installation` is its own separate manual outcome-click — the gap is presentation, not logic.** The one place a genuinely healthy wait state is already expressed in words today (`workflow_status==='completed' && invoiced && !paid` → "Invoiced {date} — log payment below") renders as plain grey muted text with no colour or priority, buried in the Workflow card — the exact "healthy waiting, never presented as broken" case the brief describes, just not yet given the visual weight it already deserves.
  - **Two of the brief's own illustrative examples checked directly against `models.py` and found not to exist in Bolton's data yet, flagged rather than invented:** no field anywhere records a supplier's expected/promised delivery date (only `OrderSheet.placed_at` — when it was ordered), so the brief's own "Delivery expected 2 September" wording can't be shown honestly; and there is no separate "signed off" field — Bolton's real workflow ends at `final_payment_date`, so the brief's own "Paid ≠ Signed off" distinction names a state that doesn't exist yet.
  - **Delivered as a written proposal** (current-state findings, a diagnosis table, two full mockups styled in Bolton's own real palette against the brief's own Smith Residence example, a stage-by-stage traffic-light derivation table using only existing fields, a section-by-section map of exactly which existing fields/cards move where, and four explicit open decisions for Burgert — wording for the new "awaiting delivery" state, whether Job Detail's own traffic light may diverge presentationally from the Order Index's shared engine, what to do about the non-existent "signed off" state, and where the Follow-Up log belongs) — published for review; nothing here has been built, per the brief's own deliverable requirement.

---

## 2026-08-29

Direct continuation of the previous day's Vinyl Quoting UX Redesign work — one commit, just past midnight.

### What shipped
- **Vinyl Quoting UX Redesign, Phase 4: live price preview, via a real backend endpoint.** Approved proposal §01/§10, and a real design decision Burgert made explicitly rather than have it picked silently: build it consistent (a backend endpoint reusing the real calculation functions) rather than repeat Flooring's own `fjCalc()` pattern — a client-side JS reimplementation of the pricing formula, an accepted-but-real "shadow calculation" for that one category already bitten once before (a hardcoded VAT rate that drifted from Business Settings). New `GET /quotes/lines/preview` calls the exact same `calculate_blinds_line()`/`calculate_trim_line()`/a new `_compute_stairwell_calc()` the real add/edit endpoints already use — one formula per category in the whole codebase, whether previewing or actually saving. Never writes anything — no `QuoteLineItem`, no `AuditLog`, no `session.commit()`. No preview for Misc — amount minus cost is plain arithmetic, not a real formula to duplicate, already computed directly in the frontend wherever it's shown.
- **`_compute_stairwell_calc()` extracted from `add_stairwell_line()`** — the preview endpoint would otherwise have been a third copy of the same product-lookup + `calculate_stairwell_line()` + landing-combining logic (add, edit, now preview). `edit_stairwell_line()` (Phase 1, previous day) refactored to call the same shared helper too.
- **Frontend**: `#genericLinePreview` box added to the Add Line Item card, wired via one delegated `input`/`change` listener on the whole card (same technique as Keyboard Dismiss on Enter, 2026-08-25) rather than an attribute on every individual field — covers Blinds/Skirting/Trim/Stairwell today and anything added to those forms later with zero extra wiring. Debounced 300ms.
- **Vinyl Redesign real-usage fixes** — four items from Burgert actually using the shipped redesign the same morning (`Bolton-Brief-Vinyl-Redesign-Real-Usage-Findings.docx`, fetched from Downloads):
  1. **Skirting/Trim category conflation — REAL GAP, confirmed not relabeled.** `add_trim_line()`/`edit_trim_line()` hardcoded `category="trim"` for every line, so a Skirting line has always been stored, displayed, and rated for commission as "trim". Checked the downstream impact before fixing anything, per the brief's own instruction: `generate_order_sheets()` never looks at trim/skirting lines at all (confirmed by reading it), so no Order Sheet impact. Real impact found instead — `CommissionRate.category`'s own model comment already lists "skirting" as a real, intended, separately-configurable builder-rep commission category that could never have actually matched, though no such rate has ever been configured, so no commission was ever paid wrong; the bug was real and dormant, not yet triggered. Fixed at the source (`_trim_line_category()`, shared by add/edit) plus a startup migration that found and corrected 2 real historical lines — "Pine Skirting 69mm" and "Pine Skirting 96mm", the exact product the brief's own example named. Fixing the source surfaced three more real, connected bugs a display-only patch would have left broken: the Quote Lines detail column, the qty/cost calc, and the actual client-facing printed/PDF document all checked `category==='trim'` only, so a skirting line's length would have silently vanished from all three the moment the category became genuinely correct.
  2. **Extra Screed auto-pricing** — "Amount charged" now auto-populates from Bags needed × R350 incl VAT (Burgert's own confirmed real sell rate), reconciled ex-VAT internally, stays editable/overridable per room (`fpAmountManuallyEdited`).
  3. **Sticky Save** — added directly into the existing persistent summary panel (Phase 3), not a third sticky element, per the brief's own suggested resolution — calls the existing `saveQuote()` directly.
  4. **Transport & Courier repositioned** to sit just above Discount, after Screed/Extra Rooms — DOM position only, no field/id/handler touched.
- **Real regression from the Skirting/Trim fix, caught and fixed same day, on direct instruction: `line_real_cost()` (calculations.py).** The shared cost basis for both the quote's own margin check and `pure_sales` commission's GP calculation only ever checked `category == "trim"` — the moment skirting became its own real category, it silently fell through to the blinds-shaped `unit_cost` (no length multiplier), understating a skirting line's real cost by exactly its length in metres. Understated cost means overstated GP means overstated commission. Confirmed directly: commission is paid on a quote's TOTAL GP regardless of whether it's only screed, only skirting, or only trim — never a special case. Fixed and verified end-to-end: a real 20m skirting line's cost was R26.78 (buggy) vs. R535.60 (correct); a full `commission_statement()` run on a skirting-only quote now returns a sane GP/commission figure instead of the ~98% "margin" the bug would have produced.
- **New Quote Pre-Population bug, real gap found and fixed** (`Bolton-Brief-New-Quote-PrePopulation-Bug.docx`, flagged priority, treated as a data-integrity risk not cosmetic UX per the brief's own framing). Investigated the actual mechanism first, per the brief's own explicit instruction, before touching anything: confirmed via a direct database check that `create_quote()` always produces a genuinely empty Quote row — never (b) real DB-level carry-over at creation. The real mechanism — three of four ways to reach Quote Builder (`startQuoteForClient()`, `startQuoteFromBuilderEstimate()`, `startQuoteWithVinylRange()`) already called `resetQuoteBuilderUI()`, a fix from an earlier brief (Client-Link Audit) covering exactly three entry points. The fourth — the "Blinds" landing tile — was missed by that fix the whole time: its "no quote currently open" branch did nothing at all, leaving `currentQuoteId`, the lines cache, client fields, and (since yesterday) the persistent summary panel showing whatever was last viewed. Confirmed NOT merely cosmetic: since `currentQuoteId` itself was never cleared, clicking this tile after viewing/editing an EXISTING (not deleted) quote and adding a line would have written that real line onto the wrong, unrelated quote. Fixed with the identical guard shape `startQuoteWithVinylRange()` already uses.
- **Flooring live-price regression — investigated thoroughly, could NOT reproduce; no code changed.** (`Bolton-Brief-Flooring-Live-Price-Regression.docx` — reported symptom: typing m² on the Flooring tab no longer updates the price pane live, only after "Add Floor Job".) Per the brief's own explicit instruction to confirm the actual mechanism, not just whether the visible price changes: got `jsdom` running in this session for the first time and built a real simulation loading the actual production `index.html`/`quote-builder.js` — confirmed byte-for-byte identical to the live deployed files (`diff`, zero output) — then dispatched a genuine `input` event on `fj_floor_m2` and confirmed `fjCalc()` fires and updates the DOM correctly, both on a fresh page and after cycling through Blinds → Trim → Stairwell → Flooring first (in case repeated tab-switching was accumulating some bad state). Every run updated correctly. No missing/misattached listener found, no thrown error, no stale cache (deployed files match source exactly). Deliberately did NOT "fix" anything, per this project's own standing discipline — there's nothing demonstrated broken in the current code to fix, and changing working code while chasing an unreproduced symptom risks introducing a real bug in place of a phantom one.
- **Flooring live-price regression — RE-CONFIRMED live, in a real browser, with Burgert, same day.** Got Claude in Chrome connected this session for the first time and drove the actual production app directly: New Quote → Aspen Herringbone Range 2mm → Price Check — typed `50` into Floor size (m²), the Result pane updated instantly and correctly (54.00m² needed, 39 boxes, R11,321.70 box total); changed it to `120`, updated again correctly; changed Markup % to `40`, updated correctly too. Could not reproduce the reported symptom at all. This is also the first live click-through of ANY part of the whole Vinyl Quoting UX Redesign (Phases 1–4, all built and verified only via simulation/direct backend testing up to this point) — and everything else visible in the same session confirmed working correctly too: category tabs, the persistent summary panel (correct ✓/○ rows and running total), Sales Owner/Branch defaulting to Burgert/Hermanus, the version badge (`#7eac98c`), Transport & Courier's new position, "Add Floor Job to Quote" landing correctly in the summary panel, and the new Save Quote button inside it.

- **Document Preview floating-panel overlap — investigated, root-caused, fixed and verified same day** (`Bolton-Brief-Document-Preview-Overlap-Bug.docx`). Confirmed via source, not screen-recording alone (no working mobile viewport was obtainable in this session — `resize_window` did not actually resize the real browser window here, a tooling limitation, not something about the app): `showSendActionsPanel()` (shared.js) built a `position:fixed; bottom:16px; right:16px` panel appended straight to `<body>`, called by `renderPrintDoc()` (the Print button/flow) on every Print — with no awareness of whatever Document Preview content happened to be scrolling underneath it, and an Email button that stayed clickable even when labelled "(no address)". Confirmed this was NOT scope creep from today's work — it's the original Aug 2026 "Client-Side Commercial Workflow" Print flow, deliberately preserved unchanged ("Print stays exactly as it is today, no changes there") when the later, correct, standardized Mail button (`documentActionBarHtml()`/`sendDocumentEmail()`) was added — a normal inline button, not an overlay, that already refuses cleanly with a real message when there's no email on file. That Mail button's existence retires the panel's entire original justification (a clickable Email action needed a home outside of `#printArea`, which is invisible outside an actual print render). Removed `showSendActionsPanel()` entirely and its call site; Print now just prints. Verified directly against the real running disposable backend: `renderPrintDoc(26, 'quote')` no longer creates `#sendActionsPanel` at all; `sendDocumentEmail(26, 'quote')` against a client with no email correctly alerts instead of offering a dead button; `#printArea` still populates correctly (31,704 chars) — the actual Print/PDF output itself is unaffected.
- **Full Real-Browser Walkthrough & Audit** (`Bolton-Brief-Full-Real-Browser-Audit.docx`) — an end-to-end lifecycle walkthrough (Lead → Quote → Accept → Deposit → Booking → Order Sheet → Job Card → Installation → Invoice → final payment → Order Index/Dashboard), run for real, never against production: a disposable copy of `bolton.db` served by a throwaway local backend (port 8010) and a throwaway local frontend (port 8090, `shared.js`'s `API` constant repointed only in the disposable copy), driven with Claude in Chrome — not jsdom, not reasoning about code alone. Login handled by minting a fresh `UserSession` token directly in the disposable database (no password touched, knowable, or entered — the disposable DB is fully test-owned) and setting it in `localStorage`, since the app's own auth was otherwise unchanged. Per the brief's own instruction, only the two pre-approved bugs above were fixed in-session; everything else below is a logged finding for review, not a reflexive fix:
  - **Quote-level Discount % cannot be set or changed without also adding a new Flooring line — HIGH.** `fj_discount_pct` (index.html) is the ONLY whole-quote discount control in the entire app, and it lives solely inside the Flooring category's Add Line Item card. It's read and PUT to `/quotes/{id}/discount` only as a side effect of `addFloorJob()` (quote-builder.js) — which itself refuses to run at all unless a floor size is entered (`if (!floorM2...) { alert(...); return; }`). `saveQuote()` never touches `discount_pct`. Reproduced directly: set Discount % to 5, clicked "Save Quote" (the visually obvious "I'm done" action next to it) — the discount was silently dropped, confirmed via the API (`discount_pct: 0.0`, `discount_amount: 0.0`) with zero error or warning anywhere. A quote with no Flooring line at all (a pure Blinds/Skirting/Trim/Stairwell job — now a completely normal shape since the Vinyl Redesign) has literally no way to apply a discount. The one working path is non-obvious: editing an EXISTING Flooring/Screed line's own form and saving that edit also fires the same PUT, since that code runs unconditionally before the add/edit branch — but this only works if a Flooring line already exists.
  - **Order Sheet generation silently omits Stairwell vinyl material — HIGH.** `generate_order_sheets()`'s own docstring documents Blinds as an intentional Phase 1 scope limit and Trim as intentionally excluded (Burgert orders those separately in bulk from Supertrim) — both deliberate, both fine. Stairwell is never mentioned at all: its `material_lines`/`screed_lines` filters only ever match `category == "flooring"`, and a Stairwell line's own vinyl product (`category == "stairwell"`, its own `vinyl_product_id`, its own box requirement) is never looked at, even though it's real, orderable material from the same supplier as the rest of the job. Confirmed end-to-end on a real 15-stair test line needing "3 boxes" of `deZIGN series 120` (a DIFFERENT product from the job's main `deZIGN series 200` Flooring line) — the generated Order Sheet, and the Job Card's own "Materials to load" (sourced directly from Order Sheets, so it inherits the same gap), both show only the main Flooring/Screed line; the stairwell vinyl never appears anywhere an installer or buyer would actually look. Very likely simply never revisited when Stairwell became a first-class quote category during this week's Vinyl Redesign — Order Sheets predate it.
  - **Client's `preferred_branch` defaults to a hardcoded "gansbaai", disconnected from the actual quote/lead — MEDIUM.** `convert_lead()` creates a new `Client` without ever setting `preferred_branch`, so it silently falls back to the `Client` model's own hardcoded field default (`models.py`, `preferred_branch: str = "gansbaai"`) regardless of which branch the lead/quote actually belongs to. Reproduced directly: a lead converted under Sales Owner Burgert (whose own default branch is Hermanus) produced a Quote correctly branched "hermanus", but the new Client record's own `preferred_branch` came out "gansbaai" — not derived from anything real on the lead or quote, just the model's own static default.
  - **Order Index search box drops characters, even at human typing speed — MEDIUM.** The search input's `oninput` (`order-index.js`) rebuilds the ENTIRE `#landing` innerHTML — including the input element itself — on every keystroke, with a documented, deliberate focus-restore (`input.focus()` + `setSelectionRange`) specifically to counter this. Confirmed it doesn't fully win the race even at a genuine ~1 keystroke/second pace (not just rapid automated typing): typing "Jane" character-by-character with 1-second pauses between each landed as "ane" in the box — the first character was dropped — though the underlying filter still matched correctly against the real search term server/cache-side. Filtering itself works; the visible characters in the box can't be trusted to match what was actually typed.
  - **Stale QA/test client records visible in the live production Order Index "Needs Attention" list — LOW, but real.** Observed directly while testing (not part of my own test data): "Edit-In-Place Test Client 2", "WF QA Hold Client", "Force Delete QA Client", "WF QA Merge Client" and similar all still appear as real, actionable rows ("Book installation", "Create Invoice", etc.) in the disposable copy of the actual production database — meaning they're sitting in real production data today, contributing to real KPI/Next-Action counts on the live Order Index and Business Overview Dashboard.
  - **Blinds price book is genuinely empty — zero products** (confirmed via `GET /price-book/blinds` → `[]`) — not a bug, but a real, confirmed operational gap: the whole Blinds category (tab, live preview, quote line) is fully built and works correctly by mechanism (confirmed via its own preview endpoint), but cannot be exercised end-to-end with real data, and no live quote has ever been able to include a real Blinds line, because there is nothing in the price book to select.
  - **Everything else tested and confirmed PASSING, real, in this real-browser session:** Lead creation → outcome-note logging (Mark Contacted, a real `prompt()`-driven note correctly landing in Activity) → Convert to Quote (client/quote correctly linked, original enquiry notes correctly carried all the way to the Job Card). Full quote build across all seven categories with live backend-preview pricing confirmed correct and instant on every category that has real price-book data (Flooring, Screed, Skirting, Trim, Stairwell, Stairwell+landing combined calc, Misc) — Flooring's own `fjCalc()` live-price mechanism re-confirmed correct a fourth time (jsdom ×2, real production browser, and now this disposable-environment browser), never reproduced. Stairwell line editing (12→15 stairs) correctly updated the SAME line, no duplicate created. Transport levy correctly independent of Save/Add-Line (its own dedicated endpoint, by design, unlike Discount). Accept Quote → real Job Number assigned (J-0004) → deposit recorded → installation date confirmed → Scheduled → Order Sheet generated with correct supplier-merge grouping (Azura: Flooring + Floor Prep combined into one sheet) → Mark as Placed → Job Card (confirmed genuinely free of any price/cost/margin figure anywhere on it) → Mark Installation Complete → Invoice generated → invoice-sent date + final payment date/method recorded → all four workflow steps correctly reached `done: true` → Order Index Next Action correctly surfaced "Invoice" (not stuck on the stale, never-marked-received Order Sheet) → Business Overview Dashboard's Today/This-Week figures correctly reflected the real accepted quote total the moment it was accepted.
- **Audit Findings: Fixes** (`Bolton-Brief-Audit-Findings-Fixes.docx`) — four of the five findings above approved and fixed, in the brief's own stated priority order, each verified against a real data flow (never a code-review-only or "success message" check) on the same disposable local backend/frontend, then re-verified against real production behaviour once deployed:
  1. **Order Sheets/Job Card now include Stairwell vinyl (HIGH, fixed).** `generate_order_sheets()` (main.py) gained a `stairwell_material_line_data()` helper mirroring `material_line_data()`'s own box-rounding/discount-breakdown logic, applied to a stairwell line's own vinyl (`product_id`) instead of a normal Flooring line's, combining stair-tread boxes (`l.boxes_needed`, already stored) with landing boxes (recomputed from `l.landing_area_m2` the same fallback way an older flooring line missing `boxes_needed` already gets recomputed) into one total. Merges into the SAME supplier-grouped sheet as any other flooring material — no separate sheet, no per-supplier special case. The Job Card needed zero changes at all: `jobCardMaterialsHtml()` (order-index.js) already just reflects whatever Order Sheets exist, so it inherited the fix automatically. Verified on the real J-0004 test job (15-stair line, 2.5m² landing, `deZIGN series 120`, a DIFFERENT product from the job's own `deZIGN series 200` Flooring line): re-generating the Order Sheet now produces a correct 4-box "deZIGN series 120 (stairwell vinyl)" line on the SAME Azura sheet as the Flooring/Screed lines, and the Job Card's own materials-to-load table now lists it too.
  2. **Quote-level Discount % no longer tied to the presence of a Flooring line (HIGH, fixed).** The actual bug: `#fj_discount_pct` lived inside the Flooring-only `#fjMain` area and was only ever read/PUT to `/quotes/{id}/discount` as a side effect of `addFloorJob()` — itself refusing to run at all without a floor size. Moved the field into its own standalone, always-visible `#quoteDiscountCard` (same id, same DOM element, just relocated — `fjCalc()`'s own live-preview math still reads it fine) and gave it the exact same independent "fire immediately, any time" pattern Transport Levy already had (`updateQuoteDiscount()`, mirroring `updateTransportLevy()`) — `onchange` now PUTs straight to the discount endpoint and refreshes the total, with zero dependency on adding a line. Removed the old PUT from `addFloorJob()` entirely rather than leaving a redundant duplicate. `loadQuote()` now also populates the field from the quote's real, saved `discount_pct` on every load — a real, separate gap closed in passing: this field was previously write-only, never reflecting the actual saved value on reopen, which would have shown 0 even when a real discount was already applied. Verified end-to-end on a brand-new Trim-only quote (zero Flooring lines, the exact scenario the bug made impossible before): set Discount % to 10, tabbed away — total dropped from R1105.31 to R994.78 immediately, confirmed persisted via the API, survived clicking Save Quote (the original failure mode), and correctly re-populated as "10" on reopening the quote.
  3. **New Client's `preferred_branch` no longer hardcoded to "gansbaai" (MEDIUM, fixed).** Root cause was broader than the one call site the brief named: `convert_lead()` (Lead conversion) had the gap, but so did `_resolve_or_create_client()` — the shared helper `create_quote()` itself (the main "+ New Quote" flow, the MORE common path), `convert_price_check_to_quote()`, and `update_quote_details()`'s own client-link branch all funnel through. Fixed at the shared helper: `_resolve_or_create_client()` gained an optional `branch` param, passed by all four real call sites from whatever real branch value they already had in scope (`create_quote()`'s own `branch` param; `quote.branch` for the other three) — a brand-new Client record now gets `preferred_branch` set from the ACTUAL quote/lead it was created for, not the model's own static default, which now only applies to a genuinely branch-less edge case rather than silently to every new client. Verified on both paths directly: a lead converted with `branch=hermanus` produced a client with `preferred_branch: "hermanus"`; a quote created directly via `create_quote()` with the same branch did too — both previously would have produced "gansbaai" regardless.
  4. **Order Index search no longer drops characters (MEDIUM, fixed).** Confirmed root cause matched the brief's own suspicion: `oninput` fired a full re-fetch + full `#landing` innerHTML rebuild (including the search `<input>` itself) on EVERY keystroke, racing an existing, deliberate focus-restore that couldn't reliably win against the next keystroke arriving mid-cycle — confirmed dropping characters even at ~1 keystroke/second, not just rapid automated typing. Fixed with a 300ms debounce (`scheduleOrderIndexSearch()`, same established pattern as the Quote Builder's own live-preview debounce, `scheduleGenericLinePreview()`) — the input's own DOM node is now never touched mid-word, so nothing can race it; the one re-render that does happen once typing actually pauses only has to win against a keystroke that's already 300ms in the past. Verified in a real browser: typing "Branch Test" at full natural speed (no artificial slowdown — the exact scenario that dropped characters before) now lands in the box completely intact and filters correctly, first try.
  5. **Blinds empty price book — confirmed not a bug, no fix made,** per the brief's own explicit "no fix required now" instruction.
  - **Stale QA/test client cleanup — NOT performed.** The existing Force Delete mechanism (Owner-only, `DELETE /quotes/{id}?force=true` — the exact override already built and reserved for "known mockup/test data", main.py's own delete-quote docstring) is the right, already-built tool for this, reachable from each stale job's own ⋮ overflow menu → Delete on the real production Order Index (offered automatically once a normal delete is blocked by real recorded history). This wasn't run: permanently deleting real data is outside what gets done directly in this session regardless of instruction — flagged back to Burgert to run himself, with the exact mechanism identified rather than a vague pointer.
- **Carpet Calculators — investigated, proposed, approved, backend built and verified, same day.** Four new categories (Tufted Broadloom, Needlepunch Broadloom, NEXBAC 920 Tiles, Cushion Vinyl), fully additive per the brief's own non-negotiable constraint. Full cycle: opened and read the Belgotex Cape Directs price list (PDF, all 6 pages) and all three of Burgert's own reference spreadsheets (Berberpoint 920, Influence, Amble Cushion Vinyl) cell-by-cell for their real formulas — not skimmed — sent a downloadable HTML proposal (`SendUserFile`, same format as the Vinyl Redesign proposal) flagging two real commercial discrepancies between those spreadsheets and Bolton's actual confirmed Vinyl methodology (VAT-before-markup; 40% vs. Vinyl's 30%) rather than reproducing them, plus six concrete open questions each backed by a real number found in Burgert's own files, not guessed. A consolidated Final Build Brief resolved all six (markup 30%, cutting fee R350 confirmed, Teckem TEC 008 glue R1,232/20L drum, Fotakis grippers R600/122LM box (provisional), underfelt assumed same as the price list's existing "Green" underlay) and gave a clear proceed/hold status per category — backend built and verified same session:
  - **`calculate_carpet_line()` (calculations.py)** — a genuinely new, separate function for the three continuous-roll types (Tufted/Needlepunch Broadloom, Cushion Vinyl: LM × roll width → m², a physical shape Vinyl's box model has no equivalent for), sharing zero code with `calculate_flooring_line()`. NEXBAC 920 tiles need no new calc code at all — they're bought by the box exactly like Vinyl tiles, so they call `calculate_flooring_line()` directly, unmodified, with a new `flooring_category="carpet_tile"` product row.
  - **Sequencing confirmed to follow Bolton's real methodology, not the reference spreadsheets':** cost+extras → markup → +labour, all ex VAT; VAT only ever applied once, at the whole-quote total — never per-line, unlike all three of Burgert's own spreadsheets (which apply VAT before their 40% markup).
  - **New endpoints:** `POST/PUT .../lines/carpet` (add/edit) plus a new `carpet` branch in the live-preview endpoint (`_compute_carpet_calc()`, mirroring `_compute_stairwell_calc()`'s "one formula, whether previewing or saving" shape) resolving every confirmed rate from `BusinessSettings` (new fields: `carpet_cutting_fee`, `carpet_cushion_vinyl_cutting_fee` defaulting to 0/unconfirmed, `carpet_gripper_cost_per_box`/`carpet_gripper_lm_per_box`, `carpet_underfelt_cost_per_m2`/`carpet_underfelt_roll_width_m`, `carpet_glue_cost_per_20l_drum`) or the product record (`FlooringProduct.labour_rate_per_m2` override, resolving Tufted Broadloom's own open R35-vs-R45 question per-product rather than a new hardcoded setting, per the brief's explicit instruction not to add a second labour rate).
  - **Order Sheet generation extended, not touched:** `generate_order_sheets()` gained a `carpet_roll_lines` loop (new `carpet_roll_line_data()`, showing LM — the unit a supplier actually sells this by — not m²) merging into the same supplier-grouped sheet as everything else; explicitly excludes grippers/underfelt/glue as confirmed stock items, while still generating a real line for the roll itself. NEXBAC tiles need zero Order Sheet changes — already inside the existing box-based `material_lines` path, unchanged. The Job Card needed zero changes at all, confirmed live — it already just reflects whatever Order Sheets exist.
  - **New `QuoteLineItem`/`FlooringProduct` fields**, all denormalized-snapshot pattern consistent with every other category (`roll_width_m`, `carpet_category`, `quantity_lm`, `gripper_perimeter_m`/`gripper_cost_total`, `underfelt_area_m2`/`underfelt_cost_total`, `cutting_fee_total`) — glue reuses the existing `glue_cost_total`/`glue_units_needed`/`glue_sell_total` fields verbatim.
  - **Real gap caught and fixed before it shipped:** the three new carpet cost fields (`gripper_cost_total`, `underfelt_cost_total`, `cutting_fee_total`) were initially missing from `strip_sensitive_fields()`'s Sales-role strip list — caught by reasoning through the existing list's own stated scope ("everything that reveals what a job actually costs"), not by a failing test, and fixed before any live verification ran with the Sales role.
  - **Real bug caught before shipping, not after (same class as twice earlier today):** `calculate_carpet_line` was written but never added to `main.py`'s own `from calculations import ...` line — a live `NameError` on the very first real preview call, caught immediately by actually calling the endpoint against a real running disposable backend, not by `py_compile`/`ast.parse` (which both passed regardless, syntax-only). Fixed by adding it to the import.
  - **A seed migration** (same "real business data, added via a one-time idempotent per-product-gated startup migration" precedent as Aspen's R15/m² delivery fee) adds the four real products (Influence, Berber Point 920, Berber Point 920 NEXBAC, Amble) to the price book — confirmed real cost prices from the Belgotex price list, supplier "Belgotex", 30% markup, 0% trade discount (confirmed — every reference spreadsheet's own "Less 0%" cell).
  - **Verified end-to-end against a real disposable backend, hand-checked against manually worked-out numbers, not a success message:** all four categories added as real `QuoteLineItem` rows on a real quote — Tufted Broadloom (20 LM, grippers, underfelt, cutting fee) → **R30,910.87**, hand-calculated to the cent beforehand and matched exactly; Needlepunch Broadloom (20 LM, cutting fee, glue) → **R28,761.32**, exact match; Cushion Vinyl (20 LM, glue) → **R18,268.80**, exact match; NEXBAC 920 Tiles (20 m², via the unmodified Vinyl box endpoint) → **R13,250.00**, exact match, 5 boxes needed — confirming the box math is genuinely untouched. Editing a carpet line (25 LM, re-run) correctly updated the SAME line, no duplicate. Generated a real Order Sheet for all four lines together: correctly showed 5 boxes (NEXBAC) + 20 LM × 3 (Influence/Berber Point 920/Amble), all on one combined Belgotex sheet, with zero mention of grippers/underfelt/glue anywhere on it. Sales role: confirmed zero cost fields leaked on any of the three carpet lines. **Vinyl non-negotiable constraint, confirmed two ways:** `calculate_flooring_line()`'s function body diffed byte-for-byte identical against the pre-session git `HEAD`; a real, pre-existing Vinyl quote (#26, deZIGN series 200 + ITE F10) re-fetched from the same running backend and confirmed producing the exact same totals as every earlier test of it today (R22,808.55 / R10,140.00 / R53,826.54).
  - **Deliberately NOT built this session: frontend UI.** The brief's own "proceed to first-draft coding" was satisfied at the backend/calculation layer — the layer with the real risk (money, Vinyl safety) and the layer the brief's own testing requirement is actually about (hand-checked math, Order Sheet correctness, Vinyl untouched), all confirmed via the same real-authenticated-API-call verification method used for every fix earlier today. Adding four new Quote Builder categories' worth of UI (tabs or sub-selectors, input forms, live-preview wiring, summary panel rows) is a real, separate scope of work in its own right — flagged as the natural next step, not silently skipped.
- **Carpet Tab, Type Split, and Product Filtering — a real, confirmed pricing bug, found and fixed same day.** Direct, urgent consequence of shipping the Carpet Calculators backend with no frontend of its own the same session: the four new products (their own real `FlooringProduct` rows, `flooring_category`-tagged) landed in the SAME table Vinyl's own product dropdowns read from, and neither `populateVinylRangeDropdown()` nor the Flooring Quotes drill-down (`startQuoteWithVinylRange()`) filtered on `flooring_category` at all — only on `pricing_type`. Confirmed on screen (screenshot evidence): "Berber Point 920" (Needlepunch Broadloom) selectable inside the Vinyl-only calculator panel, run through `calculate_flooring_line()`'s box-based formula (no `m2_per_pack` on a roll product — silently produced wrong numbers rather than erroring) instead of its own already-confirmed LM-based `calculate_carpet_line()`.
  - **Root cause fixed at both real entry points**, not just the one named in the brief: `CARPET_ONLY_CATEGORIES` (quote-builder.js) excludes all four carpet categories from `populateVinylRangeDropdown()`'s range AND colour dropdowns; a new `startQuoteWithFlooringRange(range, category)` dispatcher replaces the Flooring Quotes drill-down's unconditional `startQuoteWithVinylRange()` call — genuine Vinyl-family categories keep the exact same behaviour as before, unchanged; the four carpet categories now correctly route into the new Carpet tab instead.
  - **New Carpet tab, its own dedicated card** (`#carpetLineCard`, same reasoning `#fjMain` gets its own card instead of reusing the generic Blinds/Trim/Stairwell one) — first choice is TYPE (Stretch/Glued Down/Cushion Vinyl/NEXBAC Tile, directly answering the earlier Carpet Calculators proposal's own open tab-structure question), each type's product dropdown filtered to ONLY that type's real `flooring_category`, wired to the already-built backend: Stretch/Glued Down/Cushion Vinyl call the new `/lines/carpet` endpoints (LM input, live preview via the existing `carpet` preview branch); NEXBAC Tile reuses the EXISTING unmodified Vinyl box endpoint directly (m² input) — genuinely the same engine, not a fourth copy. Grippers/underfelt/adhesive/cutting-fee are applied automatically wherever the brief states them as a requirement of what a type physically IS (Tufted always needs grippers+underfelt; Needlepunch/Cushion Vinyl are glued down BY DEFINITION) — a checkbox appears ONLY where the Final Build Brief itself left the rate genuinely unconfirmed (Cushion Vinyl's own cutting fee; whether NEXBAC tiles need adhesive at all), both defaulting off, matching that brief's own instruction not to assume either way.
  - **Edit-in-place wired too, not just Add** — real gap caught while building this: `editQuoteLine()`'s existing `category === 'flooring'` branch would have opened Vinyl's own `#fjMain` panel for a carpet line (also `category === 'flooring'` underneath, by design), the exact routing mistake this whole brief exists to fix, just via a different door. Checked `line.carpet_category` FIRST, before the generic flooring check, routing to a new `prefillCarpetEdit()`/Carpet tab instead.
  - **Summary panel gained its own Carpet bucket** (`lineSummaryBucket()`) rather than lumping carpet lines into plain "Flooring" — same "pulled out, not mixed in" treatment Screed already gets. Quote Lines table detail column and the client-facing printed/PDF document (shared.js) both extended to show LM (and type, internally) for a carpet line instead of the Vinyl-shaped "X m² — jobtype" string, which would have shown a broken-looking "X m² — null" otherwise (`job_type` is never set on a carpet line).
  - **Verified against the real fix, in a real browser, not a success message:** confirmed "Berber Point 920"/"Influence"/"Berber Point 920 NEXBAC"/"Amble" are completely gone from the live Vinyl range dropdown (spot-checked the full remaining list — genuine Vinyl products only); selected Berber Point 920 via Carpet → Glued Down, live preview showed **R28,761.32** — the exact same figure hand-verified against the backend directly for this brief's approval — added it, summary panel correctly showed a distinct "✓ Carpet R28,761.32" row; edited the same line (LM 20→25) and confirmed it updated the SAME line via the API, no duplicate; independently confirmed all four types' product dropdowns show ONLY their own real products (Stretch → Influence only, Glued Down → Berber Point 920 only, Cushion Vinyl → Amble only, NEXBAC Tile → Berber Point 920 NEXBAC only); walked the real Flooring Quotes drill-down end to end — clicking "Berber Point 920" under `carpet_needlepunch_broadloom` now correctly lands on the Carpet tab with Glued Down + Berber Point 920 pre-selected, not Vinyl's panel.
  - **Known, minor, non-functional gap found and left as-is:** the carpet-type pill button's own `.active` visual highlight doesn't reliably apply when arrived at via the Flooring Quotes drill-down specifically (works correctly on a direct manual click) — the underlying state (`activeCarpetType`, the selected product, the live-preview figure) was confirmed correct every time regardless; a likely async-ordering race between `toggleLineFields()`'s own internal `selectCarpetType()` call and the drill-down's explicit one, not chased further given it doesn't affect pricing or which product/type is actually selected.
  - **Real production exposure check — clean, confirmed directly, not assumed.** The Carpet Calculators seed migration (this same day, earlier entry above) had been live for the whole window between that deploy and this fix — the real window during which a carpet product could have been mispriced through the broken Vinyl path. Checked with Burgert logged into the real production app himself (no password touched, same discipline as every other live-browser session today): fetched the real `FlooringProduct` ids for all four seeded products (188 Influence, 189 Berber Point 920, 190 Berber Point 920 NEXBAC, 191 Amble) via the price book endpoint, then fetched every one of production's real quotes (11 total — small enough to check exhaustively, not sample) and inspected every line on every one for any of those four `product_id`s. Zero matches — no real quote has ever used any of the four carpet products at all, through any path, correct or broken. Nothing to flag for review.
- **Carpet Tab follow-up bugs: "Product Selection Doesn't Proceed" and "Persistent Summary Panel Doesn't Reflect Added Carpet Line" — one real, confirmed root cause found and fixed; the other's literal scenario could not be reproduced.** Two briefs landed within the hour of the fix above shipping (`Bolton-Brief-Product-Selection-Stuck.docx`, `Bolton-Brief-Carpet-Summary-Not-Updating.docx`), both naming Berber Point 920/Glued Down specifically. Investigated on a disposable local copy running the exact byte-for-byte production frontend (`diff` against the live deployed `quote-builder.js`, zero output) — the literal reported path (Carpet tab → Glued Down → Berber Point 920 → Add Line, including via the drill-down's known async-race branch from yesterday) was driven live, repeatedly, in a real browser and worked correctly every time: product selection fired the preview, Add Line saved, and the persistent summary panel correctly showed "✓ Carpet" with the right running total. No console error, no stale cache (Cloudflare's `s-maxage=300` window had long since expired by the time these briefs were filed, ruling that out too).
  - **Real bug found instead, on the same feature, one type over: NEXBAC 920 Tile.** `add_flooring_line()`/`edit_flooring_line()` (main.py) — the existing, unmodified Vinyl box endpoint that Tile deliberately reuses (by design, per yesterday's build) — never set `carpet_category` on the line they create, because that parameter only ever existed on `add_carpet_line()`/`edit_carpet_line()` (the other three types' own endpoint). Reproduced live: adding a real NEXBAC 920 Tile line from the Carpet tab calculated and saved correctly (R7,860.00, right product), but the persistent summary panel bucketed it under "✓ Flooring" instead of "✓ Carpet" (`lineSummaryBucket()` keys off `carpet_category` being truthy) — Carpet stayed stuck on "→ Carpet — in progress" while a real Carpet-tab line sat invisibly inside the wrong bucket. A second, worse consequence found in the same spot: `editQuoteLine()` checks `line.carpet_category` FIRST to decide whether to open the Carpet tab or Vinyl's `#fjMain` panel (yesterday's own edit-in-place fix) — since a Tile line's `carpet_category` was never set, clicking Edit on one incorrectly opened the VINYL panel instead, and since Tile products are deliberately excluded from Vinyl's own dropdown (the pricing-bug fix from yesterday), `prefillFlooringEdit()` silently landed on Vinyl's default range/colour with a completely unrelated product's numbers pre-filled — a real, silent wrong-data bug with no error and no alert, since the original product record still genuinely exists (just filtered out of the dropdown it was being searched for in).
  - **Fixed at the single source of truth**, not by threading a new parameter through every caller: `product.flooring_category` is already the real, authoritative tag — both endpoints now set `carpet_category = product.flooring_category` whenever it's one of the four Carpet Calculators categories (`CARPET_ONLY_CATEGORIES`, a new backend-side constant mirroring the existing frontend one), tile included, regardless of which screen or future entry point creates/edits the line.
  - **Real regression this fix would have introduced, caught and fixed in the same pass:** giving Tile lines a real `carpet_category` for the first time meant they now took the SAME "LM (m²) — type" detail-string branch as the other three types in both the Quote Lines table (quote-builder.js) and the client-facing printed/PDF document (shared.js) — Tile has no `quantity_lm` at all (genuinely m²-only, the Vinyl box shape), so it would have started printing a broken "null LM (10 m²) — NEXBAC 920 Tile" the moment this fix shipped. Caught live in the same test pass, not assumed away: both files gained a `carpet_category === 'carpet_tile'` branch ahead of the general carpet-type one, showing plain "10 m² — NEXBAC 920 Tile" (internal) / "10 m²" (client-facing) instead.
  - **Verified end-to-end on the disposable backend, real numbers, not a success message:** added a fresh NEXBAC 920 Tile line (12 m², R9,142.50) — summary panel correctly showed "✓ Carpet R7,950.00" line-total-only (matches the line's own ex-VAT figure, consistent with every other bucket) instead of "✓ Flooring"; Quote Lines table showed "12 m² — NEXBAC 920 Tile", no "null"; clicking Edit on that same line now correctly opened the Carpet tab with NEXBAC 920 Tile + Berber Point 920 NEXBAC + 10m² pre-filled, not Vinyl's panel. Re-added a genuine Vinyl floor job on the same quote afterward to confirm zero regression — still buckets under "✓ Flooring" correctly, Vinyl's own dropdown still shows no carpet products. **Real production exposure check, exhaustive not sampled:** checked all 11 real quotes for `product_id == 190` (Berber Point 920 NEXBAC) or any `flooring` line named "NEXBAC" with a null `carpet_category` — zero matches, no backfill needed.
  - **Honest gap: the literal "stuck on the Carpet tab's own product dropdown" scenario named in the first brief was never reproduced**, despite deliberately driving that exact path (fresh add, drill-down preselect, and the already-open-quote async-race path from yesterday) multiple times against the real, unmodified production code. The Tile `carpet_category` gap above is a real, confirmed, connected bug on the same feature, in the same testing window, closely matching the second brief's literal description — flagged as the most likely real explanation, not asserted as certain. If either symptom recurs specifically on a non-Tile type (Berber Point 920 itself, Influence, Amble), it needs a fresh investigation — this fix doesn't touch those three types' own code path at all.
- **Owner-Only Calculation Breakdown Toggle — built, verified, deployed same day** (`Bolton-Brief-Owner-Breakdown-Toggle.docx`). Real motivation stated plainly in the brief: clients sometimes stand and watch a quote being built, and the redesigned calculators' default view must never reveal cost breakdown in that moment — but Burgert still wants to see the full working, on demand, to sanity-check the math and stand behind a number if ever questioned.
  - **§0 exception, applied first, since it inverts a pre-existing rule:** margin % is explicitly NOT part of the gated breakdown — "a percentage alone doesn't reveal actual cost figures... safe to leave visible... useful as everyone's own quick sanity check." `strip_sensitive_fields()` (main.py) used to strip `margin_pct` for Sales; that's removed — every role, including Sales, now receives it, confirmed live via a real raw `fetch()` against `/quotes/{id}` while previewing as Sales (not just checking the UI): `margin_pct` present, every real cost field absent. Every margin-warning message (Blinds/Trim/Stairwell/Carpet/Flooring's own add+edit+preview endpoints, 8 call sites) had the same inconsistency once margin_pct itself stopped being hidden — a warning that just restates the same figure in prose, still Sales-hidden, would've been an arbitrary leftover; the `role != UserRole.sales` gate on all 8 is removed too.
  - **§1, "Owner-only" tightened from "not Sales" to "not Owner"** — real, deliberate policy change: `strip_sensitive_fields()`'s gate flipped from `role == UserRole.sales` to `role != UserRole.owner`, so Admin (Madri) now loses the same cost/breakdown fields Sales always has (previously Admin saw everything Owner did). Same flip applied to `get_quote()`'s own "at a glance" job margin check — split into `overall_margin_pct` (every role, unconditional now) and `overall_cost_ex_vat` (Owner-only, same list).
  - **§3, "build once, not per-category":** a genuinely generic renderer (`ownerBreakdownHtml()`, shared.js) reads whichever of a fixed set of field names (`material_cost_total`, `unit_cost`, `gripper_cost_total`, `subtotal`, `markup_multiplier`, `labour_cost_total`, etc.) are actually present on whatever calc dict a category's preview returns, and renders a labelled line for each, plus a computed "Line, incl VAT" — a category that returns none of them (Misc) renders nothing extra; a brand-new future category only needs to return a field already in this lookup (or a new one added to it) for its own breakdown to appear for free. Verified working on Carpet (its own real fields: material/gripper/underfelt/cutting-fee/glue cost, subtotal, markup, labour) AND, as a bonus spot-check beyond the brief's own named scope, on Trim (`unit_cost` only) — confirming the "any category added later" claim isn't just asserted.
  - **`calculate_carpet_line()` gained three new return fields** (`material_cost_total`, `subtotal`, `markup_multiplier`) — didn't exist on the returned dict before, so purely additive; needed so the Owner's breakdown has real "material cost / subtotal before markup / markup applied" figures for Carpet the same way Vinyl's own `fjCalc()` already has them client-side. Added to `strip_sensitive_fields()`'s list immediately, same call.
  - **Vinyl's own `fjCalc()` Result panel — the one genuinely bespoke piece, deliberately NOT rebuilt to use the generic renderer:** it's entirely client-side already (no calc-dict shape to iterate), and IS "the same level of detail" the brief explicitly asks the breakdown to match. Two new always-visible lines (`fj_out_summary_price`/`fj_out_summary_margin`, reading `total_ex`/`gp_pct` — the exact same variables the rest of the function already computes) sit above everything else; "Job"/"What needs to be ordered" (floor size, m² needed, boxes, bags) stay visible below them, unconditionally, since they're physical/order quantities needed to actually build the job, not financial calculation detail — the rest of the card (box price, subtotal, markup, labour, screed breakdown, per-m² check) plus the entire separate "Margin / GP Check" card are wrapped in two new containers (`#fjBreakdownSection`, `#fjBreakdownGpCard`) toggled by `applyRoleVisibility()`, same mechanism as everything else.
  - **One global toggle, not one per category/line** (`#ownerBreakdownToggle`, owner-breakdown-toggle-wrap class) — sits once, at the top of the Add Line Item card, visible only when `currentRole()==='owner'`; per the brief's own "build this once" framing, a single page-level control was simpler and more consistent than a scattered per-line button, and still satisfies "on any calculated line, in-progress or already added, in any category" since it governs every preview box and the saved Quote Lines table at once. State (`ownerBreakdownVisible`) lives in `sessionStorage`, not `localStorage` — the deliberate middle ground between the brief's two stated requirements ("must never be on by default for a new session" vs. "should not need to be re-enabled every session necessarily"): survives a reload within the same browser session, always gone on a genuinely new one (new tab/window, browser restart), confirmed live both ways (cleared it, reloaded, defaulted to off).
  - **Cost vs. Margin split out of the shared `cost-col` CSS class** that used to hide both together for Sales — Margin's own table header/columns lost that class entirely (never hidden by role or toggle now); Cost kept it, now toggled by `applyRoleVisibility()` off `showBreakdown` (Owner + toggle on) instead of `!isSales`.
  - **`labour_charged_total` (and the pre-existing `*_sell_total` fields) deliberately left un-gated**, a discretionary interpretation call, not an oversight: the brief's §1 field list names "labour" as part of the gated breakdown, but this codebase already draws a real, pre-existing line between *charged* fields (what the client pays — never gated, same class as `unit_price`/`line_total`) and *cost* fields (what the job actually costs Bolton — always gated). `labour_cost_total` (the cost side) was already gated and stays gated; `labour_charged_total` (the charged side) stays visible, consistent with `glue_sell_total`/`vinyl_sell_total`/`nosing_sell_total` never having been gated either. The frontend's OWN display of it in the Quote Lines table is still toggle-gated for Owner's benefit (decluttering), just not stripped server-side for Sales/Admin — no regression, since it was never stripped for them before today either.
  - **Verified end-to-end on a disposable local backend, byte-identical to production, all four of the brief's own testing requirements, not a success message:** Vinyl (Aspen Herringbone Range 2mm, 50m²) — default view showed only "Price (ex VAT) R21,226.48 / Margin: 40.4%"; toggle on revealed the full walkthrough (box total, subtotal, markup ×1.30, labour, GP Rand R8,574.19, GP% 40.4% — reconciling exactly with the default-view figures) plus the separate GP Check card; toggle off hid all of it again instantly, live, no reload. Carpet (Berber Point 920, Glued Down, 20 LM) — same pattern, breakdown showed material cost R17,388.00 + gripper R0 + underfelt R0 + cutting fee R350 + glue R1,478.40 = subtotal R19,216.40 → ×1.30 markup + R3,780 labour charged = R28,761.32, matching the default-view price exactly. Switched to previewing as Sales (Owner Preview Mode, no separate login needed) and fetched `/quotes/{id}` directly: `margin_pct` present, every cost field (`unit_cost`, `glue_cost_total`, `cutting_fee_total`, `labour_cost_total`, `overall_cost_ex_vat`) absent from the raw JSON — confirmed server-side, not just UI-hidden. The breakdown toggle checkbox itself was completely absent from the DOM while previewing as Sales, regardless of the Owner's own toggle state moments earlier. Cleared `sessionStorage` and reloaded: toggle defaulted back to off.
- **Bring Vinyl/Screed to Carpet's "Add Line" Flow — investigated first, confirmed the real gap, fixed same day** (`Bolton-Brief-Vinyl-Add-Line-Flow.docx`). Burgert's own framing: Carpet's flow (pick type → minimal fields → see price → Add Line → instantly in the summary) tested well; bring Vinyl/Screed in line with it, investigating current state first rather than assuming the redesign had already touched this.
  - **Investigation confirmed the combined submission was still fully intact**, unlike almost everything else this brief's own "confirm before assuming" instruction turned out to matter for: Blinds/Skirting/Trim/Stairwell/Misc all already share one generic card (`#genericLineCard`) and `addLine()`, which only ever submits the ONE category currently selected — already exactly Carpet's model, confirmed by reading `addLine()`'s own branch structure, not assumed from memory. Vinyl/Screed were the one real outlier: `selectLineCategoryTab('flooring')` set BOTH `fj_include_vinyl` AND `fj_include_screed` checked, and the single "+ Add Floor Job to Quote" button (`addFloorJob()`) would submit whichever of them were checked — meaning the Flooring tab's own DEFAULT state was "add both together," a genuinely different, older pattern than every other category on the page.
  - **Fixed with a small, low-risk change, not a rebuild:** `fj_include_vinyl`/`fj_include_screed` — Screed's own tab already set them mutually exclusive; the Flooring tab's branch in `selectLineCategoryTab()` now does too. They're hidden inputs now (no longer user-facing checkboxes, index.html) — pure internal state — but every downstream reader (`fjOnIncludeChange()`, `fjCalc()`, `addFloorJob()`, `prefillFlooringEdit()` — already mutually exclusive on the edit path) needed zero changes, since what they read is still exactly "which one is active," just set by tab selection instead of a checkbox click. Zero calculation-logic touched, per the brief's own explicit non-goal — `fjCalc()`'s actual formula code is untouched; only how `includeVinyl`/`includeScreed` get set changed.
  - **Card title and Add Line button now name whichever one is actually active** ("Vinyl" / "+ Add Vinyl Line to Quote" vs. "Screed" / "+ Add Screed Line to Quote", `fjOnIncludeChange()`) — the "Floor Job" combined concept doesn't exist anywhere in the UI anymore. The floor-size field itself was deliberately left as ONE shared input rather than split into two — turned out to be a real, welcome side benefit rather than a compromise: switching from Flooring to Screed for the same room keeps the m² already typed, so the natural common case (vinyl + screed, same room) still only needs the number entered once, while each is still added as its own genuinely separate step.
  - **Verified end-to-end on a disposable backend, real numbers, not a success message:** Aspen Herringbone Range 2mm, 50m² — added as a Vinyl line first (R14,726.48), persistent summary panel showed "✓ Flooring R14,726.48" immediately, Total incl. VAT updated to R16,935.45 before anything else was touched; switched to the Screed tab (same 50m² already carried over), added ITE F10 Smooth as its own separate step (R6,500.00) — summary immediately added "✓ Screed R6,500.00" alongside the untouched Flooring row, Total ex VAT correctly became R21,226.48 (14,726.48 + 6,500.00 — the exact combined figure this same product pairing produced when tested as one submission earlier this same day, confirming the split changed nothing about the math, only when each line lands). Quote Lines table showed two genuinely separate rows. Editing each line straight after the other (clicking Edit on the Screed line immediately after the Vinyl line, no Cancel in between — the real way a user would jump between two existing lines) correctly re-opened the right tab with the right title/button label and prefilled fields both directions, each checked as its own separate, properly-awaited step. (An earlier, rushed console test that fired both `editQuoteLine()` calls back-to-back with no pause between them did show a stale label once — traced to the test itself racing `toggleLineFields()`'s own async chain, not the shipped code; re-run properly-paced, it was correct every time.)
- **Move Product Constants Off the Quoting Screen — investigated first, found the back office already existed, shipped same day** (`Bolton-Brief-Move-Constants-Backoffice.docx`, explicitly connected to the breakdown toggle: "reuse that mechanism, don't build a second one"). Problem as framed: Wastage %, m² per box, Price per m² (ex VAT), Trade discount %, and Glue rate showed as editable fields on every single Vinyl quote — product properties, not quote decisions.
  - **Investigation resolved the brief's own open question ("confirm which are per-product vs. business-wide, don't assume they're all edited in one place") — all five turned out to be per-product, and all five already had a real back-office home.** `wastage_pct`, `trade_discount_pct`, and `m2_per_pack` are already plain `FlooringProduct` columns; `base_cost_ex_vat` ("Price per m²") is already system-calculated from `price_per_box_ex_vat`/`m2_per_pack` and already read-only in the Supplier Console (Supplier Console Field Sequence Redesign, confirmed Aug 2026 — predates this session); `glue_rate_per_m2` is already an optional per-product override, same pre-fill-not-mandate pattern `labour_rate_per_m2` already uses. `FLOORING_CONSOLE_FIELDS` (index.html) already lists all five as editable Supplier Console fields. Net finding: there was no new back-office surface to build at all — the actual gap was entirely on the quoting screen, which kept exposing per-product data as if it were a per-quote input.
  - **Fixed by making the five fields `readonly` and moving them behind the SAME Owner-only breakdown toggle shipped earlier today** (`#vinylConstantsBreakdown`, new container inside `#vinylCard`, toggled by the same `applyRoleVisibility()`/`showBreakdown` logic as `#fjBreakdownSection`/`#fjBreakdownGpCard`/Carpet's own breakdown — a third reuse of the one mechanism, not a second one built for this). Still auto-filled from the selected product by the existing `onVinylProductChange()` (zero changes there), still exactly what `fjCalc()` reads by the same element ids (zero calculation-logic changed, confirmed by the calc result being byte-identical before/after for the same inputs) — only how a value gets INTO these fields changed (product record only, never a hand-typed override sitting invisibly on one quote).
  - **Markup % and Labour rate deliberately left untouched** — genuinely the brief's own explicit carve-out, not an inconsistency: both are ALSO pre-filled from a per-product default (`sell_markup_multiplier`/`labour_rate_per_m2`) the exact same way the other five are, but the brief is explicit that these two "may genuinely need adjusting job to job" and belong on the quoting screen — a real, deliberate business distinction (some product defaults are starting points a specific quote can reasonably deviate from; others, per this brief, shouldn't be quietly overridden per job at all).
  - **Interpretation call, not obviously wrong but flagged for Burgert:** the testing requirement's literal "only Range, Colour, Markup %, Labour rate, Labour source visible by default" was read as describing the five relocated constants specifically, not as a new instruction to also hide "Material only," Transport & Courier, or Extra Rooms/Floor Prep — none of which were named anywhere in the brief's own "the problem" framing, and all of which are genuine per-quote client-facing choices (not product constants) that Sales/Admin still need to set while building a quote. Hiding those behind an Owner-only toggle would have been a real functional regression for non-Owner roles, not a decluttering — left visible as before.
  - **Verified end-to-end on a disposable backend byte-identical to production, all four of the brief's own testing requirements, not a success message:** default view for Aspen Herringbone Range 2mm confirmed shorter — Range/Colour/Markup %/Labour rate/Labour source/Material-only visible, none of the five constants present anywhere in the rendered page. Selected a real colour, 50m²: **R14,726.48 / 34.8% margin — byte-identical to every earlier test of this exact same product/size combination today**, confirming the calculation is genuinely unchanged. Switched the breakdown toggle on: all five fields appeared, correctly read-only (`readOnly === true`), correctly sourced from the real product record (Wastage 8.0%, m²/box 1.4161, Price 144.76, Discount 0.0%, Glue 17.05). **Edited the constant directly through the real update endpoint the Supplier Console itself calls** (`PUT /price-book/flooring/{id}`, wastage 8%→15%) and re-selected the same product on a fresh load: the field correctly updated to 15.0, m² needed correctly recalculated from 54.00 to 57.50 (50 × 1.15), price correctly moved from R14,726.48 to R15,259.46 — confirming a real back-office edit genuinely reaches a future quote's calculation, not assumed. Reverted the test edit back to 8% afterward. Confirmed via Owner Preview Mode that both the toggle control itself and the constants container stay completely hidden while previewing as Sales, even with the Owner's own toggle left on moments earlier.
- **Auto-Add Screed for Vinyl, with an Easy Delete — scoped strictly to Vinyl/Flooring, shipped and verified same day** (`Bolton-Brief-Auto-Add-Screed.docx`, filed right after the Vinyl/Screed split shipped this same day — explicit non-goal: no change to Carpet's own optional/manual screed decision at all). Framing: "very few flooring jobs go in without needing screed" — adding a Vinyl line should default-include its own Screed line, with an effortless Delete for the rare job that genuinely doesn't need one.
  - **Point 1, the "commit boundary" between the main Screed calculator and Extra Rooms/Floor Prep, turned out to be a real, structural page-order problem, not just a subtle visual one.** Investigated the actual current DOM first: the shared "Add Line" button (today's own earlier Vinyl/Screed-split work) sat at the very bottom of the whole `#fjMain` column — past the Extra Rooms sub-section's own separate "+ Add to Quote" button AND past the entire Transport & Courier card. The main Screed calculator's own commit action was physically separated from its own input fields by an unrelated section with its own competing "Add" button in between — genuinely ambiguous which button did what, exactly as reported. Fixed by giving `vinylCard` and `screedCard` each their own dedicated, statically-labelled "Add Line" button immediately after their own fields (no more dynamic relabelling needed, since each card only shows for its own tab) — Screed's own button now sits directly before the Extra Rooms sub-section's dashed divider, never after it.
  - **Point 2, the actual automation: adding a fresh Vinyl line now also adds its own companion Screed line**, using the Screed section's own already-populated defaults (Smooth job type — the dropdown's own first/default option; area = the same floor size just entered for Vinyl) — genuinely the exact same code path (`addFloorJob()`'s existing `includeScreed` block, factored into a new shared `autoAddScreedLine()`) a manual Screed add already used, just fired automatically right after the Vinyl POST succeeds. Only on a fresh add, never on an edit — `editingLineId` already short-circuits and returns before this code runs, so adjusting an existing Vinyl line's floor size never silently spawns an unrelated new Screed line. One deliberate behavioural difference from the manual path: if no screed product exists in the price book at all, the manual "Add Screed Line" button still alerts (a real gap Burgert would want to know about when he deliberately asked for screed); the auto-add silently skips instead — surfacing an alert about a missing SCREED product the moment someone adds a VINYL line would be a confusing non-sequitur, not a helpful prompt.
  - **Point 3, the Delete button, needed no new work at all** — an auto-added Screed line is a completely ordinary, real `QuoteLineItem` saved through the exact same endpoint a manual add already uses, so it already gets the same Edit/Delete buttons every other line in the Quote Lines table has, with zero special-casing. Confirmed live rather than assumed.
  - **Verified end-to-end on a disposable backend byte-identical to production, all four of the brief's own testing requirements, not a success message:** added a Vinyl line (Aspen Herringbone Range 2mm, 50m²) via the single "+ Add Vinyl Line to Quote" click — the summary immediately showed BOTH "✓ Flooring R14,726.48" AND "✓ Screed R6,500.00" from that one click, Total ex VAT correctly R21,226.48. Deleted the auto-added Screed line via its ordinary Delete button — summary correctly dropped back to "✓ Flooring" only, Total ex VAT correctly recalculated to R14,726.48. Confirmed the new commit-boundary ordering directly in the rendered page (Screed fields → "+ Add Screed Line to Quote" → Extra Rooms' own separate fields/button), then exercised Extra Rooms independently (5m² Guest Bathroom, 10mm) and confirmed it created its own separate `category: "misc"` line, untouched by anything happening on the main Screed line. Added a real Carpet line (Berber Point 920, Needlepunch, 10 LM) afterward and confirmed via a direct fetch of the quote's real lines that zero screed line was auto-created alongside it — Carpet's own behaviour is completely unaffected, confirmed rather than assumed given this function shares no code with Carpet's own add path at all.
- **Transport & Courier Relocation + Discount Sizing — investigated, one real nuance surfaced and flagged, shipped and verified same day** (`Bolton-Brief-Transport-Relocation-Discount-Sizing.docx`). Problem as framed: Transport & Courier is a whole-quote setting that sat inside the Flooring-only `#fjMain` area, cluttering category-specific screens; the Discount % input stretched full-width for no reason.
  - **Investigation surfaced a real distinction the brief's own "move it as one unit" framing glossed over, not something to silently paper over.** Transport levy is genuinely whole-quote (one `Quote.transport_levy` value, one input) — a clean, risk-free relocation. Courier/delivery fee is NOT whole-quote — per the brief that originally built it, it's genuinely per-line ("independently overridable per room, submitted as `apply_delivery_fee` on THIS line only"), read by `addFloorJob()` at the exact moment a Vinyl line is added. Moving both together, as the brief's own testing requirement explicitly expects, still works correctly — every reader finds these fields by element id regardless of where they physically sit in the DOM, confirmed live, not assumed — but it does mean toggling Courier for a specific room now means stepping down to the whole-quote review area first, rather than setting it right next to that room's own fields. Flagged in the Build Log rather than fixed silently or left unmentioned, since it's a real, if minor, workflow trade-off of following the brief's literal instruction.
  - **Fixed by relocating the whole `#transportCourierCard` out of `#fjMain` (Flooring/Screed only) to sit as its own standalone card in the whole-quote review area, right before Discount and Quote Lines** — same pattern `quoteDiscountCard` already established (hidden until a quote is open, shown/hidden at the same 4 call sites that already manage `linesCard`/`quoteDiscountCard`'s visibility). Same `#fj_transport_amount`/`#fj_courier_toggle` element ids and handlers throughout — zero JS logic changed, only where the markup sits.
  - **Discount % input shrunk** (`style="max-width:100px;"`, same technique the Transport amount field already used for its own compact sizing) — no other change to the field.
  - **Verified end-to-end on a disposable backend byte-identical to production, all three of the brief's own testing requirements, not a success message:** confirmed `#transportCourierCard` stays `display: block` (visible) while switching between the Flooring and Carpet tabs — genuinely no longer tied to any one category's own screen, always in the same place in the review area. Set a real R500 Transport levy and added a Vinyl line: `quote.transport_levy` correctly saved as 500, `total_ex_vat` correctly included it. Found a real product with a nonzero `delivery_fee_per_m2` (Aspen Project Range 2.5mm, R15/m²), toggled Courier on from its new location, added that line: `delivery_fee_total` came back as R750 (50m² × R15) on the real saved line — confirmed the per-line Courier mechanism genuinely still works from its relocated home, not just theorized from "ids don't care about DOM position." Confirmed the Discount input's real rendered width dropped from full-card-width to 100px, then set it to 10% and confirmed `discount_pct: 0.1` saved and the total recalculated correctly. Confirmed `resetQuoteBuilderUI()` (new quote) correctly hides the relocated card, same as before.
- **New Quote Starting Screen: Price Check Fix, Clarity, and Rep Attribution — two briefs reconciled into one coherent design before building either, per both briefs' own explicit instruction** (`Bolton-Brief-New-Quote-Flow-Clarity.docx`, which itself revises `Bolton-Brief-Staff-Account-Defaults.docx`'s own assumption about Burgert's own account — read and reconciled together, not built as two separate, conflicting behaviours).
  - **§1, Price Check reset bug — confirmed real, root-caused precisely, not patched by feel.** `clearStaleQuoteResidue()` (the function Price Check calls, deliberately lighter than the fuller `resetQuoteBuilderUI()` so it doesn't also wipe `q_client`/disable Start Quote mid-flow) already reset plenty of state — transport levy, discount %, Floor Prep scratch fields — but never touched the actual "Add Line" calculator inputs themselves. Confirmed via code reading, not guessed: **Floor size had NO reset path anywhere at all**, not even indirectly (unlike Range/Colour, which get refilled as a side effect of `toggleLineFields()`'s own product-dropdown-repopulation cascade right after this function returns). **Glue rate had a real conditional gap**: `onVinylProductChange()` only overwrites it when the newly-selected product has its own `glue_rate_per_m2` override set, so a product with none would silently inherit whatever rate the PREVIOUS calculation left behind. Fixed by blanking every raw "in-progress" input across every category (Vinyl/Screed/Carpet/Blinds/Trim/Skirting/Stairwell/Misc) in one place, once — the same onChange cascades already firing right after this function returns then correctly refill anything with a real per-product/business-setting default. **Real regression caught in the same pass, before it shipped:** glue rate specifically needed its own real fallback (17.05, the input's own original static HTML default) rather than blanking to `''` like everything else — blank reads as a real 0 glue rate to `fjCalc()`, silently pricing a no-override product as if it needed no adhesive at all, confirmed live before being corrected.
  - **§2, New Quote screen restructured into two visually distinct groups**, not a flat row of one search field + three same-weight buttons. Group 1: "Find an existing client" search + Start Quote, genuinely live-disabled (`updateStartQuoteButtonState()`) until a real match is actually selected — the rule ("existing clients only, search and select") used to only ever surface as a reactive alert after a wrong click; now the button's own state teaches it up front. Group 2, below a plain "— or —" divider: New Client and Price Check as their own clearly-labelled, one-line-captioned pair ("Not in the system yet" / "Quick price, no client needed") — genuinely different goals from the search field above, never confusable with it. "Already Open" confirmed (not assumed) to be the exact same physical button as Start Quote, just relabelled+disabled once a quote exists — not a fourth distinct action despite reading like one in the old flat layout.
  - **§3, reconciled with Staff Account Defaults into one coherent behaviour for all three accounts, not two different ones.** Sales Owner is never shown as a picker for anyone now — there's genuinely nothing to choose: Ryno/Madri are always themselves when logged in as themselves; Burgert's own quotes default to himself the same way (the earlier Staff Account Defaults brief's own assumption that Burgert's account should keep a visible, changeable picker is the thing New Quote Flow Clarity's §3 explicitly revises). The one real exception — Burgert doing a job as Ryno/Madri on their behalf — is routed through the EXISTING "Viewing As" mechanism instead of a second always-visible control, per that brief's own explicit proposal. Branch collapses to one small toggle for whichever branch is the LESS common one for whichever account is currently effective: Ryno/Madri default to Gansbaai (toggle reaches Hermanus, per Staff Account Defaults' own original spec); Burgert defaults to Hermanus (toggle reaches Gansbaai) — the same symmetric shape applied to his own account too, a deliberate reconciliation choice (flagged below, not asserted as the only possible reading) rather than leaving him with no way at all to quote for Gansbaai under his own name outside of switching Viewing As.
  - **Real, pre-existing gap found and fixed to make Viewing As actually work for this**: `defaultSalesOwnerForCurrentUser()`/`defaultBranchForCurrentUser()` (shared.js) read `currentUser?.username` directly — completely blind to Owner Preview Mode. The whole premise of routing rep-attribution through Viewing As depends on these two functions resolving to the PREVIEWED account, not the real one — fixed via a new shared `effectiveUsernameForQuoting()` (resolves `previewRole` first, falls back to the real user), with the two default functions now built on top of it. Confirmed live, not assumed: starting a quote while Viewing As Sales produced a real saved quote with `sales_owner: "ryno"`, `branch: "gansbaai"` — not silently left as Burgert/Hermanus.
  - **§4, dual attribution ("assisted by" note) — investigated and a recommendation reached, deliberately NOT built**, per the brief's own explicit "propose whether this is worth doing... before building it, this may be more complexity than the case warrants." Real, relevant infrastructure already exists: `get_current_username()` (main.py) is deliberately the REAL authenticated username for every `AuditLog` entry, by design, "never affected by Owner Preview Mode" (its own docstring) — confirmed live: adding a line to a Ryno-attributed quote while Viewing As Sales would correctly log the real actor, wherever that category's own add function actually calls the audit logger. Two real, pre-existing gaps found while checking this, both unrelated to Viewing As and worth knowing about regardless of this decision: quote CREATION itself has no audit-log entry at all (confirmed — an empty result for a freshly-created quote's own audit history), and Misc line additions specifically have no audit-log call either (unlike Flooring/Carpet/Stairwell, which do). **Recommendation: don't build a new "assisted by" field.** The cheaper, lower-risk, higher-value fix — if Burgert wants real confidence in who-did-what while using Viewing As — is closing those two existing coverage gaps (log quote creation the same way line-adds already are; add the same audit call to Misc's own add function) rather than adding a new bespoke attribution concept on top of a foundation that doesn't fully work yet even for its existing, narrower job.
  - **Verified end-to-end on a disposable backend byte-identical to production, all three of the brief's own testing requirements, not a success message:** dirtied Vinyl fields on a real quote (floor size 123.45, glue rate 99.9, trade discount 55%), clicked Price Check, confirmed floor size came back genuinely blank, glue rate came back 17.05 (not stale, not zero), and wastage/markup/labour rate all came back as fresh real defaults from whichever product the reset dropdown cascade landed on — not leftovers from the dirtied state. Confirmed Start Quote stays disabled until `selectQClient()` actually fires, re-disables the instant the client field is hand-edited afterward, and a real quote created this way saved with the correct hidden `sales_owner`/`branch` values. Switched to Viewing As Sales: New Quote screen correctly showed `q_owner: "ryno"`, `q_branch: "gansbaai"`, toggle label correctly flipped to "Quoting for Hermanus?" — created a real quote in this state and confirmed it saved with `sales_owner: "ryno"`, `branch: "gansbaai"`, not Burgert's own defaults.
- **Margin Becomes Owner-Only; Price Gets a Colour Signal — real thresholds proposed from real data, confirmed with Burgert in two rounds, built and verified same day** (`Bolton-Brief-Margin-Owner-Only-Price-Colour.docx`, explicit revision of the Owner-Only Calculation Breakdown Toggle brief's own earlier §0 exception: "margin % stays visible to everyone by default" — no longer correct, read as the current instruction). Framing: Ryno/Madri should never see the real margin % anywhere, not even the "safe, percentage-only" carve-out from a few hours earlier — the price itself should carry a colour signal instead.
  - **Real Vinyl/Carpet quote data pulled first, before proposing any cutoff**, per the brief's own explicit "these are real business numbers, not something to invent": read the user's own already-authenticated production browser session (read-only, zero credentials touched) — 20 real Vinyl lines, 33.0%–54.8%, median 39.6%; zero real Carpet lines yet; and found `flooring_margin_warn_threshold = 0.30` already a live, endorsed `BusinessSettings` number used elsewhere in the app, a natural anchor rather than a fresh guess.
  - **First confirmation round materially revised the initial red/amber/green proposal, not just its numbers.** Burgert's own words: "Green should always be acceptable, different shades of green though. Amber and red is very negative and alarming... between 30 and 40 is acceptable, but 40% and up is more than what is needed or aimed... make the cutoff to an angry colour the moment it goes past the 30% threshold into the lower numbers." Landed on two families, not three bands: red only below the existing 30% floor; two shades of green at/above it, split at a new, confirmed `margin_band_bright_threshold = 0.40` (the one genuinely new number this brief needed, confirmed against the real 33–55% Vinyl data before shipping). Discrete bands confirmed over a gradient, Burgert's own choice from an explicit either/or — reasoning on both sides: a gradient risks Ryno/Madri reverse-engineering the real percentage from subtle shade differences, defeating the entire point.
  - **Same answer surfaced a genuinely new, unscoped requirement — accountability for a discount that pushes a line below the floor** ("the system won't allow going under there anyway except with a discount, but there should be accountability for those types of decisions") — pinned down with a second, tightly-scoped question rather than guessed at either extreme (a hard block vs. silent logging): confirmed as a **soft prompt** (the line/quote saves immediately either way; a reason is requested "at that moment," never blocking), triggered **both per-line and whole-quote** (a real quote can have every individual line clear 30% while the combined total — transport levy, quote-level discount, etc. — still doesn't).
  - **`margin_band_for(margin_pct, settings)`, one new backend helper, computed inside the already-shared `strip_sensitive_fields()`** (main.py) rather than threading a new parameter through every one of the 5+ `calculate_*_line()` functions — reads the real `margin_pct` every category's calc dict already carries, returns `"red" | "green" | "green_bright" | None`, attached to every line/preview dict as `margin_band` BEFORE `margin_pct` itself gets stripped for non-Owner roles. `margin_pct` moved back into `strip_sensitive_fields()`'s Owner-only strip list, reversing this same morning's earlier call. `settings` threaded through all 15 real call sites of `strip_sensitive_fields()` across every add/edit/preview/get endpoint (Flooring, Blinds, Trim/Skirting, Carpet, Stairwell, Misc, `preview_line()`, `get_quote()`) — 6 already had a `settings` variable in scope, 9 needed a new `get_settings(session, tenant_id)` call added, found and tracked via a backward-scan script rather than guessed line by line.
  - **`overall_margin_pct` (get_quote()) reverted to Owner-only**, undoing this same morning's own "safe to leave visible" call now that margin_pct itself is Owner-only again — `overall_margin_band` (every role, always) is the like-for-like colour replacement. The 8 real call sites where margin-warning TEXT gating had been removed this same morning (it embeds the real percentage in prose, e.g. "...is 32.1%, below...") had that gating restored too (`role == UserRole.owner`), closing the exact side-channel this brief's own "not just hidden in the UI" requirement warns about.
  - **Low Margin Accountability — a new `low_margin_reason`/`low_margin_reason_by`/`low_margin_reason_at` trio on both `QuoteLineItem` and `Quote`**, deliberately separate from the existing Manual Override trio (same "captured, by whom, when" shape, but a price override and a low-margin justification are two independent real-world events that can both happen on the same line). Two new generic endpoints (`PUT .../lines/{line_id}/low-margin-reason`, `PUT .../low-margin-reason`) rather than threading a reason param through 10 different add/edit signatures — QuoteLineItem/Quote are the one shared model underneath every category regardless. A fresh recalculation clears any existing reason (`edit_*_line()`, all 6 categories) — a new number is a new pricing decision, so a line that lands below the floor again (even at the same number) has to be explained again rather than silently inheriting a stale justification. `needs_low_margin_reason`/`quote_needs_low_margin_reason` computed and sent to every role (band is red AND no reason on file yet) so Sales/Admin — usually the ones actually applying the discount — see the same prompt the Owner would, without ever needing to see the real number to know one is owed.
  - **Real bug caught before it shipped, during verification, not after:** the new low-margin-reason endpoints' own `AuditLog` insert passed `old_value=None` for a brand-new reason — `AuditLog.old_value` is a non-nullable `str` column, so the very first real save threw a live `IntegrityError`, caught immediately by actually calling the endpoint against the disposable backend, not by `py_compile`. Fixed with the same `"(none)"` placeholder `_log_quote_line_audit()` already uses for a genuinely-first value.
  - **Frontend: one shared `marginBandColor(band)` helper** (shared.js, next to `ownerBreakdownHtml()`) so every price display in the app reads the three bands the same way — red reuses `var(--coral)` (the app's existing "alarming" colour, per Burgert's own "angry colour" framing, no new variable needed); two new CSS custom properties, `--margin-green`/`--margin-green-bright`, for the two "acceptable or better" shades. Applied to the Price cell in the Quote Lines table (every role, Owner included — "no reason to remove a useful visual cue from his own view," his own words) and every category's live-preview box (generic/Blinds-Trim-Stairwell, Carpet).
  - **Flooring's own client-side shadow calc (`fjCalc()`) had the one real, literal leak this brief's point 1 is centrally about** — `fj_out_summary_margin` rendered the real GP% as plain text, unconditionally, for every role, since it's entirely client-side and never passes through `strip_sensitive_fields()` at all. Found while working through every category, not named in the brief itself. Fixed the same way as everywhere else: the row now hides for non-Owner (`fj_out_summary_margin_row`), the price gets the same red/green/green_bright colour computed client-side from the same `gp_pct` against the same two real `BusinessSettings` thresholds every role already receives from the unrestricted `GET /business-settings` (thresholds are business config, never a specific quote's real number), and a plain "below the confirmed margin floor" note (no percentage) replaces the warning for non-Owner when the band is red.
  - **Quote Lines table's Margin column got the `cost-col` class back** (index.html/shared.js `applyRoleVisibility()`), reversing the earlier "split Margin out of cost-col" call now that margin_pct is Owner-only again — the whole column (header + cells) now appears/disappears together with Cost, rather than leaving an empty "Margin" header sitting over hidden cells.
  - **Verified end-to-end on the disposable local backend/frontend, via real, raw network responses, not a visual check or a success message, per all four of the brief's own testing requirements:** fetched a real quote (20 real Vinyl-equivalent lines spanning all three bands) as Ryno and Madri and grepped the raw JSON directly — zero occurrences of `margin_pct`, `overall_margin_pct`, `overall_cost_ex_vat`, or `unit_cost` anywhere in either response, while `margin_band`/`overall_margin_band`/`needs_low_margin_reason` were correctly present on every line. Confirmed the same real quote as Owner correctly received the real numbers alongside the bands. Submitted a real low-margin-reason via the API as Ryno, confirmed it persisted with the correct `by`/`at`, confirmed `needs_low_margin_reason` correctly flipped to `false`, then re-edited the same line unchanged and confirmed the reason correctly cleared and the flag correctly flipped back to `true`. Confirmed the same for the whole-quote endpoint. In a real browser (Claude in Chrome, Owner Preview Mode → Sales, no separate login): Quote Lines table showed the correct colours on real saved lines (green/red, programmatically confirmed via `getComputedStyle`/inline style, not eyeballed), the "⚠️ Explain low margin" link on the red line, "Overall margin: ✓ healthy" with zero percentage while previewing as Sales vs. "Overall margin: 31.4% ✓" for Owner, and the Cost/Margin columns fully absent (not just blank) with the breakdown toggle off. Flooring's own `fjCalc()` confirmed separately, both roles: Owner sees the real %; Sales sees only the colour, correctly flipping to coral with the "reason will be requested" note the moment a discount was pushed past the floor, live, no reload.

### Why (root causes, decisions, rejected alternatives)
- The backend-endpoint approach was Burgert's own explicit choice, offered as a real fork in the road rather than picked unilaterally mid-session — the proposal itself had flagged this as "a real design choice, not just implementation effort" once Flooring's own preview mechanism was actually read and understood to be a shadow calculation, not the shared-formula approach the rest of this project follows everywhere else.
- No `quote_id` in the preview endpoint's URL, unlike every real add/edit endpoint — deliberately, since the computation genuinely doesn't need one: only the price book and Business Settings (both tenant-scoped) feed a preview, and requiring a quote_id would have implied a dependency that doesn't exist.
- Same `strip_sensitive_fields()`/warning-gating discipline applied to the preview response as every real save endpoint, checked explicitly rather than assumed — including one thing `strip_sensitive_fields()` itself doesn't cover: the margin-warning TEXT embeds the real percentage in plain English (`"...is 32.1%, below..."`), which would have leaked exactly the number the strip list exists to hide. Cleared separately for Sales in the preview endpoint.
- **Real bug caught before shipping, not after**: extracting `_compute_stairwell_calc()` left the `@app.post(".../stairwell")` decorator sitting above the wrong function — it would have registered the POST route on the internal helper instead of the real `add_stairwell_line()` endpoint, silently breaking that endpoint while crashing FastAPI's own startup route-registration for the whole app. `py_compile` (syntax only) didn't catch this — actually starting the app against a real database copy did, immediately, the same discipline that's caught several other real bugs across this project's history. Fixed, then re-verified `edit_stairwell_line()` reproduced the exact figure (4601.88) the Phase 1 test had already confirmed for the identical inputs, before trusting the refactor was behaviour-preserving.
- Verified directly against real price-book data (throwaway database copy, never `bolton.db`, never production): blinds preview matched the documented, confirmed ~49.125% margin figure exactly; stairwell preview matched the Phase 1 edit test's figure exactly; Sales role correctly stripped of cost/margin fields; Misc correctly rejected with a clear message rather than a confusing 500 or a silent wrong answer.
- Real usage feedback arriving same-morning (within hours of the whole redesign shipping) is itself a confirmed, positive signal — Burgert was able to actually use category tabs, the summary panel, and Extra Rooms/Floor Prep well enough to find four specific, concrete issues, meaning the core redesign is functioning as intended in his hands, not just in a throwaway-database test.
- Real-usage fix #1 (Skirting/Trim) deliberately fixed at the stored-category layer, not the display layer, once the commission-category impact was confirmed real — per the brief's own explicit instruction not to "just rename the displayed text if the underlying category is genuinely wrong." Also updated `ENTITY_TYPE_LINE_CATEGORIES` (the Robust Owner Delete reference-check) and `_quote_line_sort_key()` and added a `.badge.skirting` style, both found while tracing every consumer of `category`, not assumed unaffected.
- Real-usage fix #2's "Real cost" field deliberately left untouched — the brief gives a confirmed sell rate (R350 incl VAT/bag) but no real-cost figure, so nothing there is guessed at.
- New Quote Pre-Population bug's likely trigger, confirmed not assumed: the persistent summary panel (yesterday's Phase 3) puts stale quote state front-and-center immediately on landing, where previously it sat in an easy-to-miss table further down the page. This is a visibility change, not evidence the redesign introduced the underlying gap — the Blinds tile's missing reset call predates the redesign entirely; the redesign just made a pre-existing bug impossible to miss for the first time. Swept every remaining `goToTab('quoteBuilder')`/`showRawSection('quoteBuilder')` call site to confirm this was the ONLY gap — the "open an existing quote" path correctly does not reset (loads real data via `loadQuote()`, by design) and `applyNavState()`'s browser-back restoration correctly does not reset either (restoring history is its whole job).

### Deferred / parked (and why)
- Dynamic Flooring-tab label (reading the selected product's real `flooring_category` instead of a hardcoded "Vinyl") — the smaller remaining Phase 4 item, deliberately left for its own tiny pass. It's genuinely inert until a second flooring type (Carpet/Novilon/Broadloom/Carpet Tile) actually exists in the price book, which it doesn't yet.
- ~~Every finding from the Full Real-Browser Walkthrough & Audit... logged above for Burgert's review and prioritization, deliberately not fixed in-session~~ — **four of five approved and fixed same day** (Audit Findings: Fixes brief, see above): Stairwell Order Sheet omission, the Discount trap, the `preferred_branch` default, and the Order Index search focus race are all fixed and verified. Only the stale QA client cleanup remains genuinely outstanding — not code, a real-data deletion action for Burgert himself (see "Open going into next session" below) — and the Blinds empty-price-book item was confirmed as a data gap needing no fix.

### Open going into next session
- ~~The entire Vinyl Quoting UX Redesign (Phases 1–4) has not been seen live by Burgert at all~~ — **fully resolved same day, across two sessions.** First pass confirmed category tabs, the persistent summary panel, Sales Owner/Branch defaults, the version badge, Transport & Courier's new position, and Add Floor Job. The Full Real-Browser Walkthrough & Audit (same day, second pass) then click-tested everything that first pass left open: stairwell line editing (12→15 stairs, confirmed no duplicate), and live previews for Skirting/Trim/Stairwell/Stairwell+landing (Blinds' own preview mechanism confirmed correct too, but untestable end-to-end — the price book has zero Blinds products, see the audit finding above). Only the mobile bottom-bar layout of the summary panel remains genuinely untested live — `resize_window` did not actually resize the real browser window in this session's environment, a tooling gap, not a deferred choice.
- Dynamic Flooring-tab label, per Deferred above — small, independent, no urgency until a second flooring type is added to the price book.
- Given a distinct builder-rep "skirting" commission rate has never actually been configured (no commission UI exists yet at all — `create_commission_rate()`/`update_commission_rate()` are owner-only API endpoints with no frontend wired to them), worth flagging to Burgert directly that this is now available correctly if/when he wants a skirting-specific rate, since it silently could not have worked before today.
- ~~New Quote Pre-Population fix — ... has not been click-tested live~~ — **click-tested live same day** as part of the Full Real-Browser Walkthrough & Audit: created a lead, converted it to a genuinely new quote, and confirmed it started completely empty (no residual client/lines) — the specific fixed entry point (Blinds tile) wasn't re-tested directly, but the broader "New Quote always starts clean" behavior this fix protects was exercised and held.
- ~~Flooring live-price regression is explicitly UNRESOLVED~~ — **now confirmed live FOUR times over** (jsdom simulation ×2, a real production browser, and now a real browser against a disposable local copy during the full walkthrough) — never reproduced once. Treat as resolved unless it recurs.
- **Stale QA/test client records still need deleting from real production** — identified precisely (see the Audit Findings: Fixes entry above), but not performed: this session doesn't perform permanent data deletion directly regardless of instruction. Burgert: on the real Order Index, each stale job (the ones showing test-sounding client names in Needs Attention) has a ⋮ overflow menu → Delete; if blocked by real recorded history, the app itself offers a Force Delete override (Owner-only) — that's the exact, already-built mechanism for this, not a new one-off.
- `jsdom` and Claude in Chrome are both proven, working options in this environment (installed/connected earlier same day) — this session added a third real tool: minting a `UserSession` token directly in a disposable database copy to get a real, authenticated browser session without ever touching a password, letting Claude in Chrome drive a fully isolated local instance (throwaway `bolton.db` copy + a backend on :8010 + a frontend on :8090 with `shared.js`'s `API` constant repointed only in that copy) instead of the live production app — worth reusing for any future audit/testing session that needs to freely create/accept/delete test data. Test data left behind from the EARLIER same-day production verification: a real Price Check (#100, Aspen Herringbone Range 2mm, ~120m² Flooring + Screed) exists in the live database — harmless by design, left as-is rather than deleted without being asked. The Full Real-Browser Walkthrough & Audit itself touched zero production data — everything (Lead, Quote #26/J-0004, Order Sheet O-0007) lived only in the disposable copy, which was discarded when its throwaway backend/frontend servers were stopped at the end of the session.
- **Carpet Calculators: backend built and verified, frontend UI genuinely not started.** No Quote Builder tab, sub-selector, or input form exists yet for any of the four new categories — every verification this session (all four categories' math, the Order Sheet, Sales-role stripping, the Vinyl-safety check) was done via direct, real, authenticated API calls against a real running backend, the same verification method used for every other fix today, but a real person still cannot add a carpet line through the actual app yet. Natural next step, not a deferred/parked item — the calculation engine and data model it would sit on top of are done and confirmed correct.
- Carpet Calculators' provisional/unconfirmed numbers to revisit: Fotakis gripper pricing (R600/122LM box) is explicitly flagged by Burgert as provisional pending an updated price list "expected this week" — check whether it's arrived; Cushion Vinyl's own cutting fee defaults to R0/off (genuinely unconfirmed, not assumed either way — see the Amble reference file's own unexplained R543 figure); whether NEXBAC 920 tiles need adhesive at all (built as an optional/toggleable glue param, defaulted off) is still open too.
- **"Product Selection Doesn't Proceed" — the literal scenario (stuck on the Carpet tab's own dropdown, Berber Point 920/Glued Down specifically) is unresolved, not fixed.** The connected `carpet_category` gap found and fixed the same day (NEXBAC 920 Tile mis-bucketing/edit-routing, see above) is the most plausible real explanation given the timing and shared feature area, but that fix doesn't touch the Tufted/Needlepunch/Cushion Vinyl code path at all. If a tester hits a genuine dead-end on one of those three types specifically after this fix is live, treat it as a fresh, still-open bug — not something already covered.
- **Owner-Only Calculation Breakdown Toggle — two discretionary interpretation calls worth a second look from Burgert, not obviously wrong but not spelled out word-for-word in the brief either:** (1) §0a's "trim the default view down to just Price (ex VAT) and Margin %, move anything else into the breakdown" was read as applying to FINANCIAL calculation detail only — physical/order quantities (Vinyl's floor size, m² needed, boxes/bags to order) stayed visible by default to every role regardless of the toggle, on the reasoning that staff still need them to actually order material and build the job, and section 1's own gated-field list (list price, discount, cost components, subtotal, markup, labour, line VAT) never mentions them. (2) `labour_charged_total` (what's charged to the client for labour, vs. `labour_cost_total` — what it actually costs Bolton) was left ungated server-side, matching the pre-existing, unchanged treatment of `glue_sell_total`/`vinyl_sell_total`/`nosing_sell_total` — the frontend still only shows it with the toggle on, for Owner's own decluttering, but a Sales/Admin account could technically still see the raw figure in a network response, same as before today. If either read is wrong, both are single, easily-reversed changes (one CSS/JS conditional; one line in `strip_sensitive_fields()`'s list).
- **New Quote Starting Screen — Burgert's own "Quoting for Gansbaai?" toggle is a genuine judgment call worth Burgert's own confirmation, not a certainty.** Neither brief explicitly says whether Burgert's own account should get this toggle at all versus showing nothing and requiring Viewing As even for a same-rep branch switch — New Quote Flow Clarity's §3 only explicitly proposes Viewing As for the cross-REP case. Applied the same toggle shape to his own account too, for symmetry and because Staff Account Defaults' original text says he "genuinely works across both branches regularly" — preserving that without a control at all would have been a real capability loss, not just a display change. Easy to remove (one element, one default) if Burgert would rather his own account show nothing and rely on Viewing As exclusively even for a same-rep Gansbaai job.
- **Dual attribution ("assisted by" note) — proposal delivered, not built, per the brief's own request.** See the entry above for the full reasoning: recommended closing two real, pre-existing AuditLog coverage gaps (quote creation isn't logged at all; Misc line adds aren't either) instead of building a new field, since the existing real-actor-tracking infrastructure (`get_current_username()`, deliberately Preview-Mode-proof) doesn't fully cover its own existing job yet. Awaiting Burgert's decision on whether either the recommendation or the original "assisted by" idea is worth building.
- **Margin Becomes Owner-Only; Price Gets a Colour Signal — the "Explain low margin"/"Explain low overall margin" click-through itself wasn't click-tested live end-to-end in-browser**, unlike everything else in that entry: both trigger a native `window.prompt()`, which blocks Claude in Chrome's own automation the moment it fires (a documented tooling constraint, not something about the app). Verified instead by calling the exact same `PUT .../low-margin-reason` endpoints the prompt's own `fetch()` call hits directly, confirming the full round trip (save → `needs_low_margin_reason` flips false → reset on next recalculation), and by reading `explainLowMarginLine()`/`explainLowMarginQuote()` (quote-builder.js) to confirm they're wired to the exact onclick handlers rendered next to a real red line/quote. Worth Burgert clicking through once for real, on a genuinely low-margin line, to see the actual prompt dialog himself.

---

## 2026-08-28

### What shipped
- **Job Workflow Phase 1** — supplier-merge rewrite for `generate_order_sheets()`, retiring the `materials_ordered` manual checkbox in favour of a real derivation from Order Sheet status, and a new On Hold capability (`on_hold_reason`/`on_hold_at`, `POST /quotes/{id}/hold`/`resume`).
- **Job Workflow Phase 2** — the numbered step indicator (`_job_steps()`/`renderJobStepsHtml()`) and compact per-Order-Sheet procurement tiles inside Job Detail.
- **Job Workflow Phase 3** — retired the two redundant Order Sheet surfaces on Job Detail (the compact list card and the full inline "Order Sheet Preview" panel); moved their real functionality (Document Preview + the 5-button Action Bar) onto the standalone Order Sheet Detail screen, which is now the one place with every real Order Sheet action.
- Together, these three phases implement the approved [Job Workflow Design Proposal](https://claude.ai/code/artifact/985d91e7-96f6-4b24-a649-7dbfbd9385b9) — a cross-cutting redesign proposed first, approved, then built and verified in three separately-deployed, separately-verified phases.
- **Permanent Build Log** — this file. One-time reconstruction of the full build history (129 commits, 2026-08-19 → 2026-08-28) from the real git log, plus the standing practice this entry is itself following.
- **Leads** — the first genuinely new piece of the Master Workflow proposal to actually get built: a new `Lead` table (name/contact/source/`lead_status`/notes/`converted_quote_id`), a Leads list screen with its own Next Action column, and a "Convert to Quote" action that creates a real linked Quote. First real application of the proposal's own Proof-of-Work principle — every status change requires a one-line outcome note, captured as a single `AuditLog` row, no parallel notes mechanism.
- **Leads: visit date, site address, printable day list** — same-day follow-up, from real usage feedback after Burgert actually used the Lead feature within hours of it shipping. Added `visit_date`/`site_address` to Lead (New Lead form, Detail, Edit), a new reusable `_leads_for_day()` query, and a printable "Today's Leads" day list screen reusing the existing Document Action Bar Print mechanism — an explicit stepping stone toward a future calendar feature, not a throwaway.
- **Job Detail: Needs Attention bug fixed, Quote/Invoice Preview shown by stage, real Order Sheet previews** — a third same-day round of real usage feedback on the Job Workflow redesign. Fixed a genuine disagreement between Job Detail (correctly green) and the Order Index Needs Attention list (still orange, mislabeled) for a job with materials ordered but not yet confirmed received. Quote Preview (plus a new compact payment-status strip) now shows for every pre-invoice stage; Invoice Preview only once the job is actually `completed`. Each Order Sheet procurement tile is now a real compact click-to-expand Document Preview + 5-button Action Bar, reusing the exact same component Order Sheet Detail's own preview already uses.
- **Job Card** — the next Master Workflow proposal item, pulled forward out of its originally-proposed build order on direct instruction: a printable, installer-facing document with a hard no-pricing constraint. One new field (`Quote.installation_notes`), no new tables — materials-to-load sourced entirely from the job's real Order Sheets (never re-derived separately, so it can't disagree with them), plus reference notes pulled in from wherever they already exist on the system (`Client.notes`, the original Lead's notes if this job came from one) alongside a dedicated, editable notes section for anything specific to enter right before printing.
- **Order Index row overflow: ⋮ menu** — proposed first (per that brief's own explicit gate), a kebab overflow menu approach confirmed, then built: Quick View and the Next Action button stay one tap away; Edit client, Flag, Duplicate, and Delete collapse into one ⋮ menu per row. Fixes a real, growing problem — the row had accumulated 5 actions plus 6 data columns over the day's earlier work, wide enough to force a horizontal scrollbar with Delete cut off at the right edge.
- **Sticky header flicker — re-diagnosed from scratch, not patched on top of the earlier same-day attempt.** Real root cause found one level deeper than that attempt reached: no hysteresis on the scroll-compact threshold, so genuine (not spurious) re-crossing of one shared pixel boundary during fast direction-reversing scrolling kept restarting the header's own CSS transitions before they could settle. Fixed with a real hysteresis band. Confirmed with a quantitative test of the actual mechanism; **not** marked resolved — no working browser was obtainable in this session to visually confirm it, and Burgert hasn't confirmed it live yet either.
- **Master Workflow proposal, first two build-order items: Booking visibility fix + Decline Quote reason.** `_job_workflow_info()`'s "accepted" branch (Next Action) and `_job_steps()`'s scheduling step (the step strip) both now check `installation_date`/`installation_confirmed_date` — a tentative date already sitting on an accepted job now reads "Confirm installation — [date] proposed" / a step-strip note, instead of a plain "Book installation" that was blind to it ever being set. `decline_quote()` now takes a mandatory `reason`, writes it to `AuditLog` (no new field on `Quote`), and `get_quote()` reads it back so Job Detail's declined panel shows why, not just when.
- **Version badge fixed** — real `GET /version` endpoint (Render's own `RENDER_GIT_COMMIT` env var, set automatically on every deploy, plus a process-start timestamp captured fresh every restart), fetched on login. Replaces a hardcoded `"v65"` confirmed via `git blame` to have been literally unchanged since this repo's very first commit — 130+ commits and 9 days silently unreflected.
- **Sticky header flicker, third pass.** Burgert confirmed live on his phone that the hysteresis fix (previous pass, same day) didn't fix it — his repro is a single, plain, one-directional downward scroll, flickering right as the header shrinks, which rules out repeated toggling as the cause (a single clean class toggle only ever runs its transition once). Applied `will-change`/`translateZ(0)` to `#appHeaderWrap`, forcing early GPU compositing-layer promotion — the documented fix for a CSS transition running on/inside a `position: sticky` element during active touch-scrolling, a known Mobile Safari/Chrome flicker source independent of toggle count.
- **Header Flicker: real cause found on video, fourth pass.** Burgert sent two screen recordings (brief: "Bolton-Brief-Header-Flicker-Final.docx", fetched from his Downloads folder). Frame-by-frame analysis in the brief showed the actual on-screen behaviour for the first time this bug has been chased: the header TITLE TEXT genuinely flipping between fitting on one line and wrapping to three, repeatedly, within about a second of scrolling — a text-wrap width threshold with no safety margin, a different mechanism entirely from the previous two passes. Traced two concrete, self-inflicted width triggers: the logo's `height` scroll-compact transition let `width:auto` continuously recompute the image's rendered width as a side effect (~165px→~117px, confirmed by decoding the embedded PNG's real 340×70 dimensions) — fixed at a constant 165px with `object-fit:contain`; and the new version badge (this session's own version-fix change) popping in empty→populated on page load — given a fixed `min-width`. The title itself also got `white-space:nowrap`/`text-overflow:ellipsis`, removing the wrap-vs-no-wrap failure mode structurally rather than just narrowing its remaining triggers, since CSS has no native way to add real hysteresis to a text-wrap decision.
- **Header Flicker: static by design, fifth and final pass, same day.** The fourth pass's own fix introduced a new, worse regression the moment it went live: the header fully disappeared on scroll-down, reappeared on scroll-up — not spec. Per Burgert's direct, explicit instruction: stopped patching individual scroll-linked symptoms one at a time and removed the entire category instead. All scroll-linked header behaviour deleted — the `.scrolled` class, its scroll listener, every compact-on-scroll CSS rule, the padding/height transitions, and the `will-change`/`transform:translateZ(0)` GPU-layer-promotion hack from the third pass (very likely the actual cause of the new hide/show regression — a `transform` on the same element `position: sticky` depends on is a known way to break sticky positioning on some mobile browsers; removed along with everything else). The header now renders at one fixed size, always, and stays visible purely via native `position: sticky` — nothing about it reacts to scroll position or direction at all. The title's nowrap/ellipsis and the badge's fixed min-width (fourth pass) were deliberately kept — neither is scroll-linked, both remain valid protection regardless.
- **Vinyl Quoting UX Redesign — investigated, proposed, approved, build started same day.** Full investigate-and-propose cycle per that brief's own explicit "no code until reviewed" gate: read the real quote-building flow (`index.html`/`quote-builder.js`) cold, confirmed concretely where the Add→scroll→inspect→Add→scroll-again friction actually happens (Quote Lines sits after every entry card; only Flooring has a live price preview; Screed isn't a real category, only a checkbox inside Flooring), sent a downloadable HTML proposal (persistent added/outstanding/total summary, category tabs, before/after loop) via `SendUserFile`, then revised it twice more on request — once to add outstanding-categories visibility to the summary (a real gap in the first draft, self-caught), once to add two more required sections after Burgert's own follow-up brief: **Editability** (§09) and **Designed for reuse across future flooring types** (§10). Approved with six decisive answers to the proposal's own open questions, build started same session.
- **Vinyl Quoting UX Redesign, Phase 1 (approved, built, deployed): Sales Owner default + Stairwell editability.** `defaultSalesOwnerForCurrentUser()` (shared.js) mirrors the existing `defaultBranchForCurrentUser()` exactly, wired at the same two call sites — REAL GAP CLOSED, confirmed via grep that no such default existed anywhere before this. **Stairwell editability** — REAL GAP CLOSED, confirmed by reading the actual code: every other category already edited in place; Stairwell's Edit button was explicitly hidden in the Quote Lines row template and no backend PUT endpoint existed at all, meaning changing a stairwell line (Burgert's own example, 14→16 steps) genuinely required delete-then-re-add. Added `PUT /quotes/{id}/lines/{line_id}/stairwell` (main.py), calling the exact same `calculate_stairwell_line()`/`calculate_flooring_line()` (landing) that `add_stairwell_line()` already calls — zero new calculation logic — and wired the frontend Edit button/prefill.
- **Vinyl Quoting UX Redesign, Phase 2 (approved, built, deployed): category tabs.** The plain `<select id="line_category">` replaced with a visible, always-on-screen row of seven tabs (Flooring, Screed, Blinds, Skirting, Trim, Stairwell, Misc) — Screed gets a real tab for the first time, still "flooring" underneath (no calculation-engine change), just pre-configured screed-only via the existing vinyl/screed include checkboxes.
- **Vinyl Quoting UX Redesign, Phase 3 (approved, built, deployed, landed 23:59 — the last commit of the day): persistent summary panel.** The proposal's own headline requirement — "always see what's been added, what remains, and the total, without scrolling." New `#quoteSummaryPanel`, grouped by the same seven categories the tabs use, a done row (✓ + count + subtotal) per category with at least one line, a plain muted "none yet" per category without one, plus the running total — reusing the exact same backend-computed `total_incl_vat` the existing total display already uses, never a second, independently-summed figure. Desktop: real two-column grid, sticky sidebar, `main` widened to 1200px scoped to this one screen only. Mobile: fixed bottom bar, tap to expand. STATIC BY DESIGN, explicitly — one fixed size on both layouts, nothing about it reacts to scroll position or direction, directly informed by today's own five-pass header-flicker saga on the identical `position: sticky` mechanism.

### Why (root causes, decisions, rejected alternatives)
- **Real, confirmed data-quality finding**: the same real supplier appears under three different exact strings in the live price book — `"Azura"` (floor-prep), `"Azura Distributors"` (most flooring material), `"Azura Distributors (iTe)"` (screed). The *old* hardcoded `if supplier == "Azura"` merge check could never have matched `"Azura Distributors"` — confirmed as the real mechanism behind a job showing two separate Azura sheets instead of one combined delivery. Fixed with a small, explicit, extensible alias list used *only* for the merge decision — never changes what's actually displayed or stored.
- **Real bug fixed**: `materials_ordered` was a checkbox with zero connection to whether an Order Sheet was actually placed — a real, live source of disagreement between what Next Action said and what had actually happened.
- Chose a general supplier-based merge rule over a hardcoded "Azura is special" case specifically because which suppliers cover multiple categories could change over time — a structural fix, not a one-off patch.
- **Phase 3 caught a real design gap before shipping, not after**: Phase 2's tiles only rendered while Procurement was the *active* step. Removing the old surfaces as originally planned would have made a job's Order Sheets completely unreachable once it moved to Scheduling — fixed by making the tiles persist for the whole life of the job.
- **Blinds explicitly excluded from this whole redesign**, per direct instruction — flooring gets built and proven end-to-end first; Blinds procurement tracking is deferred to its own later phase.
- On Hold deliberately kept as two nullable fields alongside `workflow_status`, never a 5th status value — matches the standing "no second status system" principle and the existing `declined_at` pattern.
- **Lead deliberately isolated from the Job workflow**, per the proposal's own risk assessment: a Lead only ever feeds INTO a Quote via `converted_quote_id`; nothing reads back. `_lead_next_action()` is a genuinely separate, smaller engine from `_job_workflow_info()` rather than a branch bolted onto it — merging the two would mix two different engines for two different stages.
- **The Robust Owner Delete startup completeness check did exactly its job**: adding `Lead.converted_quote_id` (a new FK to `quote.id`) failed app startup immediately with a clear message until a cascade policy entry was added — precisely the "can't reach production without a developer consciously deciding its policy" guarantee that check exists for. Policy chosen: `nullify` — the enquiry genuinely happened and is real history, so deleting the quote it converted into must not delete the Lead, only clear the backward-link.
- **Real bug caught before shipping** (not a live incident): the convert-to-quote endpoint's second `session.commit()` (saving the lead's new status) silently expired the already-refreshed `Quote` object still attached to that session — SQLAlchemy's default `expire_on_commit` — so the response briefly serialized the new quote as an empty `{}`. Fixed by re-refreshing it after that second commit too; caught by the project's own "verify end-to-end before pushing" discipline, not by a user report.
- No outcome-note field on Lead itself — every status change's note is folded directly into the existing `AuditLog.new_value` (`"contacted — Called 14:20, wants a site visit Thursday"`), reusing the general-purpose audit table rather than adding a parallel notes-with-timestamp mechanism, per the proposal's own explicit instruction.
- `visit_date` deliberately independent of `lead_status`, not a value or a side-effect of any status — a visit can be proposed while a lead sits in any non-terminal status, same "no second status system" discipline applied again.
- `site_address` was the *only* new field added for the day-list follow-up, not a broader address/detail rewrite — the brief's own "propose the smallest set... rather than guessing at an exhaustive list" instruction, and "what's needed" per lead on the printed list reuses the existing `notes` field rather than adding a redundant one.
- `_leads_for_day()` built as a standalone, reusable query and deliberately does NOT filter by `lead_status` — the brief explicitly frames this as a stepping stone toward a future calendar feature, where a calendar view would want to show every visit on a day regardless of outcome; status-based filtering/styling was kept a display-layer concern instead of baked into the data access.
- Since `lead` was already a live production table (created hours earlier the same day), the two new columns went through the `_ensure_new_columns()` migration list, not just a model change — added to both together this time, the exact discipline learned from the `documentarchive.is_accepted_version` incident (2026-08-27).
- Real bug caught before shipping: the day list's Print button originally serialized the day's lead data straight into an `onclick` attribute via `JSON.stringify` — a name or note containing an apostrophe or quote (e.g. "O'Brien Family") would have broken it. Fixed to read from a cached variable instead, same convention as `leadsCache`/`orderIndexQuotesCache` elsewhere in this codebase.
- **Needs Attention root cause, confirmed not guessed**: the brief's own hypothesis was "two different code paths" — checked directly by reading `list_quotes()`/`get_quote()` side by side, and ruled out: both already read the identical `_job_workflow_info()`, with `materials_ordered` already derived identically for both. The real defect was one level deeper — inside that single shared function, two genuinely different states (materials not ordered vs. ordered-but-receipt-unconfirmed) reused the identical `"Materials required"` label, so a job that had genuinely progressed still displayed as if nothing had happened. Fixed with a distinct label in the one already-shared function, per the brief's own "one source of truth" instruction — no second fix, no new code path introduced.
- Quote/Invoice Preview conditional logic reads `workflow_status` directly (the same field `_job_workflow_info()` already uses to decide "invoicing is relevant now") rather than a new flag — guarantees the preview pane can never disagree with what Next Action itself says.
- Order Sheet preview's Edit button question (raised explicitly by the brief as open) resolved by reading the actual behaviour rather than guessing: for `docType='ordersheet'`, Edit already just navigates to Order Sheet Detail — identical to what the tile's own click used to do — so there's no real inline-editing risk, and Edit was kept rather than dropped.
- Job Card's materials sourced entirely from Order Sheets, never QuoteLineItem directly — same "reuse the exact same data, so the two can never disagree" reasoning as the Order Sheet previews above; a known, deliberate consequence is a Blinds-only job showing no materials today, since Blinds never produces an Order Sheet at all (Job Workflow Design Proposal's own scope boundary) — not a bug, a future phase's problem.
- Job Card's action bar deliberately NOT the standard five buttons, per the brief's own explicit "confirm which actions genuinely apply, don't include all five out of habit": Edit excluded (view-only per the original proposal), Save/Dropbox-archival excluded (a Job Card is a regenerate-on-demand operational aid, not a document of record the way Quote/Invoice/Order Sheet are — archiving it properly would mean extending `ARCHIVE_CATEGORY_FOLDER` for comparatively little benefit), Mail kept (genuinely cheap, a plain mailto with no pre-filled recipient since "the installer team" has no stored contact record anywhere in this app).
- PaymentFollowUp entries deliberately excluded from Job Card's pulled-in notes despite being real per-job notes too — they're payment-chasing records, not installation information, and showing them on an installer-facing document would work against the same "operational, not commercial" principle the no-pricing constraint itself exists for.
- Order Index overflow: proposed before building, per that brief's own explicit gate — presented the row-growth analysis and two candidate approaches (kebab menu vs. responsive reflow), Burgert chose the kebab menu. One unified fix for desktop and mobile rather than two different treatments, since it solves both symptoms (horizontal scroll on desktop, wrapped-link clutter on mobile) with the same mechanism.
- **Sticky header flicker root cause, confirmed one level deeper than the earlier same-day fix reached, not assumed to be the same bug**: that fix was real and correct as far as it went (throttling how often the scroll handler ran per burst, confirmed then with its own quantitative test), but it never added hysteresis to WHAT the handler decided. A single shared threshold with no gap between "compact" and "expand" means fast, direction-reversing scrolling genuinely re-crosses that one pixel value across many separate frames — not a redundant-execution problem the earlier throttling fix could have caught, a genuine, correctly-computed toggle firing once per frame for as long as scrollY keeps crossing it, each one restarting the header's own CSS transitions before the last one settles.
- Ruled out, not skipped, the brief's other named suspect (Chrome mobile dynamic toolbar / `100vh` interaction) — confirmed by inspection that neither `#appHeaderWrap` nor `header` uses any `vh` unit; the only `100vh` usage anywhere in this codebase is the login/reset-password screens and a photo lightbox, none visible while scrolling the app itself.
- Also reviewed, per the brief's own instruction to check what changed later today: whether the new `.job-detail-preview` sticky element (added during today's Job Detail layout work) could be a new contributor. Ruled out — it's an independent sticky context further down the page with no shared threshold logic, and the brief's own reproduction wasn't Job-Detail-specific.
- **Booking visibility root cause, confirmed by inspection not guessed**: the Master Workflow proposal's own current-state audit had already found `installation_date` and `installation_confirmed_date` were genuinely two separate, already-correct fields — `installation_date` settable via the Job Details form alone, never touching `workflow_status` — so this was a UI/logic gap over a correct schema, not a data-model problem. Confirmed directly that `installation_confirmed_date` is only ever set inside `schedule_quote()`, together with the flip to `"scheduled"` — so while a job sits at `"accepted"` it's structurally always `None`, meaning `installation_date and not installation_confirmed_date` is exactly "a date's been proposed but not confirmed," checked explicitly rather than assumed so this keeps working even if that invariant ever changes. Deliberately does NOT flip `workflow_status` itself — only the existing "Confirm Installation — Book" action still does that, matching the proposal's own Test 4 ("tentative date must not auto-confirm").
- Decline Quote reason scoped identically to On Hold's existing pattern (`hold_quote()`) rather than inventing a new one: mandatory reason, one `AuditLog` row, no new field on `Quote`. Found by the proposal's own instruction to sweep the whole system for the "marked done, zero evidence" shape, not just check Leads — Complaints/Reworks/Repairs/Reinstalls don't exist in Bolton yet, so Decline Quote was the one real, live instance.
- `decline_reason` deliberately read back from `AuditLog` inside `get_quote()` (most recent `field=="declined"` row for that quote), never cached onto `Quote` itself — keeps the "no new field" constraint honest while still letting the declined panel show why, not just when. Correctly shows nothing for quotes declined before this fix shipped — no reason was ever captured for those, and none is invented.
- Version badge deliberately NOT another manually-bumped counter — that's exactly the mechanism that let it go stale for 9 days/130+ commits the first time. Derived instead from two things that are always real: Render's own deploy-time env var and a startup timestamp, neither of which anyone has to remember to touch.
- **Real mistake caught and fixed same-session, not left standing**: `get_version()`'s own docstring claimed "No auth" because the endpoint declares no per-endpoint `Depends()` — wrong, caught by actually curling the live endpoint (401, not 200) rather than trusting the assumption. The closed-by-default `require_auth` middleware (2026-08-20) covers every path not explicitly in `PUBLIC_PATHS` regardless of per-endpoint declarations — exactly the guarantee that middleware exists for. Not a functional bug (the one real caller, `showApp()`, only ever runs post-login), just a wrong claim in the comment, corrected once actually checked.
- Sticky header, third pass: also tried converting the logo's `height` transition to a compositor-only `transform: scale()` — reverted before shipping once it became clear that stops the shrunk image's layout space from actually being reclaimed, quietly working against the compact header's whole stated purpose ("more page fits above the fold on mobile"). A real regression traded for an unconfirmed gain, not worth it — left that transition exactly as it was.
- Ruled out repeated toggling as this report's cause by re-reading the actual JS: the hysteresis band means a single monotonic downward scroll only ever crosses the compact threshold once, so a single clean toggle firing its transition once was already the confirmed behaviour before this pass — the remaining candidate had to be something that happens even on ONE clean toggle, not something reintroducing multiple toggles.
- Fourth pass on the header flicker deliberately given real video evidence to work from, not another round of code-reasoning-only diagnosis — the brief itself frames it as "please watch it before touching any code." This is why it found a materially different, more specific mechanism (text-wrap oscillation) than either of the first two passes did, both built from code reading and simulated scroll-position math alone.
- CSS genuinely has no mechanism to give a text-wrap decision "hysteresis" (a buffer zone) the way the earlier scroll-threshold fix could — wrap-or-not is a binary the browser recomputes from available width with no memory of its previous state. `white-space:nowrap` + ellipsis was chosen over trying to further narrow the width triggers because it removes the SECOND LAYOUT STATE entirely, closing off the failure mode rather than just making it statistically less likely to trigger — including against a cause outside this codebase's control (mobile Chrome's own address-bar-driven viewport resize during scroll), which no amount of width-trigger-chasing here could ever fully rule out.
- `min-width:0` added to the h1's parent flex-item div specifically because flex items default to `min-width:auto`, which silently prevents a flex child from shrinking below its content's natural width — the exact CSS gotcha this codebase already has direct precedent for (Mobile Rendering Audit brief, 2026-08-26) — without it, `overflow:hidden`/ellipsis on the h1 would never actually engage.
- Static-by-design decision explicitly Burgert's own, not a default fallen into after running out of ideas — the brief instructed stopping the symptom-by-symptom pattern outright, given four separate scroll-linked fix attempts on one mechanism (shrink flicker → width-lock → badge-width → hide/show) had already accumulated. Trading away the compact-on-scroll visual polish for eliminating the entire bug category was an explicit, stated tradeoff, not a compromise reached by default.
- The hide-on-scroll-down regression's likely cause (not just guessed at, reasoned from a known, documented browser compatibility issue) — a CSS `transform` on the same element `position: sticky` is declared on can interfere with the browser's own sticky-positioning computation in some mobile implementations. This was added by the THIRD pass specifically (`will-change`/`translateZ(0)`, intended to fix the shrink-transition flicker via early GPU layer promotion) — its removal here is very likely the actual fix for the new regression, not merely incidental to the broader cleanup.
- **Vinyl Quoting UX Redesign investigation found two things already correct that the brief worried about** — Screed's job-type default is already "Smooth" (first `<option>`, no code needed), and the main floor job's screed calculation already reuses the flooring room's own m² (one shared `#fj_floor_m2` field) — confirmed by reading the actual HTML/JS rather than assumed, so neither got rebuilt.
- Sales Owner default deliberately a lookup map (`STAFF_DEFAULT_OWNER`), not `currentUser.username` used directly — even though the three real values already match usernames one-to-one, a Trusted Tester or future account with a username outside that set falls through to an explicit `'burgert'` fallback instead of silently setting an unmatched `<select>` value, same defensive shape `defaultBranchForCurrentUser()` already uses.
- Stairwell edit endpoint's `product_changed` tracking deliberately checks only the vinyl product id, not the nosing product — confirmed by reading the model that `QuoteLineItem` never stored a separate `nosing_product_id` in the first place (only baked into `product_name`/cost totals), so there's nothing to compare a nosing change against; not a corner cut, a real schema constraint stated honestly rather than worked around with a guess.
- Category tabs kept the underlying `<select id="line_category">` in the DOM (hidden) rather than removing it, specifically because half a dozen existing call sites across three files already read/write its `.value` directly (`pendingCategory` deep-links, `editQuoteLine()`, the landing-tile dispatcher) — replacing it outright would have meant re-touching and re-verifying every one of them for a purely visual change with zero functional upside.
- Screed deliberately NOT given its own `line_category` value on the backend — doing so would have meant a real (if small) backend/data-model change for what the brief explicitly scoped as UX-only; pre-configuring the existing vinyl/screed checkboxes from a tab click gets the same discoverability outcome for zero calculation-layer risk.
- Persistent summary panel's total deliberately reuses `data.total_incl_vat` from the same `get_quote()` response `loadQuote()` already displays elsewhere on the page, never a second sum of `line_total` across the cache — the per-category SUBTOTALS shown in the panel are a fresh sum (no backend equivalent exists to reuse), but the one number that has to agree with everything else on the page never gets a second, parallel calculation.
- The summary panel's "Calculated" state deliberately approximated as "this is the currently active tab" rather than tracking real per-field dirty state — the latter would mean a change listener on every input across five different entry forms just to answer a display question, a real, non-trivial addition for a nice-to-have distinction the brief itself didn't demand precision on.
- Widened-`main` scoping mechanism (a body class) deliberately reused for the mobile bottom bar's clearance rule (`#backToTopBtn`/`#mobileBackBtn` `bottom` offset) rather than inventing a second hook — one class, two unrelated-looking CSS consequences, still one source of truth for "is Quote Builder the active screen."
- Two stale-cache gaps (`createQuote()`/`startPriceCheck()` never calling `loadQuote()`; `currentQuoteLinesCache` never reset in `clearStaleQuoteResidue()`) were pre-existing, not introduced by this work — confirmed by reading the functions' own existing comments, which already described the first gap without having closed it. The new summary panel simply made both visible for the first time by being the first thing to actually need `currentQuoteLinesCache` to be correct before the first line was added.

### Deferred / parked (and why)
- Blinds procurement tracking (Order Sheet `sheet_type` extension, `generate_order_sheets()` branch) — deliberately deferred to its own phase, not bolted onto this one.
- The rest of the Master Workflow proposal (formal payment ledger, installation "In Progress" state, Job Sign-off, booking visibility fix, Decline Quote reason) — approved for full build, not yet started; Job Card was pulled forward ahead of these on direct instruction.
- A full calendar visualizer for leads — the day list is an explicit, acknowledged stepping stone toward one, not the real thing; only the underlying query is meant to survive unchanged when that gets built.
- Job Card Dropbox archival (Save) — deliberately not built this round, per the reasoning above; worth revisiting if it turns out Burgert does want a permanent record of what a specific installer was actually handed.

### Open going into next session
- Live, interactive click-through verification of the Leads screens on real production data was an open item earlier today — resolved better than planned: Burgert actually used the live feature himself within hours of shipping and sent real usage feedback (this same day's second entry above), which is stronger evidence than a self-driven click-through would have been.
- Same limitation applies to today's third and fourth entries (Job Detail fixes, Job Card) — verified thoroughly via Node vm integration harnesses loading the real production files together, and confirmed the frontend/backend redeployed and stayed healthy live each time, but this session had no login credentials to click through any of it on Burgert's actual data. Worth a real look next session, or whenever Burgert next opens J-0002 and its Job Card.
- ~~The sticky header flicker is explicitly NOT closed out~~ — **superseded later the same day**: chased through five total passes, a new regression along the way, and finally CONFIRMED RESOLVED live by Burgert himself. See the later entries below for the full arc; left this line in place rather than deleted so the "not yet confirmed" state earlier in the day is still visible in the historical record.
- The Order Index ⋮ menu is new UI, not yet seen live by Burgert either — low-risk (verified via a Node vm harness: correct markup, correct open/close/mutual-exclusion behaviour, correct owner/child-row gating) but genuinely new, worth a quick look next time the Order Index is open.
- Build order for the rest of the Master Workflow proposal, smallest/lowest-risk first: ~~booking visibility fix + Decline Quote reason~~ (done, this session) → Order receiving state + Installation "In Progress" + Sign-off → PaymentRecord ledger.
- Booking visibility fix + Decline Quote reason verified directly against the real functions in a throwaway copy of the local database (never the real local `bolton.db`, never production) — `_job_workflow_info()`'s three real branches (no date / tentative date / confirmed-and-scheduled), `_job_steps()`'s scheduling note, and `decline_quote()`'s reason-required + AuditLog-write + `get_quote()` read-back all confirmed correct this way. Not yet clicked through live by Burgert — no login credentials in this session, same limitation as the rest of today's work.
- Version badge fix deployed and confirmed live (both backend `/version` and the frontend fetch call) — low-risk, cosmetic, but like everything else today not yet seen by Burgert.
- **Header Flicker fourth pass deployed and confirmed live** (styles.css/index.html redeployed, verified directly against the live frontend). Immediately surfaced a new regression (hide/show on scroll) the same day — see the fifth pass below. Worth knowing for any future session: the fourth pass's diagnosis (text-wrap oscillation) was real and its own fix stands; the regression traced to a DIFFERENT line the third pass had added, not anything the fourth pass itself introduced.
- **Header Flicker CONFIRMED RESOLVED, live, by Burgert himself** — "The flicker is confirmed fixed" (his own words), after the fifth pass (static-by-design, no scroll-linked behaviour at all). This closes out a bug that took five separate passes across one day — first genuine live confirmation of any of them. Worth remembering the shape of this one: three passes built from code-reasoning-and-simulation alone (hysteresis, GPU-layer promotion) each fixed a real, distinct mechanism but never the one Burgert was actually seeing; the turn came from real video evidence (fourth pass) and finally from removing the whole category of behaviour rather than chasing one more symptom (fifth pass, Burgert's own explicit call).
- **New regression Burgert found in the same live check**: the header fully disappeared on scroll-down, reappeared on scroll-up — a side effect of the fourth pass's own `will-change`/`translateZ(0)` fix interfering with `position: sticky` on his phone specifically. Fixed by the fifth pass above; separately confirmed deployed, not yet separately re-confirmed live (the "flicker fixed" confirmation predates this specific regression report — see his message ordering this session).
- **Vinyl Quoting UX Redesign — full cycle same day**: fetched from Downloads (flagged as pending the previous entry), investigated, proposed (sent as a downloadable HTML document, not a code change), revised twice on request (a self-caught summary gap, then two brief-mandated additions — Editability and Designed for reuse), approved with six decisive answers, and build started — Phase 1 (Sales Owner default, Stairwell editability) and Phase 2 (category tabs) both shipped and deployed same session. Phases 3–4 (persistent summary panel, live preview for the remaining categories, widened desktop layout, dynamic Flooring-tab label) not yet started — see Open below.
- ~~Vinyl Quoting UX Redesign build order, remaining: Phase 3~~ — **done, this session**, landed as the last commit of the day (23:59). Built static-by-design from the start per the note this bullet originally carried, not as a lesson relearned — no `.scrolled`-style class, no scroll listener anywhere near the new panel.
- **Vinyl Quoting UX Redesign, Phase 4 — not yet started, and flagged with a real reservation, not just "next up."** Live price preview for Blinds/Skirting/Trim/Stairwell/Misc was approved to ship WITH the layout, not as a fast-follow (Burgert's own explicit call). Investigated how Flooring's OWN live preview (`fjCalc()`) actually works before starting Phase 4: it's a genuine client-side JS reimplementation of the pricing formula, a "shadow calculation" already accepted for that one category (and already bitten once — a hardcoded 15% VAT that drifted from Business Settings, fixed separately). Building four more of these (Blinds/Trim/Stairwell each have a real, distinct formula; Misc is closer to a passthrough) means either following that same accepted-but-risky pattern four more times, or introducing a new, more consistent approach (a backend preview endpoint reusing the real `calculate_*_line()` functions with no DB write) — a real design choice, not just implementation effort, deliberately not decided silently mid-session. Dynamic Flooring-tab label (reading the selected product's real `flooring_category`) is much smaller and untouched by this question — worth doing either alongside Phase 4 or as its own tiny pass.
- Phase 3 verified the same way as Phase 1/2 — full syntax checks across every touched file, HTML tag-balance count (348 open/close `<div>`s, matching before and after), and careful manual tracing of every entry point that shows the Quote Builder screen (`createQuote()`, `startPriceCheck()`, opening an existing quote, `resetQuoteBuilderUI()`) to confirm the panel renders, hides, or refreshes correctly at each one — no browser available in this session to click through it directly. One real bug caught and fixed before shipping in that tracing pass: an inline desktop `top` value would have combined with the mobile bar's `bottom:0` to stretch the panel full-height across a breakpoint change, not just look slightly off.
- Phase 1/2 verified: `edit_stairwell_line()` run directly against real quote/line data in a throwaway local-DB copy (recalculation, wrong-category rejection, DB persistence all confirmed correct); every touched JS file syntax-checked; category-tab sync logic traced by hand through every call site (`toggleLineFields()`, `fjOnIncludeChange()`, `editQuoteLine()`) since no browser was available to click through it. Not yet seen live by Burgert.

---

## 2026-08-27

### What shipped
- **Real, urgent bug fixed**: passwords silently reverting on every deploy. Three incident-remediation functions (`_old_password_still_works_remediation()` and two others) ran unconditionally on *every* startup and force-reset three real accounts back to one hardcoded historical password hash whenever the current hash didn't match — with no guard against a legitimate later change. Removed from `on_startup()`.
- **Database Backups** — daily/weekly automated Postgres backups (pg_dump preferred, pure-Python JSON fallback), stored in Dropbox, 7-daily/4-weekly retention with automatic pruning, plain-language restore documentation (`RESTORE_BACKUP.md`), a verification-only content preview so a real backup's contents can actually be inspected.
- **Dropbox long-lived refresh token** support, replacing the short-lived access token that required manual renewal.
- **Dropbox Document Archive v2** — extended the existing Quote-only archiving to Invoices and Order Sheets, plus a nightly Order Index CSV snapshot (new in-process APScheduler, since no scheduling infrastructure existed anywhere in the repo before this).
- **Dropbox folders flattened to per-branch** (`Bolton/Gansbaai/`, `Bolton/Hermanus/`, blank branch falling back to `Bolton/Unassigned/`) and a new **Send button** (Quotes/Invoices/Orders) opening the user's default mail client, with a clear "no email on file" message when there isn't one.
- **Standard Document Action Bar** — one shared View/Edit/Print/Save/Mail component across Quote/Invoice/Order Sheet, replacing three previously-inconsistent per-screen implementations. Order Sheet quantity edits are now genuinely enforced server-side once "placed" (previously UI-hidden only).
- Two **real production bugs found and fixed** in the PDF rendering pipeline while verifying the above: xhtml2pdf crashing outright (not a clean error) on `@keyframes`/`@media print` at-rules, and separately on CSS custom properties (`var(--x)`) — both had been silently 500ing every real archive call that sent the actual production stylesheet, for as long as archiving had existed.
- **Dead Code Audit** — two small, separately-deployed batches (backend, then frontend) removing confirmed zero-reference code; several ambiguous items explicitly flagged and left alone rather than guessed at.
- **Single Active Session Per Login** — the backend half already existed and was correct; the real gap was the frontend never looking at a 401 response. Fixed via the existing global fetch wrapper.
- **Old-Password-Still-Works investigation** — the password check itself was never wrong; the real gap was that self-service password changes (unlike Owner-triggered resets) never ended a user's other active sessions.
- **Real production bug fixed**: quote delete blocked by its own Order Sheets — a Postgres-enforced FK ordering issue (`ordersheet_quote_id_fkey`) that SQLite never caught locally; `_delete_quote_cascade()` had been extended piecemeal three separate times over the project's history and OrderSheet/OrderSheetLine were never added.
- **Real production bug fixed**: `DocumentArchive.is_accepted_version` was added to the model without its matching `_ensure_new_columns()` migration entry — every DocumentArchive query on production had been failing since that column was introduced.
- **Edit Quote Line In Place** — replaced the old delete-then-re-add pattern with genuine in-place edit endpoints per category, with explicit Manual Override survival rules (cleared on product/colour change, preserved on quantity-only edits).
- **Robust Owner Delete** — root-fixed the recurring cascade-delete bug (missed a table three separate times under the old hand-maintained list) via dynamic FK discovery with a startup-time completeness check; added an explicit, off-by-default Dropbox archive purge option at delete time.
- **Force Delete** override for quotes blocked by real dependency history (deposit/final payment, logged hours, linked builder estimate) — reuses the exact same cascade mechanism, never a narrower path; real historical records (hours, builder estimates) survive even a forced delete, only their link is cleared.
- **Trusted Tester Accounts** — three named, trusted non-staff people get real working accounts; everything they create is automatically labelled and structurally excluded from KPI calculations (derived at read time, not a stored flag); a "Flag for Review" mechanism for any user to flag something for Owner attention.
- Closed a small pre-existing gap: accounts could be shown as Active/Inactive but nothing could actually change that — added deactivate/reactivate.

### Why (root causes, decisions, rejected alternatives)
- The password-reversion bug's real root cause: three *incident-specific* remediation functions, written to fix one-time historical problems, kept running on every single deploy with no "has this already been legitimately changed since" guard — every redeploy silently undid any real password change.
- Chose dynamic FK discovery over DB-level `ON DELETE CASCADE` for the cascade-delete root-fix specifically because a pure DB cascade can never clean up Supabase Storage files or Dropbox archives anyway, and SQLite (used for local testing) doesn't enforce Postgres-style cascades the same way — a DB-only fix would still have left local tests blind to the exact bug class that kept recurring.
- The PDF-rendering bugs were found as a *byproduct* of rigorous verification (testing with the real, full production stylesheet instead of a trivial test string), not something anyone had reported — directly validated the project's own "verify against the real deployed app, never a success message" standing rule.
- Single Active Session and Old-Password-Still-Works were investigated and shipped as two *separately tested* changes, per both briefs' own instruction, despite sharing the same underlying `UserSession`-ending pattern.
- Database Backups deliberately excluded from the same-day Dropbox Document Archive v2 work, per that brief's own explicit "propose the safest approach rather than immediately implementing" instruction — then built same-day once explicit proceed-without-further-signoff instruction was given.

### Deferred / parked (and why)
- Full environment isolation (a separate walled-off Test environment with an Owner view-toggle) — proposed, then deliberately scaled down by Burgert to the smaller Trusted Tester Accounts version actually built.
- Various Dead Code Audit findings explicitly left alone as ambiguous rather than guessed at: `CommissionPayment` (looks like unfinished work, not dead code), the AI-Assisted Price Sheet Import path (confirmed still live, contrary to the brief's own premise).

### Open going into next session
- The three real Trusted Tester accounts weren't created yet at end of day (needed real names from Burgert) — later created (Erna/Anine/Ockert) and one-time set-password links issued.
- Live production verification for Database Backups' actual Dropbox landing wasn't yet possible (no Dropbox token configured at time of that specific commit) — resolved later the same day once the long-lived refresh token was set up.

---

## 2026-08-26

The single biggest day of the project so far — 33 commits.

### What shipped
- **Manual Override** on quote line items and quote total, Owner-only — a real urgent business need: a client had already accepted and paid a deposit under Burgert's old pre-Bolton system, at figures different from what Bolton's calculator produces, and needed entering into Bolton matching those exact already-agreed numbers rather than recalculated. New per-line and per-quote override endpoints (mandatory reason, full audit trail, revert-to-calculated), a "Manually adjusted" badge visible to every role wherever an override is in effect (Quote Builder, Job Detail, Order Index), Owner-only apply/revert respecting Owner Preview Mode.
- **Builder Portal mobile layout bug fixed** — whole page was rendering confined to the left half of the screen with blank space on the right.
- **Order Index group header row** now shows real Value/Status/Install Date instead of a blank cell, matching the individual-row column layout.
- **Post-RLS security precaution** — force-ended every session live as of the RLS-fix cutover, and reset the three real staff passwords, once Burgert confirmed the RLS fix (see 2026-08-25) was live and stable.
- **Dropbox Document Archive & Backup Layer foundation** built — the base of what would later become the full v2 archive system (2026-08-27): Dropbox integration, archived-version tracking (`is_accepted_version`), and the mechanism later extended to Invoices/Order Sheets.
- **Sticky Header** brief implemented, plus a same-day **flicker fix**, plus **Page Title** added into it, plus a full **Mobile Rendering Audit** off the back of it (card-overlap and preview-overflow bugs found and fixed).
- **Order Sheets**: v1 of Supplier Order Sheets shipped; a UX pass plus a duplicate-sheet bug fix; **Order Sheet Corrections** capability added; two separate real bug fixes to `delete_order_sheet()` for Postgres foreign-key-violation failures that SQLite's local testing never caught.
- **Self-Service Password Reset** shipped.
- **Client Info** gained company/VAT fields and multi-contact support.
- **Dashboard**: Today/Monthly Sales & Profit figures added; **Business Overview** got a Total Quote Value figure (with a same-day follow-up correction) and two separate conversion-rate calculation fixes.
- **New Quote screen**: Price Check tool and a marketing-source field added.
- **Single Active Session Per User** — the backend mechanism (ending other sessions on new login) first built this day; its frontend counterpart (acting on a 401) was the actual gap fixed later, on 2026-08-27.
- **Create New Client From Quote** action added, plus a **Name Link Uniformity** pass making client-name links behave consistently everywhere they appear.
- **Order Index Quick View** gained a document preview.
- Real bug fixed: **add-line data loss** in Quote Builder — adding a line could silently lose data; fixed alongside a **fixed display order** for line items and a **Revert to Original** action.
- Real incident: **Old-Password-Still-Works** — first fix attempt this day (the deeper root cause, missing session-invalidation on self-service password change, wasn't fully understood until the 2026-08-27 investigation).
- **Deposit Amount** field added to jobs, with a save confirmation and a sensible default.
- Closed off every remaining **orphaned-quote path** (quotes that could end up with no client link) found up to that point.
- Two **urgent password recoveries** performed directly for Burgert.

### Why (root causes, decisions, rejected alternatives)
- Manual Override: chose to mutate `line_total` directly (preserving the true calculated value separately for revert) rather than adding a parallel "effective value" field, because the real subtotal is already summed from `line_total` in four separate places in the codebase — an in-place mutation means all four keep working correctly with zero risk of one of them forgetting to check for an override.
- Manual Override deliberately Owner-only, with a mandatory reason on every apply and a permanent AuditLog entry on both apply and revert — never a silent number change.
- The client-facing printed document was confirmed (by inspection, not assumption) to already only ever render `line_total`/`total_incl_vat` directly with no generic field dump — so an override shows a clean number to the client automatically, with zero internal override language, no frontend change needed there.
- Builder Portal mobile bug root cause confirmed empirically (headless Chrome, measured actual rendered widths): a long unbroken referral-link string was forcing one table wider than its container with no overflow containment, which forced the *whole* mobile page to zoom out to fit it — not a fixed-width container as originally suspected, the opposite: an unconstrained table forcing everything else to shrink around it. Fixed with the same `overflow-x:auto` wrapper pattern already used on Order Index, Supplier Console, and Change Log — simply missed when Builder Portal was first built.
- Order Index group header fix: which value to show when collapsed (combined total vs. leave blank) was confirmed directly with Burgert rather than guessed.
- Post-RLS precaution: `usersession` held live tokens and was publicly readable via PostgREST for the entire window RLS was disabled — a token alone is enough to impersonate a user with no password needed, and it would never show up as a failed login anywhere. Force-ending every session as of a hardcoded cutover timestamp (not "all sessions ever") means nobody who logs in after the deploy is affected.
- The two `delete_order_sheet()` fixes are the same recurring pattern later confirmed as the root reason for Robust Owner Delete (2026-08-27): Postgres enforces FK ordering that SQLite's local test DB silently ignores, so a cascade delete can pass every local test and still fail in production the first time a new dependent table is added.
- Old-Password-Still-Works' first fix this day addressed the visible symptom; the real root cause (self-service password changes never ending other active sessions, unlike Owner-triggered resets) wasn't fully identified until the dedicated investigation the next day.

### Deferred / parked (and why)
- None explicitly deferred this day — an unusually large, mostly bug-fix-and-polish day rather than a proposal/scoping day.

### Open going into next session
- The RLS incident (see 2026-08-25) was fully closed out with this day's precautionary session/password reset.
- Old-Password-Still-Works needed a deeper root-cause pass — picked up and actually resolved the next day.

---

## 2026-08-25

18 commits — a security-incident day plus a major architecture rebuild (Order Index / Job Workflow), on top of a full pilot feature.

### What shipped
- **URGENT: Row-Level Security enabled on every table** (real Supabase-flagged security incident) — chased through three commits: the initial fix (`1c479ca`), a follow-up making table discovery dynamic instead of hardcoded (`e8e2900`) once a 25th, unaccounted-for table was found, and an emergency same-day fix (`40b4b65`) for a production-down crash the RLS fix itself introduced.
- **Clear the Unlinked Quotes List** — one-time, per-quote-confirmed data remediation for three specific real quotes (#48/#49/#40), executed directly against the data with Burgert's explicit per-quote sign-off; isolated into independent try/except blocks per action so one failure couldn't crash the whole startup migration.
- **Save Quote redirect** to the Order Index instead of a "Saved ✓" message, plus a root-cause fix for **Duplicate Quote silently orphaning clients**.
- **Order Index → Client Link Gap** — two real gaps found and fixed: missing "Edit client" link on non-grouped rows, and quotes silently created without a real `client_id` when a client's name was typed but the autocomplete suggestion never clicked.
- **Keyboard Dismiss on Enter** shipped (global delegated listener); a reported **Order Index Edit Button bug** investigated and NOT reproduced — most likely a stale mobile browser cache, not a live bug.
- **Remove Unwanted Auto-Focus** (3 real bugs fixed, 1 already-correct case left alone) and **Consistent Mobile Back Navigation** (History API integration + persistent back button).
- **Client Page & Quote Detail: Document Preview + Inline Edit** — one reusable document-preview component used in two places (client Order History, Quote/Job Detail), built from the real print-doc HTML so it can never drift from what's actually sent to a client; plus AuditLog entries on any line added/removed after acceptance.
- **Order Index: Group Multi-Quote Clients** addendum — clients with 2+ jobs collapse into one row showing the single most urgent Next Action across their jobs.
- **The big one: Order Index / Job Workflow Redesign + Next Action Addendum** — the first version of the 4-status workflow model (`quoted/accepted/scheduled/completed`), `job_number`, a real Quote-vs-Job structural distinction, and the Next Action / Needs Attention engine computed at read time. This is the architecture that later Job Workflow phases (2026-08-28) built on top of.
- **Client Order History**: Address + Value columns added; `_quote_totals()` extracted to kill a duplicated discount/VAT/deposit/balance formula that existed in two places.
- **Quote Builder Layout Corrections** — Order Details card relocated to the Order Index; Floor Prep genuinely nested inside the Screed tile (a previous comment had incorrectly claimed it already was).
- **Duplicate Quote + Quote Description field**, and **Order Index Bulk Delete (Owner-only)** — the existing single-quote delete was found to have no owner gate and no audit trail at all; fixed to match the new bulk endpoint rather than leaving a weaker, ungated path standing.
- **Quote Photo Attachments, Phase 1 pilot** — Supabase Storage-backed photo uploads on quotes and builder estimates, proxied through authenticated endpoints rather than public storage URLs.
- **Builder Referral Portal, Phase 1 pilot** — a fully public, unauthenticated estimate/statement flow for external builders, with commission computed at read time (never stored) from the real linked quote's final payment.
- **Login Activity Log**: fixed the real root causes of two known issues — a double-submit race creating duplicate session rows, and no way to distinguish a real logout from a 24h auto-expiry in the log.

### Why (root causes, decisions, rejected alternatives)
- RLS incident root cause: Supabase auto-generates a public PostgREST API for every table, completely independent of this app's own FastAPI auth — with RLS never enabled, anyone with the project URL could read/edit/delete every row of every table, no authentication required. `app_user` (password hashes) and `usersession` (live tokens) were the two most urgent tables.
- The dynamic-table-discovery follow-up was needed because the hardcoded 24-table list (from SQLModel classes) didn't match the Advisor's 25 flagged issues — rather than guess the 25th table's name, the fix now asks the live database for every table that exists, which is correct regardless of what that table turns out to be and self-covers any future table too.
- The production-down follow-up: `inspect`/`text` were used in the new RLS code but only ever imported at LOCAL scope inside a different function — never at module scope — so the RLS code crashed the whole app on every startup with a `NameError` the instant it ran, meaning RLS was, ironically, never actually being enabled by the code that shipped to fix it.
- Duplicate Quote's orphaning bug: `duplicateQuoteFromIndex()` prompted for "client name" as free text pre-filled with the source's name — editing that text at all (even to add a genuinely useful note) silently sent it back as a different client by name, no real `client_id`, orphaning the copy. Fixed structurally by removing the free-text override from the backend entirely — the only way to change a duplicate's client is now a validated `client_id`, never free text, so this failure mode can't recur regardless of what any future frontend sends.
- The Order Index / Job Workflow Redesign's 7 open questions were each explicitly confirmed with Burgert before building (sequential job numbers; declining as its own action, not a 5th status; invoice status computed not stored; manual override always available as an escape hatch, automation as the default path not the only path; two independent manual "materials ordered"/"ready for installation" fields, since Bolton has no real stock tracking to infer either from).
- Two real staleness bugs found and fixed while migrating off the legacy `status` field: `commission_statement()` and the Business Overview conversion-rate report were both still filtering on the old field, which the new workflow endpoints never touched — both would have silently gone stale for every job processed through the new system if not caught during this migration.
- Order Index Bulk Delete: found the pre-existing single-quote `DELETE /quotes/{id}` had no owner gate and no audit trail at all — leaving it as-is would have trivially defeated the entire point of gating the new bulk-delete endpoint, so it was fixed to match in the same pass.
- Quote Photo Attachments: confirmed with the user which storage mechanism to use before building, rather than assuming — the brief's own named reference (AI Price Sheet Import) turned out not to store files at all, it streams straight to the Claude API and discards the bytes.

### Deferred / parked (and why)
- Stairwell line editing (Sprint B) — explicitly not built; landings/nosing make a faithful pre-fill materially more complex, flagged honestly as a known gap rather than shipped half-working. Delete-and-re-add still works for stairwell lines.
- Retroactive cleanup of old orphaned login sessions, rate-limiting, and account lockout — explicitly out of scope for the Login Activity Log fix, per that brief's own stated limits.

### Open going into next session
- Post-RLS precaution (force-logout + password reset) still needed live confirmation the RLS fix was stable before it could run — done the next day (2026-08-26) once Burgert confirmed.
- Order Index / Job Workflow Redesign was v1 of the 4-status model — later substantially extended by the Job Workflow Design Proposal work (2026-08-27/28).

---

## 2026-08-24

14 commits — the Client-Side Commercial Workflow (Sprints A–D), the Screed/Floor-Prep calculator, and a Supplier Console pricing-data-quality pass.

### What shipped
- **Client-Side Commercial Workflow, Sprints A–D**, built in sequence over the day:
  - Sprint A — client creation gaps (duplicate-name warning, double-submit guard, a working Edit form for an existing client that had never been wired to the frontend despite the backend endpoint already existing) and a real quote-builder residue bug.
  - Sprint B — Edit button for existing quote lines (flooring/blinds/trim/skirting/misc; stairwell explicitly excluded as a known, honestly-flagged gap).
  - Sprint C — a simple send path for quotes (pre-filled mailto:/wa.me links), after finding the real gap wasn't the print/PDF layout (already solid) but that there was no way to hand a quote to a client beyond the browser's native print dialog.
  - Sprint D — rebuilt a completely missing Order Details + Follow-Ups card (the backend and JS were already fully correct; the HTML for it had simply never been built, so those functions had been throwing on a null reference the entire time), plus controlled post-accept edit confirmations and a Print Invoice shortcut.
- **Screed Calculator: Extra Rooms + Floor-Prep Material Calculator** — new `FloorPrepProduct` model living in the Supplier Console like every other product category, calculated and manual modes, verified against the brief's own worked reference example. Same-day UI refinement pass added collapsible per-room cards and a room-name field.
- **Courier/delivery fee clarified + reused**, plus a new, independent **Transport Levy** field — courier reuses the existing `delivery_fee_per_m2` field rather than adding a new one (confirmed with Burgert); found and fixed a real bug where Sales could see the internal delivery cost figure despite a schema comment claiming it was already stripped.
- **Transport levy relocated** into the floor job calculator as a toggle, and a new genuinely-per-job **Courier toggle** added alongside it.
- **Supplier Console UI cleanup** — Dismiss button on the import summary card, a new "Discard staged changes" safety net (previously the only way to back out of a staged import was to not click Commit and navigate away), and removal of the now-fully-superseded Como-specific manual correction tool.
- **Master Spreadsheet as System of Record** — re-import via the Standard Import Format is now a genuine update cycle (governed fields only, never touching Burgert's own trade-discount/wastage/markup settings), with unmatched existing products flagged Discontinued (never deleted) rather than silently dropped.
- **Standard Import Format** — a new deterministic, non-AI spreadsheet import path (exact-header validation, `openpyxl`), replacing the AI-PDF path as the primary/recommended route; the AI-PDF path demoted to a collapsed section, not removed.
- Two **real Supplier Console pricing bugs found and fixed**: Azura AI extraction missing plank dimensions/zone prices for some rows, and a structural Como pricing bug (box price landing directly in the per-m² field with nowhere correct to go).

### Why (root causes, decisions, rejected alternatives)
- Sprint D's real substantial finding: `saveOrderDetails()`, `logFollowUp()`, and `loadFollowUps()` — with fully correct, already-working backend support — had **no corresponding HTML anywhere in the page**. Every one of those functions had been throwing immediately on a null-element reference the whole time; Order Index displayed deposit/payment data per row, but there was never actually a way for Ryno or Madri to write it.
- Sprint B deliberately did NOT build a second, approximate recalculation engine to reconstruct a line's original raw inputs (wastage %, discount %, markup %, glue/labour rate) for editing — `QuoteLineItem` only ever stored calculated outputs, never raw inputs. Re-deriving from the product's *current* price book entry (same as adding a new line) was chosen as honest-by-construction, over guessing backward from a possibly-since-changed override — explicitly the same shadow-calculation-drift bug class this project had already found and fixed elsewhere.
- Como pricing bug root cause: the Console never had a dedicated "price per box" field, so when a supplier's sheet states box price (as Como's does), it had nowhere correct to go and landed directly in the per-m² fields instead — a genuine schema gap, not a formula bug. Reusing Azura's logic never caught this because Azura's sheet gives per-m² directly, so Azura never exposed the gap. Fixed by adding real box-price fields as the raw input and making the per-m² fields calculated-only, rejected on any direct staged edit going forward.
- Azura extraction bug root cause: two concrete, addressable prompt gaps — the AI was never told how to split a combined "L × W × T" dimension string, and this project's own earlier "compact JSON" instruction (added for a real token-ceiling bug) was ambiguous enough to be misread as license to drop optional fields on a long sheet, not just whitespace.
- Standard Import Format replacing AI-PDF as primary: every import bug hit up to this point (Como's box-price confusion, Azura's missing fields, a 150s timeout) traced back to the same root cause — an AI inferring column meaning fresh from an unpredictable PDF layout, live, with no human check before commit. A deterministic, exact-header spreadsheet format removes that inference step entirely.

### Deferred / parked (and why)
- Stairwell line editing (Sprint B) — not built; landings/nosing make a faithful pre-fill materially more complex than other categories, flagged as a known gap rather than shipped half-working.
- Persisted per-supplier extraction-mapping config (referenced by the Field Sequence Redesign brief) — confirmed not yet built, flagged rather than silently assumed to exist.
- The PDF-to-spreadsheet conversion step for the new Standard Import Format — explicitly manual/outside Bolton by design (Burgert + Claude in chat), nothing to build.
- Full 94-row hand-verification of the Como price correction — intentionally not part of the same-day commit, flagged separately.

### Open going into next session
- A confirmed cross-supplier product-name contamination ("Como Bonsai 2.0 / Como Bellagio") needed a manual rename — the price-correction tooling fixed the number but deliberately never guesses a corrected name.
- A second, related name-contamination case ("deZIGN series 200" attached to a Como product, actually an Azura naming convention) was found and alias-matched the same day, building on the previous day's (2026-08-23) verified-pricing work — see that entry below for the original finding.

---

## 2026-08-23

20 commits, dominated by two real production incidents chased to genuine root cause — a mobile auth/loading outage and a cascading Como Flooring 94-product import failure — plus Supplier Console hardening.

### What shipped
- **"Loading..." hangs forever on mobile, fixed** — bound every API request to a 20s timeout via the shared global fetch wrapper, plus a `renderWithRetry` pattern (auto-retry once, then a clear "Tap to retry") applied to every landing screen.
- **Mobile 401s after login, fixed** — moved the session token off a cookie entirely and onto a `localStorage`-backed `Authorization: Bearer` header.
- **Stale Cloudflare edge cache serving a poisoned 502 as shared.js**, fixed via a forced cache-busting redeploy — a second, separate incident layered on top of the first, initially indistinguishable from it.
- **Supplier Console**: new-supplier creation flow, an audited/staged product Delete (with a live reference-check against real quote lines before allowing it), a "Local Supplier" → "Skirtings" display rename, sticky column headers (three iterations to get right), redundant dead front-page tiles removed (Supplier Prices/Supplier Uploads — the old, unaudited, un-staged direct-write price book).
- **AI price sheet import (Como Flooring's 94-product sheet)**: a real cascading failure chased through five separate fixes in sequence over the day — request timeout too low, `max_tokens` ceiling too low, verbose non-compact JSON output wasting the token budget, a `NOT NULL` violation on zone-priced products with no flat per-m² price, and finally the Commit Changes call itself having no timeout/catch at all (so a slow 94-row commit could silently "succeed" with nothing actually saved).
- **Supplier-level Trade Discount % default** and **per-supplier zone pricing** (replacing one business-wide zone setting) — both via a new small `SupplierDefault` table, reusing the existing staged-commit-audit pipeline.
- **Real Como pricing data-quality bugs found and flagged/corrected**: a stored price for one product turned out to be a box price, not a per-m² price (AI misclassification, not a code bug); a second product had another supplier's naming convention attached to it entirely (cross-supplier name contamination).

### Why (root causes, decisions, rejected alternatives)
- Loading-hang root cause: native `fetch()` has no default timeout, and 62 of ~65 call sites across the frontend had no try/catch at all — on mobile specifically, a carrier silently dropping an idle TCP connection during a Render free-tier cold start leaves the fetch promise never settling, with no error and no way out; desktop/broadband just waits it out, matching exactly the reported "desktop fine, mobile hangs forever" split.
- Mobile-401 root cause: `bolton-frontend.onrender.com` and `bolton-backend.onrender.com` are different subdomains of `onrender.com`, which is on the Public Suffix List — browsers treat them as different *sites*, making the session cookie a genuine third-party cookie. `SameSite=None; Secure` satisfied the SameSite policy but not mobile browsers' separate third-party-cookie-blocking defaults (mobile Safari's ITP specifically) — login succeeded and set the cookie, but mobile browsers silently declined to persist or send it back. CORS and `credentials:'include'` were both already correct; this was never a missing-header bug. Moving the token to a normal `Authorization` header sidesteps cookie policy entirely rather than fighting it.
- The Cloudflare edge-cache incident looked identical to the auth bug at first (mobile broken, desktop fine, "hangs then goes blank") but was a completely separate, coincidental issue — a transient 502 from a prior deploy got cached at the edge with a spoofed 200 status. Confirmed by directly comparing `cf-cache-status: HIT` (poisoned) against a cache-busted `MISS` (correct content) — evidence-first, not assumed to be the same bug as the one just fixed.
- Como import cascade: each of the five fixes addressed a real, distinct root cause rather than symptom-patching the same error message repeatedly — the frontend's outer timeout and backend's inner timeout were coincidentally identical (an exact tie, not a deliberate margin), `max_tokens` was tuned for the smaller sheets this feature was originally tested against, the extraction prompt never told the model to output compact JSON, and the zone-priced NOT-NULL bug was a real edge case the existing "never silently convert an ambiguous price" safety logic hadn't accounted for (a zone price is a directly-extracted real number, not a guessed conversion, so using it as a fallback isn't the same risk class that safety rule exists to prevent).
- The Como box-price/per-m² confusion was confirmed (by tracing the actual mapping code) to be a real AI extraction misclassification for that specific row, not a bug in this project's own mapping/staging logic, which does no unit conversion by deliberate design.
- Trade Discount % default and per-supplier zone pricing both chosen to live in a new small `SupplierDefault` table (one row per supplier name) rather than bolted onto `BusinessSettings`, since there's no existing `Supplier` entity to attach to and each needs to persist independently per supplier.

### Deferred / parked (and why)
- Deleting the 4 duplicate "Azura Distributors" products — investigated and the tooling (audited Delete with a live reference-check) was built, but the actual deletion deliberately left for Burgert to trigger once he's seen the real reference-check result firsthand, rather than assumed safe.
- A known gap noted, not fixed: a `TrimProduct` used only as a stairwell's nosing isn't tracked by id on `QuoteLineItem`, so the delete-reference-check can't catch that specific usage.
- The 94-individual-flush pattern in the Supplier Console commit handler (one DB round-trip per row rather than batched) — flagged as a real, understood performance improvement, deliberately not bundled into the timeout fix since it trades away precise per-row error attribution, a genuine tradeoff needing its own decision.
- Full 94-row hand-verification of Como Flooring pricing against the source PDF — intentionally left as Burgert's own review step, not automated.

### Open going into next session
- The Como Flooring 94-product commit needed to actually be re-attempted and confirmed successful once all five import-pipeline fixes landed.
- The Como Bonsai 2.0/Bellagio and "deZIGN series 200" name-contamination findings needed a manual rename — picked up as a same/next-day follow-up (see 2026-08-24 above).

---

## 2026-08-22

12 commits — the Supplier & Price Book Console's foundational build, Owner Preview Mode, Login Activity Log Phase 1, a real production migration crash, and the AI-Assisted Price Sheet Import.

### What shipped
- **Real pricing bug found and fixed**: all 35 Aspen Flooring products were storing (Feb 2025 list price ÷ m² per box) instead of the real list price — corrected across the board; a 20m² job's ex-VAT cost went from R2,524.99 (wrong) to R6,796.86 (correct). Confirmed no existing quote had used an Aspen product, so no historical quote needed review.
- **Supplier & Price Book Management Console** built from scratch — per-product glue/labour rate defaults, Azura's real zone-tiered pricing (`price_zone_a/b/c`), a business-wide pricing zone setting, and plank-dimension fields that genuinely drive `tiles_per_pack` rather than sitting as reference-only data.
- **Commit-and-acknowledge workflow** — edits stage locally in the browser; nothing hits the database until an explicit "Commit Changes", which applies the whole batch atomically and reports plain-English per-field change lines, not a generic "Saved."
- **Change Log** — new general-purpose `AuditLog` table (not special-cased to pricing), read-only, no edit/delete endpoint ever, same principle as the session log.
- **AI-Assisted Price Sheet Import** — built ahead of its own stated precondition (the brief said not to start until the Console had seen real manual use; built anyway per explicit instruction) — sends a supplier's PDF/photo to the Claude API describing fields to find (not a fixed table shape, since every supplier's sheet layout differs), explicitly instructed never to convert a per-box price into per-m² itself.
- Real production migration crash fixed same day: `DATETIME` isn't a valid Postgres type name (Postgres uses `TIMESTAMP`) — SQLite had silently accepted it in every local test, masking the bug until it hit the real production database and rolled back the entire migration batch.
- **Sticky Supplier Console table headers** — three separate attempts before landing on a spec-correct fix; plus collapsible sections with per-browser memory, search-across-all-suppliers, and (later) genuinely screen-fixed scroll arrows with an `IntersectionObserver` handing off control between tables.
- Real column-alignment regression fixed (an import-work side effect had left existing-product rows one column left of their header) and a real horizontal-scroll-unreachable bug fixed (a global `table { width: 100% }` rule was silently capping the table instead of letting it overflow).
- A real markup-percent display inconsistency found and fixed — the raw stored value was always correct, but the Console showed it as a raw multiplier ("1.3") while the Floor Job builder showed the same number as "30.0%" elsewhere in the same app.
- **Owner Preview Mode ("View As")** — lets the real Owner preview the app as Sales/Admin, with genuinely stripped fields and blocked actions server-side (not hidden client-side), via one shared `get_current_role()` dependency nearly every endpoint already reads through.
- **Login & Session Activity Log, Phase 1** (session-level only — a feature-level breakdown explicitly flagged and deferred) — real logout now soft-ends a session rather than deleting the row, so there's an actual history to report.
- Real cost-leakage bug found and fixed during Owner Preview Mode regression testing: two stairwell-specific cost fields were stripped for Sales only at line-creation time, not when re-opening a saved quote — `get_quote`/`list_quotes` shared a stripping function that didn't yet know about those two fields.

### Why (root causes, decisions, rejected alternatives)
- Aspen pricing bug: confirmed the exact wrong-formula pattern held for every single one of the 35 products, no exceptions — verified against the real `calculate_flooring_line()` function, then live on the deployed backend, not just by inspection.
- AI Price Sheet Import deliberately never does unit conversion itself (per-box → per-m²) — extracts the raw price and its stated unit basis separately and leaves the conversion to a human reviewer, specifically to avoid repeating the exact shape of bug that caused the real Aspen price-book mix-up this project had already found and fixed once.
- `DATETIME`-vs-`TIMESTAMP` root cause: SQLite has no real type enforcement on column declarations, so it silently accepted an invalid type name in every local test — the bug was structurally invisible until it hit real Postgres. Every other type string in that same migration was then manually re-audited against real Postgres syntax rather than assumed safe by association.
- Sticky-header root cause (confirmed twice, across three attempts): setting `overflow-x: auto` on a wrapper div forces its computed `overflow-y` to `auto` too, per the CSS overflow spec — not overridable by also setting `overflow-y: visible` — which silently makes that div `position: sticky`'s containing block instead of the page, so a naively-applied sticky header parks at a fixed offset into the table rather than tracking real scroll. The final fix moved the header out of the scrolling wrapper entirely (a cloned header bar as a page-flow sibling) rather than fighting the spec.
- Column-misalignment bug traced to a real regression: the AI import work added a leading empty header cell for the "NEW (import)" indicator, but the renderer for *existing* products never grew a matching leading cell to match.
- Markup-display fix scoped deliberately narrow (exact field-name match, not a "multiplier" substring match) because `over_tiles_multiplier`/`removed_tiles_multiplier` are a genuinely different kind of multiplier (a screed job-type rate factor) where the raw "×1.5" display is already correct and shouldn't be touched.

### Deferred / parked (and why)
- SPC Rigid Core 6mm and Premium Stair Treads — still missing from the Aspen price book entirely, flagged rather than added, per Burgert's own choice to hold off.
- Login & Session Activity Log Phase 2 (feature-level breakdown, not just session-level) — explicitly flagged as not built this pass.
- Real extraction-accuracy testing of the AI Price Sheet Import against actual supplier PDFs — could not be done in this environment (no API key, no real documents available at the time); everything around it (staging, commit, audit log, role-gating, an atomicity bug caught along the way) was verified against real running servers instead.

### Open going into next session
- Real Supplier Console usability problems (column truncation, unreachable horizontal scroll, sticky headers) surfaced immediately once used with real multi-supplier data — chased through the first half of the next day (2026-08-23 in reverse order above) before the Como import cascade took over the rest of it.

---

## 2026-08-21

One commit — multi-tenant groundwork, done invisibly ahead of any real second tenant existing.

### What shipped
- **`tenant_id` added to every business-data table** (17 tables — the original 14 the brief named, plus the 3 price-book tables identified as obviously business-specific too) and every query scoped through a new `get_current_tenant` dependency, sourced from the logged-in user's own record, never client-supplied.
- New idempotent `_ensure_new_columns()` startup migration mechanism — added specifically because `SQLModel.metadata.create_all()` only ever creates brand-new tables, never alters an existing one, and this was the first time an already-live table needed a new column added under it. This mechanism went on to carry essentially every future schema change in the project.
- Hardcoded business-rule constants (flooring margin warning threshold, stairwell labour rates, floor-prep bag defaults, tile removal fee) moved out of `calculations.py`/`models.py` and into `BusinessSettings`, with the original hardcoded values kept as defaults — no behaviour change, just made them editable going forward.
- Two real, small findings fixed in the same pass: uploads now land under a tenant-scoped path; the printed quote's hardcoded logo can now be overridden via `BusinessSettings.logo_base64`.

### Why (root causes, decisions, rejected alternatives)
- `UserSession` was deliberately excluded from `tenant_id` scoping — it's looked up by its own random token, not by tenant, so tenant-scoping it would add nothing.
- Business-rule constants were moved to settings specifically to make them owner-editable later without a code deploy, while deliberately changing nothing about the actual formulas — the functions gained new parameters that default to the exact original hardcoded values, so this shipped as a pure plumbing change with zero calculation risk.
- Real-money verification discipline: the whole migration was tested against a real historical copy of the local database (82 flooring products, 14 quotes, 24 line items) before shipping, confirming a real historical quote's totals and a fresh quote's figures were byte-identical to pre-migration numbers.

### Deferred / parked (and why)
- No UI changes, no tenant switcher, and no way to actually create a second tenant through the app yet — explicitly scoped as groundwork only, per the brief. This remains invisible infrastructure until a real second tenant is onboarded.

### Open going into next session
- None carried forward — a clean, self-contained infrastructure day.

---

## 2026-08-20

Two commits — real per-person login (replacing a spoofable client-supplied role) and a stairwell calculator correction.

### What shipped
- **Real per-person login** — replaced the earlier self-reported "Viewing as" role dropdown, flagged in the original go-live handover as a blocking dependency, since a client-supplied role query param meant anyone could claim to be Owner just by editing the URL. Server-side sessions (PBKDF2-hashed passwords, `httponly`/`secure`/`samesite=none` cookies), three real accounts seeded (Burgert/owner, Ryno/sales, Madri/admin) with random initial passwords given to Burgert directly in chat, never committed.
- **Stairwell tread coverage correction** — tread width per stair corrected from 2 to 3 planks, per three independent flooring reps' confirmation — plus multi-landing support (L/U-shaped staircases), billed as a normal flooring material line rather than folded into the stairwell tile/glue formula.
- **Two real security gaps closed**, found while reviewing a parallel, unapplied auth patch rather than adopting it wholesale: a closed-by-default `require_auth` middleware (previously, protection depended entirely on every endpoint individually remembering to declare its own role dependency — nothing stopped a future endpoint from silently shipping unprotected), and server-side enforcement added to commission-rate CRUD, which had only ever been Owner-gated by UI convention.

### Why (root causes, decisions, rejected alternatives)
- The parallel auth patch (a bcrypt+JWT-in-localStorage design from a different dev lineage) was reviewed but deliberately not applied — it conflicted outright with the PBKDF2+server-side-session auth already built, tested end-to-end, and committed. Discarding a tested implementation for an unreviewed one wasn't worth it, but the review itself caught two real, independent gaps worth keeping regardless of which auth mechanism won.
- The closed-by-default middleware was registered *before* `CORSMiddleware` deliberately — verified live that without that exact ordering, a blocked (401) response was missing its CORS header entirely, which would have broken every blocked request from the real frontend with an opaque CORS error instead of a readable 401.
- Tread-width correction sourced from three independent professional confirmations, not a single guess — the previous 2-plank figure had been silently wrong in every stairwell quote calculated before this fix.

### Deferred / parked (and why)
- None explicitly deferred this day.

### Open going into next session
- None carried forward — real login replaced the last known spoofable trust boundary from the original handover.

---

## 2026-08-19

Four commits — the actual creation of this git repository, and the first days getting the pre-existing Phase 1 app (already at v65 per `CHANGELOG.md`) live on Render + Supabase.

### What shipped
- **`f7dc67f` — Initial commit: Bolton Phase 1, v65.** The starting point of this repository — Price Book + Quote Builder + HR & Commission, already refactored into six frontend modules (`shared.js`, `price-book.js`, `clients.js`, `order-index.js`, `quote-builder.js`, `hr.js`) plus the app shell, FastAPI + SQLModel backend, ready to switch from local SQLite to Supabase Postgres via `DATABASE_URL`. Everything before this point lived as pre-repo history — see [`CHANGELOG.md`](../CHANGELOG.md) (v1–v65) for that record.
- **Supabase migration fixed** — the hand-written `supabase_schema.sql` used snake_case table names (`flooring_product`) that never matched SQLModel's real auto-generated names (`flooringproduct`), meaning the schema file's tables had never actually been queryable by the running app at all; renamed and reordered to respect real foreign keys, plus a working one-time SQLite → Supabase migration script.
- **Live backend cutover verified** — API constant pointed at the real Render backend only after confirming the real price book (81 flooring products, 15 trims) and Business Settings data were actually present and a real calculation matched.
- **Real production bug found and fixed on the live deployed app**: the Flooring Quotes screen showed "No flooring products in the price book yet" despite 81 real products existing — a race condition where a rendering function read a product cache synchronously, assuming an unawaited page-load fetch had already finished. Invisible against instant local SQLite; trivially easy to hit against a real Render free-tier cold start (30–60+ seconds).

### Why (root causes, decisions, rejected alternatives)
- The Supabase schema mismatch was a real, structural gap — the hand-written schema file had simply never been run against real Postgres before this deployment, so the naming mismatch had never been caught until it mattered.
- The flooring-products race condition was the *same class* of bug already fixed once elsewhere in the code (an unguarded synchronous cache read racing an unawaited fetch) but had not yet been applied to this specific rendering function — found by deliberately checking every other call site for the identical unguarded shape rather than assuming the earlier fix was complete, and confirmed present in three more places (blinds/trim/skirting dropdowns) in the same pass.

### Deferred / parked (and why)
- Nothing explicitly deferred — this was the project's first days getting a pre-built app through its first real production deploy and finding the bugs only a real cold-start deploy could surface.

### Open going into next session
- None carried forward from this earliest window — the app was live, real data was confirmed present, and the immediate priority shifted to closing the spoofable-role security gap, addressed the very next day (2026-08-20 above).
